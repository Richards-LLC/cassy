- 2026-09-02 — **version-literals** — Symptom: a release test asserted a literal crate version. Root cause: the test did not use env!("CARGO_PKG_VERSION"). Release: v3.11.0.
- 2026-09-02 — **workspace-tests** — Symptom: the workspace test graph was not proven before release. Root cause: the local train stopped at a narrower compile check. Release: v3.11.0.
- 2026-09-02 — **nextest** — Symptom: a full nextest run failed on refresh and LWW tests. Root cause: cross-lane behavior changed without an assembled-tree run. Release: v3.11.0.
- 2026-09-02 — **doctests** — Symptom: doctests were omitted from an otherwise green test train. Root cause: nextest does not run doctests. Release: v3.11.0.
- 2026-09-02 — **archive-mode** — Symptom: archive extraction exhausted a 32-GB /tmp tmpfs. Root cause: the suite archive and TMPDIR were placed on tmpfs. Release: v3.12.0.
- 2026-09-02 — **archive-mode** — Symptom: archive execution rejected the workspace remap because Cargo.toml and package directories were absent. Root cause: the remap was only an empty directory. Release: v3.12.0.
- 2026-09-02 — **archive-mode** — Symptom: seven archived tests read source via CARGO_MANIFEST_DIR or invoked rg. Root cause: the queue has no checkout and no rg; snapshot tests need source-tree .snap files. Release: v3.12.0.
- 2026-09-02 — **snapshot-portability** — Symptom: wrapped output snapshots differed by host git wording, continuation layout, or temp-path width. Root cause: redaction and joining were host-sensitive. Release: v3.11.0.
- 2026-09-02 — **builtin-projections** — Symptom: root projections stayed stale after a builtin source edit. Root cause: the scoped cas update --sync resync step was skipped. Release: v3.12.0.
- 2026-09-02 — **nextest** — Symptom: marker tests from one lane contradicted trimmed guidance from another. Root cause: sibling lanes were not reconciled on the assembled tree before prep. Release: v3.12.0.
- 2026-09-02 — **nextest** — Symptom: conflicts_resolved assertions passed with the wrong meaning. Root cause: a counted field changed semantics without grepping its tests or asserting direction. Release: v3.12.0.
- 2026-09-02 — **changelog-and-versions** — Symptom: release metadata did not describe all six crates and the dated bullet section. Root cause: version bump validation was not run as a gate. Release: v3.11.0.
- 2026-09-02 — **working-tree** — Symptom: generated or scratch files were present at release close. Root cause: the final tree-clean check was treated as a manual memory item. Release: v3.12.0.
- 2026-09-02 — **release-script** — Symptom: ISA audit found duplicate BLAKE3 build outputs after a prior prewarm. Root cause: a mismatched release build left stale build and fingerprint directories. Release: v3.11.0.
- 2026-09-02 — **release-script** — Symptom: a fresh tag worktree lacked the Zig toolchain and failed preflight. Root cause: .context/zig is gitignored and was not hardlinked into the worktree. Release: v3.12.0.
- 2026-09-02 — **procedure-guardrails** — Symptom: publishing stopped with the caller after pkill -f matched its own command line. Root cause: process cleanup used a pattern instead of a recorded PID. Release: v3.12.0.
- 2026-09-02 — **manual:epic-close** — Symptom: an epic close found sibling lanes had rewritten delivered files. Root cause: the stranded-branch inspection narrative was omitted. Release: v3.12.0.
- 2026-09-02 — **manual:merge-queue** — Symptom: PR admission checks were mistaken for merge_group validation. Root cause: the synthetic queue host and pr-<n> headBranch were not watched. Release: v3.12.0.
- 2026-09-02 — **manual:slack-route** — Symptom: the documented release Slack account was unavailable or wrong. Root cause: the route was assumed rather than read-only preflighted from the supervisor session. Release: v3.12.0.
- 2026-09-02 — **manual:operator-reporting** — Symptom: manual gate fiddling obscured green-to-tag latency. Root cause: the timeline did not name the blocking step or publish moment. Release: v3.12.0.
- 2026-09-02 — **manual:host-verify** — Symptom: the host was not verified from the published receipt before the next release. Root cause: cas update, version, and hub checks were not carried into POSTED evidence. Release: v3.12.0.
- 2026-09-02 — **builtin-projections** — Symptom: root managed projections and reference history were stale after builtin edits. Root cause: the source tree changed without resync and ledger regeneration. Release: v3.12.0.
- 2026-09-02 — **snapshot-portability** — Symptom: snapshot-portability check reproduced the live cas-src doctor report instead of the fixture: the deep TMPDIR was created inside the checkout, and worktrees live under the main repo's .cas/, so find_cas_root walked up into the real store Root cause: gate placed its temp path under the worktree instead of the home-disk base outside every checkout Release: operator-reported.
- 2026-09-02 — **nextest** — Symptom: a builtins marker test pinned the exact failure-log entry count, so the first --learn broke nextest Root cause: the seed count was asserted with == instead of >= Release: operator-reported.
- 2026-09-02 — **snapshot-portability** — Symptom: the snapshot-portability row left component_output_test__doctor_snapshot.snap.new behind and the working-tree row then failed; the temp path was also unredacted when TMPDIR was outside /tmp and wrapped mid-word at 80 columns Root cause: insta pending snapshots were not cleaned and the test redacted temp paths by regex only; the renderer had no width override Release: operator-reported.
- 2026-09-02 — **archive-mode** — Symptom: archive-mode with empty package directories failed a pre-existing cwd-relative source inspection (mcp::daemon::tests::every_production_agent_pid_assignment_has_nearby_fingerprint_stamp) that passes on CI Root cause: the queue shard runner has a real checkout at a different path than the build host; only compile-time CARGO_MANIFEST_DIR reads break there, so the simulation must remap to a real checkout copy (git archive) without rg, not to empty dirs Release: operator-reported.
- 2026-09-02 — **archive-mode** — Symptom: archive-mode run placed the fixture TMPDIR under $HOME; cas update --all-projects walked up from the fixture cwd to the user-level ~/.cas and counted the home directory as a project (2 instead of 1) Root cause: find_cas_root treated the user-level store as a project root and the discovery tests relied on TMPDIR having no .cas ancestor Release: operator-reported.
- 2026-09-02 — **archive-mode** — Symptom: archive-mode fixture children walked up from a HOME-nested scratch TMPDIR into the real ~/.cas (cas setup dry-run reported the host's cloud login; cas update counted the home directory) Root cause: scratch base under $HOME has a .cas ancestor unlike the queue runner; the gate now refuses any scratch base with a .cas ancestor and documents CAS_RELEASE_GATE_HOME_DIR=/var/tmp/cas-release-gate for this host Release: operator-reported.
- 2026-09-02 — **archive-mode** — Symptom: archive-mode remap made with git archive had no .git, so root_managed_projections_stay_synced (git check-ignore) failed while it passes on the runner's real checkout Root cause: the queue runner's checkout is a git repository at a different path; the remap must be a detached git worktree of HEAD, removed after the run Release: operator-reported.
- 2026-09-03 — **nextest** — Symptom: full nextest failed because a stale supervisor liveness fixture conflicts with same-name registration supersession Root cause: cas-ef8b registration now retires active same-name supervisor rows, so the c2cc7c9 public-register fixture cannot represent a live predecessor plus shutdown successor; reconcile the fixture while preserving liveness-vs-recency coverage Release: operator-reported.
- 2026-09-03 — **workspace-tests** — Symptom: merge_group preflight failed because self-hosted Rust setup raced another lane sharing /var/lib/cassy-actions/.rustup: rustup rollback reported Directory not empty and a conflicting libaddr2line...rlib Root cause: ci.yml invoked dtolnay/rust-toolchain@stable independently in preflight, suite-archive, and doctest lanes on two listeners with one shared RUSTUP_HOME; setup must avoid redundant mutation or serialize the exceptional install under a shared lock Release: operator-reported.
- 2026-09-03 — **manual:release-credentials** — Symptom: release.sh could not see required telemetry credentials after the operator sourced the wrong or unconfigured file Root cause: the train named a host-specific path but did not define a configurable user-level release.env or prove required names without exposing values Release: v3.13.0.
- 2026-09-03 — **manual:release-artifacts** — Symptom: release log, PID, or done files dirtied the tag worktree and made clean preflight fail Root cause: runtime evidence was redirected inside the clean release checkout instead of the configured durable artifacts root Release: v3.13.0.
- 2026-09-03 — **manual:release-tag** — Symptom: local preflight reported a missing remote tag before publication Root cause: the procedure called check-release-preflight.sh --local before annotated-tag creation instead of giving release.sh --publish-tag one tag-and-preflight boundary Release: v3.13.0.
- 2026-09-03 — **manual:release-exit** — Symptom: a failed release process had no durable numeric completion receipt under an outer shell with errexit Root cause: the background command relied on the caller's wait or shell status instead of a PID-safe non-errexit wrapper that captures $? and writes done Release: v3.13.0.
- 2026-09-03 — **manual:release-receipts** — Symptom: publication was announced without complete tag, workflow, asset, latency, Slack, and host evidence Root cause: the train had no exact post-publication receipt checklist tied to the landed main commit Release: v3.13.0.
- 2026-09-03 — **manual:slack-channel** — Symptom: authenticated MechaCassy tools/list passed, but the channel read with `C0B44GUKDK2` was rejected while the rubric channel name `cas-internal` succeeded and all four posts landed Root cause: the train used the channel ID for MechaCassy preflight/post instead of the rubric channel name and did not state the direct configured mecha-cassy fallback when a live Cassy proxy lacks the registration Release: v3.13.0.
- 2026-09-03 — **manual:slack-tool-rename** — Symptom: every MechaCassy call returned denied by policy while tools/list itself succeeded Root cause: the hub replaced the four slack_* tools with mecha_read and mecha_post, and each consumer's allowlist and prose still named the retired quartet, so preflight passed and dispatch failed; pin documented tool names to MECHA_CASSY_TOOLS, verify a renamed contract against a live authenticated tools/list rather than the last-known list, and re-run `cas integrate mecha-cassy` to rewrite the machine allowlist Release: operator-reported.
- 2026-09-03 — **nextest** — Symptom: three push_queue_scoping_test rows passed locally and failed on the CI shard with zero push requests issued Root cause: the same find_cas_root ancestor class as the proxy-config entry below, in a different guard — ephemeral_project_refusal classified the PROCESS root from find_cas_root() instead of the root its CloudSyncer was built for, so locally it resolved to the real durable project and let the push proceed, while on the shard runner it resolved to the ephemeral runner workspace and refused; a guard that asks 'which project am I in' must be handed the root it is guarding, never ask the process, and any test that can run from a different cwd will find the difference Release: v3.15.0.
- 2026-09-03 — **nextest** — Symptom: nextest and archive-mode failed proxy_vercel_client_round_trip_against_fixture_mcp_server and the two lazy-init proxy tests with reqwest No provider set, then with expected empty-config Err Root cause: a host-local .cas/proxy.toml (an http MCP server registered for MechaCassy) in the main checkout was inherited by the worktree through ancestor lookup, because HOME and XDG_CONFIG_HOME redirect only user-level config while project config resolves through find_cas_root, which walks up from the cwd and maps a git worktree onto its main repository's .cas; fixed in cas-4ccc by having TestEnvGuard::temp_home pin CAS_ROOT inside its temp HOME, so no ancestor project config is reachable, with a test that plants a proxy.toml in an ancestor and proves it is ignored, plus a release-gate ancestor-proxy-config row that neutralizes such a file by pinning CAS_ROOT at an empty directory for the run and naming it in the receipt, rather than refusing and sending a human to move their own MCP config aside; adjacent finding, distinct from the 2026-09-02 archive-mode entry that already documents CAS_RELEASE_GATE_HOME_DIR: scripts/test-release-gate.sh does not set it either, so on a host with a user-level ~/.cas the self-test dies mid-run with no summary line and reads as a broken script rather than the named host condition it is (16/16 once the variable is set) Release: operator-reported.
- 2026-09-04 — **manual:parallel-release** — Symptom: a second supervisor session on the same clone had already queued its own release PR bumping to the same version, so the queued entry would have failed with add/add and CHANGELOG conflicts after the first landed Root cause: the train never checked open PRs or the merge queue for a competing version bump before the prep commit; sequence behind it and take the next patch number Release: operator-reported.
- 2026-09-04 — **archive-mode** — Symptom: archive-mode failed because a test's child `cas init` aborted on its fixed 300s watchdog while the host was saturated by concurrent compiles and idle daemons; the same tree passed minutes later on a quiet box Root cause: the init watchdog budget was a wall-clock constant with an all-or-nothing opt-out, and no gate row runs `cas init` directly — the children live inside integration tests below it, behind two deliberate CAS_* env scrubs; the gate now exports CAS_INIT_TIMEOUT_SECS=900 and both scrubs carry that one timing-only variable across, with nextest's 600s per-test kill still the backstop for a genuine hang Release: operator-reported.
