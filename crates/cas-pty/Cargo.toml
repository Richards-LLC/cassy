[package]
name = "cas-pty"
version = "0.1.0"
edition = "2024"
description = "PTY management for CAS factory mode"
license = "MIT"

[dependencies]
# PTY management
portable-pty = "0.9"

[target.'cfg(unix)'.dependencies]
# Process-group kill for PTY tree shutdown (cas-8c5a)
libc = "0.2"

# Async runtime
tokio = { version = "1", features = ["sync", "io-util", "time", "net"] }

# Serialization
serde = { version = "1", features = ["derive"] }

# Error handling
thiserror = "2"

# Structured logging (cas-5232: cwd audit trail)
tracing = "0.1"

# Home directory detection
dirs = "6"

# UUID generation for session IDs
uuid = { version = "1", features = ["v4"] }

# JSON parsing for RPC mode
serde_json = "1"

# TOML parsing for the Codex trust table (cas-28a49): a line-based matcher
# cannot tell `[projects."x"]` from an inline/dotted spelling of the same key,
# and a duplicate table would make the operator's whole config unparseable.
toml = "0.9"

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
