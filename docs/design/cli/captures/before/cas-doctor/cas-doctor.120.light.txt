cas doctor · cas-src · 3.17.2
────────────────────────────────────────────────────────────────────────────────
Store         ✓ cas directory  ✓ database  ✓ schema  ✓ factory session cas-src-lively-panther-31  ✓ tables
              ✓ entry store  ✓ memory stats  ✓ memory decay  ✓ rules  ✓ tasks
Indexes       ✓ legacy search index  ✓ search index  ✓ symbol index  ✓ embedding drain  ✓ embeddings
  ⚠ code history index  1 source(s) reporting errors — github: gh api graphql failed: gh: API rate limit exceeded for
                        71.184.143.57. (But here…; provenance 16.9% high-confidence, 24.6% any-edge
Cloud         ✓ supervisor relay  ✓ delivery retries  ✓ canonical id
  ⚠ cloud identity metadata  foreign cloud scope(s) for project `cas-src`:
                             team_project_registered_2a57bec9-5dfa-4a8f-b711-31f9aeb8d6cb_gabber-studio=2026-08-31T18:04
                             :22.829530994+00:00,
                             team_project_registered_2a57bec9-5dfa-4a8f-b711-31f9aeb8d6cb_github.com/richards-llc/mecha-
                             cassy=2026-09-02T15:59:09.927260211+00:00,
                             team_project_registered_2a57bec9-5dfa-4a8f-b711-31f9aeb8d6cb_penguinz=2026-09-01T11:54:45.2
                             21271774+00:00,
                             team_project_registered_2a57bec9-5dfa-4a8f-b711-31f9aeb8d6cb_petra-stella-cloud=2026-08-29T
                             18:05:12.828862848+00:00,
                             team_project_registered_2a57bec9-5dfa-4a8f-b711-31f9aeb8d6cb_pippenz=2026-08-31T18:12:46.69
                             1529131+00:00,
                             team_project_registered_2a57bec9-5dfa-4a8f-b711-31f9aeb8d6cb_soundwave-config=2026-09-03T13
                             :54:46.782103318+00:00
  → run `cas cloud purge-foreign --dry-run`, then `cas cloud purge-foreign`; only after the purge, run `cas cloud sync` to re-register the current project
  ⚠ registered project roots Registered root `/tmp/.tmpdInYXx` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpdInYXx`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpwxViVG` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpwxViVG`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpNFiYzI` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpNFiYzI`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpMSpDH2` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpMSpDH2`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpZE7LKN` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpZE7LKN`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpOBnUQl` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpOBnUQl`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpkyioLw` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpkyioLw`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpX7nVOn` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpX7nVOn`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpQRLYeY` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpQRLYeY`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpEBJP48` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpEBJP48`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpVyPPV0` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpVyPPV0`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpN3kYmJ` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpN3kYmJ`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpwYUtC2` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpwYUtC2`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpzfICF9` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpzfICF9`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmp4FHLXd` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmp4FHLXd`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpYgubMr` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpYgubMr`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmp7gSkxZ` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmp7gSkxZ`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpzE6Aev` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpzE6Aev`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpJcGU6o` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpJcGU6o`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpVbrlZ4` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpVbrlZ4`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpA5Jlu2` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpA5Jlu2`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpPeCbOv` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpPeCbOv`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmplvlqhD` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmplvlqhD`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpZtpkwA` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpZtpkwA`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ registered project roots Registered root `/tmp/.tmpCtI3wW` is excluded from `cas update` discovery: a disposable
                             temp root at /tmp. Remove the stale registration with `cas known-repos forget
                             /tmp/.tmpCtI3wW`. If it has a cloud link, run `cas cloud unlink --purge-remote` from that
                             root first; this explicitly removes its remote rows without changing local files.
  ⚠ cloud sync queue         72 queued content change(s) block purge-foreign (task: 72)
  → Run `cas cloud queue --retry`, then `cas cloud push`, then `cas cloud purge-foreign --dry-run`; repeat the push until this count reaches 0. The cloud refused 147 parked row(s): project_mismatch ×147 — another project already owns this id in the cloud; re-link with `cas cloud link`, then `cas cloud queue --retry-reason project_mismatch`
  ⚠ cross-project rows       10 foreign task row(s) from 6 other project(s) (/home/pippenz/Richards LLC/Accounting,
                             gabber-studio, ozer, pantheon, petra-stella-cloud, pulse-card), 5 of them not closed; 686
                             replicated row(s) whose home project cannot be established (113 not closed). foreign
                             evidence: 10 task row(s); purge delete set: 4 task row(s) — purge cannot reach 6 evidence
                             row(s): cas-fa1d (id collision across peer rows; purge fails closed), cas-1469 (id
                             collision across peer rows; purge fails closed), cas-ac73 (id collision across peer rows;
                             purge fails closed), cas-0c2e (id collision across peer rows; purge fails closed), cas-8003
                             (id collision across peer rows; purge fails closed), cas-90b1 (id collision across peer
                             rows; purge fails closed). purge excludes 686 unattributed task row(s) and 236 id
                             collision(s) — deleting either would destroy real work. unattributed: 686 row(s) (113
                             open), 115 quarantined locally — quarantined rows are hidden from the board and never
                             pushed, and the row itself is untouched (`cas doctor --fix-cloud-rows --yes` to quarantine
                             the open ones, `--release-cloud-rows --yes` to reverse). id collisions: 236 — two rows
                             sharing an id whose titles differ, so a peer row is not evidence about the local row; these
                             are neither deletable nor quarantinable and need an id rekey (GH #133)
  → Do not treat purge-foreign as complete remediation: inspect the retained evidence rows and their stated reasons before taking action.
Config        ✓ configuration  ✓ issue repositories  ✓ MCP stdio upstreams  ✓ sync target  ✓ mcp config
  ⚠ user skills         1 stale user-level skill file(s) no `cas update` will ever refresh:
                        /home/pippenz/.claude-daniel@petrastella.io/skills/cas/SKILL.md (managed_by: cas but no longer a
                        builtin)
  → Review then delete the directory
Integrations  ✓ integrations  ✓ mecha-cassy

25 ok · 30 warnings · 0 errors · 1.4s
