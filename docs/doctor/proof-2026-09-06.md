# Doctor self-heal proof — 2026-09-06

The release binary built from this worktree (`target-cas493c/debug/cas`, build
exit 0) was exercised against eight registered projects. Each project received
a SQLite `.backup` before `doctor --fix --yes`; the backup files and raw JSON/log
receipts are under `/home/pippenz/.cas/artifacts/cas-493c/`.

The first runner attempt exposed the inherited factory `CAS_ROOT` override and
was stopped after its output identified that every invocation targeted cas-src.
The corrected run used `env -u CAS_ROOT` for every command. The cas-src backup
and first before JSON predate that initial invocation; the other seven project
backups and before JSON files are from the corrected run. No explicit
`purge-foreign` apply was run. Doctor's consent path always ran its dry run and
refused to apply or sync when the dry run reported queued local changes.

| Project | Before warnings | Before info | New quarantines | After warnings | After info | Fixable residuals |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| cas-src | 30 | 0 | 0 | 30 | 0 | 0 |
| gabber-studio | 31 | 0 | 212 | 31 | 0 | 0 |
| ozer | 34 | 0 | 214 | 34 | 0 | 0 |
| abundant-mines | 30 | 1 | 0 | 30 | 1 | 0 |
| pulse-card | 30 | 0 | 171 | 30 | 0 | 0 |
| Penguinz | 30 | 0 | 211 | 30 | 0 | 0 |
| Woodworking | 30 | 0 | 0 | 30 | 0 | 0 |
| rocketship-template | 35 | 0 | 215 | 35 | 0 | 0 |

“Fixable residuals” excludes warnings that require human credentials or
decisions: GitHub rate limits/authentication, missing issue-repository config,
legacy index migration, symbol indexing, stale registrations, foreign rows
whose ownership is unknown, and purge safety refusals. Every open unattributed
row was either newly quarantined or already present in the quarantine ledger;
the logs show `quarantined N row(s)` for each project. The two cloud-scope
purge previews that refused cite queued local changes and produced no apply or
sync step.

Receipts:

- summary: `/home/pippenz/.cas/artifacts/cas-493c/proof-8-projects-correct.tsv`
- per-project backups: `proof2-<project>.cas.db.before`
- per-project before/after JSON: `proof2-<project>.before.json` and
  `proof2-<project>.after.json`
- per-project apply output: `proof2-<project>.fix.log`
