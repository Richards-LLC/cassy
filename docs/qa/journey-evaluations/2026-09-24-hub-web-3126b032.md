# Journey evaluation — hub-web/dist 3126b032

**Verdict: PASS. Blocking findings: none. High findings: none.** All 11
journeys reached their goal, and no dimension scored 3. The only 2s are on
HUB-J8, which is not a core journey, so no finding routes to High. The
previous report's High (F1, a refused message pointing at a control that was
not on screen) is fixed in this bundle.

## Receipt

- hub_web_dist: 3126b0323bfd1c2874b9a78f57a1a2cc86e57d34
- evaluated_commit: dc41886787ea47ccb0a769b6f914af6b6c966441
- suite_run: /home/pippenz/.cas/artifacts/cas-823e/journeys — 11 journeys, 11 PASS
- evaluator: tender-falcon-92 with three independent Opus reviewer subagents (J1–J4, J5–J7 + J10, J8 + J9 + J11); not an implementer of the release epic, the journey suite or the UI under test
- label: real-bundle, protocol-double
- blocking_findings: 0

## Scores

Scores are 0–3 for each dimension: 0 none, 1 minor, 2 noticeable, 3 blocking.
The rubric and severity routing are in `docs/qa/journey-evaluation.md`. The
Run column is copied from the suite. PASS means the journey reached its goal.

| ID | Run | Dead end | Copy | Steps | Context | Waits | Severity | Findings / tasks |
|---|---|---|---|---|---|---|---|---|
| HUB-J1 | PASS | 0 | 1 | 0 | 0 | 0 | Normal | F13, N1, N8 — task: TBD |
| HUB-J2 | PASS | 0 | 1 | 0 | 0 | 0 | Normal | F3, F13, N2, N3 — task: TBD |
| HUB-J3 | PASS | 0 | 1 | 0 | 0 | 0 | Normal | F3, F15, F16 — task: TBD |
| HUB-J4 | PASS | 0 | 1 | 0 | 1 | 0 | Normal | F6, F7 — task: TBD |
| HUB-J5 | PASS | 1 | 1 | 0 | 0 | 0 | Normal | F4, F5, F13, N4, N5 — task: TBD |
| HUB-J6 | PASS | 0 | 0 | 0 | 0 | 0 | Note | N4, N6 |
| HUB-J7 | PASS | 0 | 1 | 0 | 0 | 0 | Normal | F12 — task: TBD |
| HUB-J8 | PASS | 0 | 2 | 0 | 1 | 0 | Normal | F1, F2, F10, F16, F17, F18, F19, N7 — task: TBD |
| HUB-J9 | PASS | 0 | 1 | 1 | 1 | 0 | Normal | F8, F14 — task: TBD |
| HUB-J10 | PASS | 0 | 1 | 0 | 0 | 0 | Normal | F11 — task: TBD |
| HUB-J11 | PASS | 0 | 1 | 0 | 0 | 0 | Normal | F1, F9, F16, F19 — task: TBD |

Severity counts: Blocking 0 · High 0 · Normal 10 (HUB-J1, J2, J3, J4, J5,
J7, J8, J9, J10, J11) · Note 1 (HUB-J6).

`frame_defects` and `page_errors` are empty in all 11 `result.json` files.
Every stage screenshot was read, screencasts were sampled at 1 fps (denser at
transitions), and every `final.aria.yml` was read. Visual defects are scored
under Copy, as the previous report did. Every finding that names a cause was
checked against `hub-web/src` at the evaluated commit.

## Findings

All findings are Normal (P2) unless marked Note. Most severe first.

### F1 — Normal — The machine drawer and Fleet overview still lead with the codename (HUB-J8 stage 12; HUB-J11 stage 6)

- **Receipt:** HUB-J8 `J12.png`. Drawer rows read "keen-lynx-1" over "keen-lynx-1 · 1 worker · live": the codename twice, and no project. The Fleet overview ledger title is "keen-lynx-1" with "orion · supervisor keen-lynx-1 · …" beneath it. HUB-J11 `receipt.webm` frame 24 (the drawer during the outage) shows "patient-pelican-9" over "patient-pelican-9 · 1 worker · Reconnecting".
- **Cause:** `hub-web/src/main.ts:3034` (`sessionButton`) renders `session.name` then `session.supervisor`, with no project. The Fleet overview has the same shape at `hub-web/src/fleet-board.ts:66`.
- **User experience:** the list, the Terminal view title, the picker and the palette now lead with the project (the previous F2 is fixed there). The drawer and Fleet overview are the two surfaces left where the user has to match "keen-lynx-1" to a project from memory. Stage 12 asks that these surfaces agree with each other.
- **Fix:** reuse the picker's project-first headline and meta helpers in `sessionButton` and the Fleet overview rows, and drop the repeated "supervisor &lt;codename&gt;".
- task: TBD

### F2 — Normal — The Fleet overview reads like a debug page (HUB-J8 stage 12)

- **Receipt:** HUB-J8 `J12.png` and `final.aria.yml`.
  - The columns are headed "1 2 3 4 5" and need a legend to decode. The "Working" column is shaded even when nothing is working (`hub-web/src/styles.css:478`).
  - A run-on provenance paragraph reads "Alpha · Linux · Live · Hub journey-double · catalog 19:17 / Atlas · …" (`hub-web/src/fleet-board.ts:173`).
  - The red "Remove selected machine" button does not say which machine (`hub-web/src/main.ts:2648`).
  - An unlabelled "‹" sits next to "Conversations" but goes somewhere else: its accessible name is "Back to patient-pelican-9".
- **User experience:** the page that should summarise the fleet at a glance makes the user decode numbers, provenance and an unnamed destructive action.
- **Fix:** put the state words in the column headers at desktop width, and shade a column only when it holds a session. Replace the provenance line with "Last updated 19:17", with details on hover. Label the button "Remove Alpha · Linux from this browser". Give the "‹" a visible label.
- task: TBD

### F3 — Normal — Opening a conversation flashes transport jargon, and the machine's status disagrees with itself (HUB-J2 stage 5; HUB-J3 stages 2 and 8)

- **Receipt:** HUB-J2 `receipt.webm` @00:13.5. On one screen at the same time:
  - the toast says "Atlas · Linux connected"
  - the header and the row say "Connecting"
  - the footer says "Reconnecting" with a grey dot
  - a card reads "Connecting to patient-pelican-9… / ATTEMPT 1 / checking session authorization", and the toast overlaps its right edge

  HUB-J3 `receipt.webm` @00:05 shows "ATTEMPT 1 dialing the relay", with the footer dropping to "3 paired machines · 2 connected" and an amber dot.
- **Cause:** the stage copy is `hub-web/src/connection-state-view.ts:40-47` ("resolving the target node", "dialing the relay", "checking session authorization", "waiting for relay handshake", "waiting for the terminal heartbeat").
- **User experience:** against the double this lasts one or two frames (40–80 ms). Against a real relay the card stays up for the whole attach. Right after being told the machine is connected, the user reads Reconnecting, a lower connected count and relay vocabulary.
- **Fix:**
  - Show nothing for the first ~400 ms of an attach.
  - After that, say "Opening the conversation…", with attempt and stage detail behind "Details".
  - Don't count a machine as disconnected in the footer while one of its sessions attaches.
- task: TBD

### F4 — Normal — A message confirmed after the supervisor has spoken never shows "Delivered" (HUB-J5 stage 9)

- **Receipt:** HUB-J5 `J09.png` and `receipt.webm` @00:22.5–00:31. "Run the gate once more." reads "Sending…" for about 3.4 s, then the label simply disappears when the receipt lands. No tick and no "Delivered" is ever shown. Earlier, "Ship it after the gate passes." loses its "✓ Delivered" as soon as "Gate run 2 of 3 is going." arrives, although that turn does not answer it.
- **Cause:** `hub-web/src/conversation-history.ts:278-286`. `delivered()` returns nothing once any supervisor turn follows the send, and `hub-web/src/conversation-view.ts:511` draws "Delivered" only for that send.
- **User experience:** the stage's promise ("from Sending… to delivered") is met in state but not on screen. The status vanishes at the moment of confirmation, which is the ambiguous delivery state the brief asks evaluators to look for.
- **Fix:** keep a quiet "✓ Delivered" on any acknowledged send that no reply answers (by `reply_to`), rather than hiding it when an unrelated turn crosses it.
- task: TBD

### F5 — Normal — A refused Take control is explained twice, in engineering words, and Take control stays pressable (HUB-J5 stage 6)

- **Receipt:** HUB-J5 `J06.png`. The message reads "This device isn't the one in control of the session. Studio iPad is in control. Take control when it's released, then retry." Directly below it, the composer repeats this in two amber lines: "Studio iPad controls this session, and the hub only accepts a message from its controller. Wait for control to be released, then take control and retry."
- **Cause:** the composer string is `hub-web/src/main.ts:2267`, and the same wording is in `hub-web/src/supervisor-message.ts:118`.
- **User experience:**
  - Stage 5 says the reason is given once and the composer only points at it. Here it is given twice, in different words.
  - "the hub only accepts a message from its controller" is engineering language.
  - Take control stays enabled although it will be refused again while the iPad holds control.
  - Nothing tells the user when control is released, so the only path is to wait and retry blind. This is scored Dead end 1.
- **Fix:**
  - Have the composer say "Not sent — see the message above.", as it does in `J05.png`.
  - While another device holds control, show "Waiting for Studio iPad" in place of an active Take control, and re-enable it when the lease is released.
- task: TBD

### F6 — Normal — A report that fails to open still opens a tab, and the reason lands far from the file (HUB-J4 stage 4)

- **Receipt:** HUB-J4 `trace.zip`. For each of the three failure taps, a popup page opens and closes 10–15 ms later. HUB-J4 `J04.png`: the error toast sits at the top of the thread, over "Today" and the user's "Is the release ready to cut?", about 400 px above the tapped "Bench results.pdf". It says "Couldn't reach Atlas · Linux. Check that it's on and connected, then tap the file again." next to a footer that says "Connected" and a header that says "Live".
- **Cause:** `hub-web/src/artifact-open.ts:98` opens the tab before fetching the signed view, and `:111-112` close it and notify on failure. The catalog says a file that cannot open "says so instead of opening a blank tab".
- **User experience:** with a real machine that is slow to answer, an "Opening file…" tab stays up for the whole request while the explanation arrives in the other tab. That real-machine behaviour is inferred from the trace and the code; the screencast records only the main page. The message also contradicts the connection state on screen, and "tap" reads oddly on a desktop.
- **Fix:**
  - Show a file's known state on its card ("Only on Atlas") before the tap, and don't open a tab for a file that was never uploaded.
  - Show a failure on the file card itself.
  - When the socket is live but the file request fails, say that the machine is connected but didn't send the file.
- task: TBD

### F7 — Normal — "Load earlier" does not keep the reading position (HUB-J4 stage 2)

- **Receipt:** HUB-J4 `receipt.webm` frames 10→11. "Today" moves from y=211 to y=406, and "Is the release ready to cut?" moves from y=262 to y=457. The view stays pinned to the top of the newly prepended page.
- **Cause:** `hub-web/src/conversation-view.ts:260` swaps in the older turns without scroll compensation.
- **User experience:** the catalog expects "Loading earlier keeps the reading position". With this short history the move is mild. With real multi-page history the user loses their place on every load.
- **Fix:** record `scrollHeight` before prepending, then add the difference to `scrollTop`.
- task: TBD

### F8 — Normal — Pairing from a phone lands back on the list, not in the new conversation (HUB-J9 stage 9)

- **Receipt:** HUB-J9 `trace-actions.txt` action 174: the test taps "forge-tools" in the list to open it. `receipt.webm` frame 27 shows the list, with the "Forge · Linux connected" toast lying over the brand row and the appearance button.
- **Cause:** after pairing, `hub-web/src/main.ts:1053` only calls `commitSelection({ machineId })`, which selects the machine but not a session.
- **User experience:** the catalog says "pairing from the phone opens its conversation". Instead, the user has to find and tap the new row. The toast then sits correctly below the thread header, so the previous F8 is fixed where the catalog checks it.
- **Fix:** on a phone, open the new machine's only (or first) live session after pairing, and anchor the toast below whichever header is showing.
- task: TBD

### F9 — Normal — Outage wording differs from the banner, and disabled controls hide their reasons (HUB-J11 stages 2 and 6)

- **Receipt:**
  - HUB-J11 `J02.png`. The banner says "Lost connection to Atlas · Linux. Reconnecting…", while the refused send says "The hub connection is reconnecting, so this message was not delivered. Try again once the session is live." (`hub-web/src/main.ts:2329`). Send stays fully active throughout.
  - `receipt.webm` frame 23: "Release control" and "Interrupt" are greyed out, but their reasons exist only as tooltip and aria text.
  - Frame 24: the drawer status is clipped to "Reconnect…".
- **User experience:** the same outage is described in two vocabularies. A touch user sees disabled controls with no visible reason.
- **Fix:**
  - Word the refusal like the banner: "Not sent: lost connection to Atlas · Linux. Your message is kept; send it again when it's back."
  - Show the disabled reason as visible text, and let the drawer status wrap.
- task: TBD

### F10 — Normal — "Not confirmed" and Retry stay after the supervisor has replied (HUB-J8 stages 7–8)

- **Receipt:** HUB-J8 `J07.png` and `J08.png`. "⚠ Not confirmed · The hub never confirmed this reached calm-otter-4. Retry sends it again." stays above the supervisor's later "Tests are running on the Mac."
- **Cause:** the copy is in `hub-web/src/conversation-view.ts:525`.
- **User experience:** the previous F5 (endless "Sending…") is fixed. Now, though, a message the supervisor has visibly moved past still invites Retry, and a duplicate send. "hub" is an internal word.
- **Fix:** once a later supervisor turn exists, soften the copy ("The supervisor has replied since; resend only if it missed this"). Say "Cassy couldn't confirm delivery" rather than "The hub never confirmed".
- task: TBD

### F11 — Normal — With forced colours on, a status line gets a thick bar under it (HUB-J10 stage 5)

- **Receipt:** HUB-J10 `receipt.webm` @00:11–00:14 (frame 24 dark, frame 29 light). "Status: the Linux lane is green; the Mac lane is still running." sits on a heavy crescent bar about 10 px thick. `J05.png` does not render forced colours, so only the video shows it.
- **Cause:** `hub-web/src/styles.css:3244`. `.thread .coalesce` uses `border-bottom: 10px solid transparent` as a line-clamp spacer, and forced colours repaints that transparent border in the text colour.
- **Fix:** use `padding-bottom` instead, or set `border-bottom-color: Canvas` under `@media (forced-colors: active)`. Add a forced-colours assertion for it.
- task: TBD

### F12 — Normal — A waiting blocker doesn't say what to do, and an answered question still looks urgent (HUB-J7 stages 3–6)

- **Receipt:** HUB-J7 `J04.png`, `J06.png` and `final.aria.yml`. The red blocker "A second gate went red; the train is still held." carries no action and no hint, and the rail's "Waiting on you · Blocker" only jumps to it. The answered question (`J03.png`, `J04.png`) keeps its full amber attention styling with "✓ Fix in-train", so two items look urgent after one was handled.
- **Cause:** any later send clears the blocker (`hub-web/src/conversation-history.ts:189`), but the screen never says so.
- **Fix:** add "Reply to unblock" under a waiting blocker and in its rail entry. Quiet an answered question to the normal supervisor colour, keeping the tick on the chosen answer.
- task: TBD

### F13 — Normal — The generated supervisor name is the address, and it breaks at hyphens (HUB-J1 and J2 stage 5; HUB-J5 stages 1 and 11; HUB-J6 stage 1)

- **Receipt:**
  - HUB-J1 and HUB-J2 `J05.png`. Right after pairing, the composer reads "Message patient-pelican-9" and "Send to patient-pelican-9". The empty state reads "Nothing waiting on you. patient-/pelican-9 will write here…", with the name in bold monospace, split at the hyphen.
  - HUB-J5 `J01.png` and HUB-J6 `J01.png` show the same split.
  - HUB-J5 `J11.png` (long name): the placeholder and Send label now ellipsise, but the empty-state sentence still breaks the codename across three lines, and "checks-77" wraps onto a second meta line.
- **User experience:** the list and header rightly lead with "cas-src". But the first screen after pairing asks a new user to write to an unexplained generated name, which the catalog calls tertiary, and the text breaks it mid-word. The previous F9 is only partly fixed.
- **Fix:** say "Message the cas-src supervisor" and "Send". In the empty state, say "The supervisor (patient-pelican-9) will write here…", with the name kept on one line (`white-space: nowrap`) and ellipsised beyond a cap.
- task: TBD

### F14 — Normal — The phone header cuts the one name the composer uses (HUB-J9 stages 2–9)

- **Receipt:** HUB-J9 `J05.png`: the header reads "Studio Mac · macOS · c… · Live" while the composer says "Message calm-otter-4". Also "patien…" in `J02.png` and "steady…" in `J09.png`.
- **Fix:** below 500 px, drop the OS word ("macOS", "Linux") before truncating the codename.
- task: TBD

### F15 — Normal — An empty right-hand column is left after going back to the list (HUB-J3 stage 6)

- **Receipt:** HUB-J3 `J06.png` and `receipt.webm` frame 42. After "‹ Conversations" at 390 px and a return to desktop width, the no-selection screen keeps an empty 240 px column with a divider at x≈1040, and "Stay close to the work." is shifted left (x=499, against 595 in `J01.png`).
- **Cause:** `hub-web/src/main.ts:137-142` passes the module-level `contextProgress` from the last open thread even when no session is selected, so the rail keeps `context-open`.
- **Fix:** reset `contextProgress` and `contextAttention` when no session is selected.
- task: TBD

### F16 — Normal — Palette and Terminal view jargon, and two spellings of one shortcut (HUB-J3 stages 7–10; HUB-J8 stage 4; HUB-J11 stage 6)

- **Receipt:**
  - HUB-J3 `J10.png` shows "THIS SESSION · Release control · patient-pelican-9". The placeholder says "Type a command or session", and a filter with no match shows "No commands or sessions match "zzzz"."
  - HUB-J8 `J04.png`: in the same Linux browser, the list says "Search conversations (Ctrl K)" and the Terminal view button says "⌘K" (hard-coded at `hub-web/src/main.ts:2658`).
- **User experience:** the palette now groups well (the previous F4 is fixed). "Session" and "Release control" remain the terms a non-engineer has to decode, and a Linux user is shown a Mac key.
- **Fix:** say "conversation", not "session", in the palette. Give the control action a plain name with a one-line hint. Choose the shortcut label by platform.
- task: TBD

### F17 — Normal — "● Status unavailable" with a green dot after a reload (HUB-J8 stage 11)

- **Receipt:** HUB-J8 `receipt.webm` frames 62–64, for about 3 s.
- **Cause:** `hub-web/src/main.ts:2514` shows "Status unavailable" until the first latency sample arrives.
- **Fix:** show "Checking…", or nothing, until the first sample.
- task: TBD

### F18 — Normal — The session picker goes blank when its filter matches nothing (HUB-J8 stage 4)

- **Receipt:** HUB-J8 `receipt.webm` frame 16. The filter "zz" leaves an empty dialog with two × controls stacked.
- **Cause:** the picker (`hub-web/src/main.ts:3581-3592`) has no no-match line; the palette has one (`hub-web/src/main.ts:2708`).
- **Fix:** add "No sessions match "zz"".
- task: TBD

### F19 — Normal — A screen reader hears "Switch session — 2 available", not the open conversation (HUB-J8, HUB-J11)

- **Receipt:** HUB-J11 `final.aria.yml` has `heading "Switch session — 2 available"`. The visible "cas-src patient-pelican-9" is not announced.
- **Cause:** the `aria-label` on the title button overrides its visible text (`hub-web/src/main.ts:2655`).
- **Fix:** label it "cas-src on Atlas · Linux — switch session (2 available)".
- task: TBD

### N1 — Note — Product name and build hash (all screens)

- **Receipt:** HUB-J1 `J01.png`: "Cassy Cloud" branding, and "Hub d09cacfb" in every footer. Carried from earlier reports; a brand decision.
- report only

### N2 — Note — A second pair link silently replaces the first machine (HUB-J2 stage 1)

- **Receipt:** HUB-J2 `receipt.webm` @00:01.5–00:02.5. Studio Mac / studio.test swaps to Atlas · Linux / atlas.test, and the granted abilities grow, with the same status line. Carried from the previous report.
- report only

### N3 — Note — The pair-link dialog with its disclosures open (HUB-J2 stages 2–3)

- **Receipt:** HUB-J2 `J02.png`: "Name for this browser" drops below the dialog's bottom edge. `J03.png`: the title and hub-address label scroll away, and the scopes show as raw `machine:read` / `pane:interrupt` tokens with native checkboxes. Carried from the previous report; opt-in states only.
- report only

### N4 — Note — The session-start line reads like a log line (HUB-J3, J5, J6, J9, J10, J11)

- **Receipt:** HUB-J5 `J02.png`, HUB-J6 `J03.png`: "session patient-pelican-9 started 19:16" in a monospace pill. Carried from the previous report.
- report only

### N5 — Note — Retry moves the message and leaves no trace of the first try (HUB-J5 stage 10)

- **Receipt:** HUB-J5 `receipt.webm` @00:35.5 onwards. The "Not confirmed" card sits between two supervisor turns. After Retry it disappears and reappears at the bottom as "Sending…" and then "✓ Delivered". This is truthful, because the retry is a new send, but it may surprise a user.
- report only

### N6 — Note — No focus ring on the composer after dictation (HUB-J6 stage 2)

- **Receipt:** HUB-J6 `J02.png` against `J01.png`. If focus stays on the mic, reviewing the transcript costs a click. Not traced in code. The listening state itself is now visible: the placeholder says "Listening — speak, then review", the mic fills red, and its name toggles to "Stop listening".
- report only

### N7 — Note — Small leftovers in Terminal view and on the phone (HUB-J8, HUB-J9, HUB-J11)

- **Receipt:**
  - HUB-J8 `receipt.webm` frame 59: the footer says "0 conversations" next to "Loading…".
  - The picker's "Open" badge reads like a button.
  - List rows don't mark a thread with an unsent draft (HUB-J8 `J03.png`).
  - An empty 48 px right-rail strip shows (HUB-J8 `J10.png`, x≈1232).
  - "Cassy Cloud" appears twice in the drawer (HUB-J8 `J12.png`).
  - The working dots stay visible after "Back. Nothing was lost." (HUB-J11 `J05.png`).
  - The thread-wide focus ring after a palette Enter is heavy, though intended (HUB-J9 `J07.png`).
- report only

### N8 — Note — Pairing dialog wording (HUB-J1 stages 2–4)

- **Receipt:** HUB-J1 `J02.png`–`J04.png`.
  - "Name for this browser" defaults to the generic "Cassy Cloud browser".
  - The heading "Machine authorized" shows while the user still has to press Pair.
  - The command "cas hub authorize KQ7M-4XTR" is small grey monospace under a large code, and "Copy command" looks like plain text.
- report only

## Since the previous report

The previous report (`2026-09-24-hub-web-393ff850.md`) had one High and nine
Normal findings.

- **F1 (High), the refused message pointing at a control that was not on screen:** fixed. HUB-J5 `J05.png`: the refused message carries Take control, Edit and Retry, and says "Take control, then retry." After the take (`receipt.webm` @00:17), it reads "This device controls the session now. Retry to send it." and Take control is gone. The refused-take case is new F5.
- **F2 (codename first in Terminal view, picker and palette):** fixed there. The Terminal view title reads "cas-src patient-pelican-9 ▾", picker rows and palette "Jump to" rows lead with the project (HUB-J8 `J09.png`, `J11.png`; HUB-J3 `J10.png`). It remains in the drawer and Fleet overview (F1).
- **F3 ("A·" rail badge):** fixed. The rail reads "AT", "SM" and "AL" (HUB-J8 `J11.png`).
- **F4 (palette groups):** fixed. The groups are Conversations, This session, Machines, Appearance and a collapsed Advanced, and "Dismiss all info" is hidden when nothing is outstanding. The wording is F16.
- **F5 (endless "Sending…"):** fixed. After 5 s without a receipt, the message shows "Not confirmed" with Retry, and the retry is delivered (HUB-J5 `J10.png`). A 3.4 s late receipt never flashes "Not confirmed" (HUB-J5 stage 9; the suite's frame watcher counted zero). New related findings: F4 and F10.
- **F6 (thread order):** fixed. There is no future day header ("19:16 · machine clock ahead", HUB-J7 `J05.png`). The order after reload matches the visit (HUB-J7 `J06.png` against `J04.png`), and the session line stays above "Are we back?" on desktop and phone (HUB-J11 `J04.png`, `J05.png`).
- **F7 (transport alarm in the rail during an outage):** fixed. No transport card appears (HUB-J11 `J02.png`). Terminal view says "Not all clear. Atlas · Linux is reconnecting." The remaining wording is F9.
- **F8 (toast over the rail heading):** fixed. The toast sits below the thread header (HUB-J1 and HUB-J2 `J05.png`, HUB-J9 `J09.png`).
- **F9 (long supervisor name):** partly fixed; see F13.
- **F10 (phone copy):** fixed. The phone search has no "(Ctrl K)" (HUB-J9 `J01.png`), and times use the 24-hour clock. Paired machines says "Can't reach · retrying" and "Version unknown until it connects" (HUB-J9 `J08.png`).
- **N1–N4:** still present, re-recorded above as N1–N4.

## Stage timings

The slowest stages, from each journey's `result.json`. Timings include about
0.3 s per annotated action.

| Journey | Stage | ms | Note |
|---|---|---|---|
| HUB-J8 | Keep my place in the session picker while updates arrive | 10324 | Includes test holds while hub updates re-render. |
| HUB-J8 | Reopen the session picker after closing it | 9094 | Several open, close and filter cycles. |
| HUB-J3 | Reopen the palette to the full list | 8549 | Reloads and palette cycles. |
| HUB-J11 | On a phone, the banner stays readable through an outage | 7562 | Includes two 1.7 s holds in light and dark. |
| HUB-J5 | A message the hub never confirms offers Retry | 7127 | A deliberate 5.4 s hold; "Sending…" and "Sending to patient-pelican-9…" stay visible throughout. |
| HUB-J8 | Keyboard focus lands somewhere real on every route | 6214 | Many route checks. |
| HUB-J8 | See which session is open while pointing at it | 5934 | Hover checks in two schemes. |
| HUB-J3 | Jump to a supervisor from the keyboard | 5679 | Repeated Ctrl+K cycles. |
| HUB-J5 | A late receipt after the supervisor talks on never offers Retry | 4830 | A deliberate 3.4 s receipt delay. |
| HUB-J4 | Open a report the supervisor sent | 4109 | Four file taps, three of them failure cases. |

Opening a conversation takes 1.00–1.15 s (stage 1 of HUB-J4, J5, J6, J7,
J10 and J11), about the same as last time. HUB-J8's first stage (1.96 s)
includes typing the draft. No stage showed a wait without visible progress.
The only progress states too brief to judge were "Loading earlier…" (HUB-J4)
and the attach card (F3), both under one frame against the double.

## Not covered

From each journey's **Gaps:** in `docs/qa/journeys.md`, and from what these receipts cannot show. No real-build proof for this dist tree was found for any gap below.

- **HUB-J1:** the relay and the machine-side `cas hub authorize` are doubled, and `docs/design/hub-web/pairing-verification.md` predates this dist tree. Not walked: relay down, expired code, the Tailscale/DNS guidance, 401/403 and reload-resume. Whether "Copy command" works is not observable.
- **HUB-J2:** the exchange endpoint is doubled. Not walked: malformed or truncated links, and a link without `scopes`. "Ungranted scopes are visibly unavailable" is not observable, because the read-only link was replaced before its details opened. The secret leaving the address bar is asserted by the suite, not captured.
- **HUB-J3:** no declared gaps. Not walked: "No live supervisors listed" with the dormant route, "Unreachable · message pending", and many-row sorting.
- **HUB-J4:** history pages come from the double, and `hub-web/scripts/conversation-history-qa.mjs` was not run for this dist tree. One page reaches "No earlier history", so multi-page position holding is not exercised. The successful report tab is not recorded.
- **HUB-J5:** daemon delivery and operator stamping are doubled. Enter versus Shift+Enter was not exercised (every send clicks Send). Not walked: sending while reconnecting from this journey, no `message:send`, and nobody holding control.
- **HUB-J6:** speech recognition is stubbed. Not walked: mic denied, no speech heard, and "Voice input unavailable". A transcript landing mid-draft was not tested.
- **HUB-J7:** no declared gaps. Not walked: a refused answer, a free-text answer, and a question with no options.
- **HUB-J8:** no declared gaps. "Reload restores the last machine and conversation" is seen only as the loading skeleton. Removing a machine was not walked.
- **HUB-J9:** a real on-screen keyboard resize is not emulated, so "the composer stays visible above the keyboard" is not observable.
- **HUB-J10:** no declared gaps. The live "System" follow was not walked. `J05.png` does not render forced colours, so that stage was judged from the video and the assertions.
- **HUB-J11:** a real network loss is simulated by closing the socket, so heartbeat misses, browser-offline events and the 30 s backoff are not exercised. Revoked pairing (401/403) was not walked. Disabled-control reasons are tooltip and aria text only.

Catalog drift, which `journeys-for-diff.py --check` does not catch: the suite
has stages that the catalog's Steps omit. These are HUB-J3 "A live update
while a palette row is focused" and "Reopen the palette to the full list";
HUB-J5 "A long supervisor name leaves the message box usable"; and HUB-J9
"Jump from the palette with a tap". HUB-J3's catalog also puts the codename
"on the line beneath" each "Jump to" row, but the bundle right-aligns it on
the same line.

## Routing (supervisor)

No finding is Blocking or High, so nothing blocks the 3.31.0 cut. F1–F19
are Normal, so they route to P2 tasks; findings that share a fix may share a
task. N1–N8 are report only.

| Finding | Severity | Suggested priority | Suggested grouping | Task |
|---|---|---|---|---|
| F1, F2 | Normal | P2 | Fleet overview and drawer copy | TBD |
| F3 | Normal | P2 | Attach state copy and timing | TBD |
| F4, F10 | Normal | P2 | Delivery status after supervisor turns | TBD |
| F5, F9 | Normal | P2 | Refusal and outage wording | TBD |
| F6 | Normal | P2 | Report open failure | TBD |
| F7 | Normal | P2 | Load earlier scroll anchoring | TBD |
| F8, F14 | Normal | P2 | Phone pairing and header | TBD |
| F11 | Normal | P2 | Forced-colours coalesce border | TBD |
| F12 | Normal | P2 | Blocker and answered-question states | TBD |
| F13 | Normal | P2 | Supervisor name in composer and empty state | TBD |
| F15, F17, F18, F19 | Normal | P2 | Shell leftovers (rail, status, picker, aria) | TBD |
| F16 | Normal | P2 | Palette vocabulary and shortcut label | TBD |
