# Slack draft — v2.63.0 release, 2026-08-11

Channel: #cas-internal (C0B44GUKDK2)

## User thread

**Top-level:**
Live on production — **User** — CAS v2.63.0 is out: agents you message now actually wake up and answer, every Codex agent starts instantly without trust prompts, and sessions run leaner with two-thirds less boilerplate.

**Reply (Was → Now):**
- Was: on v2.62.0, a message to an idle Codex agent could sit unread forever, fresh Codex installs stalled on an interactive trust prompt, and stuck-agent cleanup left ghost terminals behind. Now: delivery is watched end-to-end and wakes the agent, installs come pre-trusted and verified before launch, and cleanup really tears down.
- Was: team-launched sessions started blind to project memory, and every session paid ~30KB of skill boilerplate. Now: all sessions get their memory bundle and the boilerplate is down 66%.
- Was: a cloud build-cache outage could fail your test run. Now: the build falls back uncached with a loud note and your change still validates.
- Install: grab v2.63.0 from the GitHub releases page — `cas update` on an existing install, or the tarball for your platform.

## Dev thread

**Top-level:**
Live on production — **Dev** — v2.63.0 (80691e8): PTY submit classification + delivered-unsurfaced watchdog, flock+fsync+read-back trust transactions gating spawn, canonical hooks.json serialization with zero-diff regen, sccache fail-open on all five setup sites, and the −65.6% skill-description cut.

**Reply (Was → Now):**
- Was: v2.62.0 daemons deferred NORMAL prompt injection on a composer-dirty gate 120× then dropped it; heartbeat reaps abandoned live panes; concurrent Codex spawns raced the config.toml pre-trust write. Now: submit classification observes the turn, stale maintenance forces shutdown with direct-child wait, and spawn refuses until a locked read-back proves trust (PRs #241/#245/#247/#248).
- Was: hooks.json regen churned key order; generated commands drifted from the committed harness-aware contract. Now: one canonical serializer, byte-identical regen, trusted_hash registration verified byte-exact against Codex 0.147.
- Was: sccache-action failures (503s/cert) hard-failed jobs. Now: probe + fail-open with valid-zero-stats post hook, pinned by the tier contract.
- Archives: linux-x86_64 sha256 4fd5bac2f546cef33c84e52f8a61a24c6fecf707ee02069ca564591ac1c33371; binary reports `cas 2.63.0 (80691e8 2026-08-11)`.
