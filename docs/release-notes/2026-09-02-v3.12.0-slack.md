# Slack draft — Cassy v3.12.0 runtime release

Channel: #cas-internal (C0B44GUKDK2)

**Status:** Posted 2026-09-02 to #cas-internal. Both User and Dev threads have
exactly one Was → Now reply; the receipt below records all four messages.

## User thread

**Top-level:**
Live on production — **User** — Every built-in Cassy skill and agent was reviewed against the current runtime and rewritten where it was stale, oversized, or wrong, so agents get accurate, shorter instructions.

**Reply (Was → Now):**
- Was: skills still taught retired review states and flags, listed commands that no longer exist, told the verifier to record a field it never saved, injected more worker guidance than a session could hold, and kept a removed code-review skill in every menu. Now: each skill states when to use it in its first line, every command and field named in a skill exists today, the verifier records what it reviewed, worker sessions start with their ready tasks and memories visible, removed skills disappear from menus, and shipped skills carry no operator-specific paths or accounts. Three overlapping skills and two unused agents were merged or retired.
- Install: use `cas update` for an existing installation, or download the
  v3.12.0 archive for Linux x86_64 (SHA-256 `58784cd9c29a907a57ec8ae65a4ec9eadf84b70adb142c37d082837f62ba2151`) or macOS ARM64
  (SHA-256 `07b0352a0de54d7fc4070d82f4348eb11f2fad51fa150f95e9291efd59eaa1ab`) from the GitHub release.

## Dev thread

**Top-level:**
Live on production — **Dev** — Skills hygiene wave from the Fable 5.1 review: verifier field and rg fixes, pruner inversion, supervisor_override documented, memory skill rewritten against the live API, generated action lists pinned by tests, worker guidance under the SessionStart budget, opt-in stack tier, drift guard on non-markdown twins.

**Reply (Was → Now):**
- Was: task-verifier templates passed `files=` (schema field is `files_reviewed`) and ran `rg -E`; `prune_stale_cas_skill_dirs` only deleted unmanaged dirs so removed managed builtins persisted; worker_guidance() was 9,956 B against a 9,216 B budget; cas-memory-management described a markdown file store and dead commands; cas-task-tracking/cas-search hand-listed actions that diverged from the dispatch table; `builtin_flavor_drift_test` compared only .md twins. Now: templates use `files_reviewed=` and `grep -E` with an executed fixture test; learning-reviewer receives unreviewed IDs; the Stop hook include_str!s the session-learn body; managed stale dirs and orphaned workflow files are pruned (marker/allowlist guarded); close/factory guidance names real procedures; dead guide constants removed; supervisor/worker references teach awaiting_merge and supervisor_override only, one worktree_merge procedure, no raw SQL; cas-memory-management lists every MemoryRequest field with response-shapes.md; mcp_action_surface_test parses service/mod.rs dispatch arms; worker bodies are ~6.0 KB with a budget test; eight descriptions lead with "Use when"; `[release] claude_account_allowlist`-style config replaces the hard-coded account; `[skills] optional` plus stack detection tiers fallow/cas-nuxt-playwright; doc-hygiene.md shared by codemap/project-overview/design-spec; cas-domain-modeling merged into cas-codebase-design; git-history-analyzer and issue-intelligence-analyst retired; drift guard covers .sh/.yaml/.js.
- Validation and artifact: `cargo check --workspace --tests`, `cargo nextest run -p cas`, doctests on the assembled tree; new/updated targets: builtin_doc_hygiene_test 14, builtin_flavor_drift_test 15, agent_definition_contract_test 5, builtin_skill_description_test, mcp_action_surface_test 3, factory_codex_skill_guardrails, --lib builtins 85; all three harness mirrors regenerated and the reference-history ledger rebuilt. The published archives are
  `cas-x86_64-unknown-linux-gnu.tar.gz` (SHA-256 `58784cd9c29a907a57ec8ae65a4ec9eadf84b70adb142c37d082837f62ba2151`) and
  `cas-aarch64-apple-darwin.tar.gz` (SHA-256 `07b0352a0de54d7fc4070d82f4348eb11f2fad51fa150f95e9291efd59eaa1ab`). Linux and
  macOS are both available from CI, regardless of the tagger's host.

## Posting sequence

1. Replace the narrative placeholders after the release PR is merged.
2. Tag the fetched `origin/main` landing and run
   `./scripts/release.sh --publish-tag`. A bare `release.sh` is a local audit
   and does not touch the remote.
3. Run `release-published-receipt.sh --write-draft` against this draft. It
   downloads and hashes both published assets before replacing every digest
   placeholder; never type a digest from a local audit archive.
4. Post the User top-level and its only reply, then the Dev top-level and its
   only reply; append the receipt below.

## POSTED

Channel: `#cas-internal` (`C0B44GUKDK2`).

| Message | UTC timestamp | Permalink |
| --- | --- | --- |
| User top-level | 2026-09-02T15:36:16Z (ts 1788363376.233289) | https://petra-stella.slack.com/archives/C0B44GUKDK2/p1788363376233289 |
| User reply (Was → Now) | 2026-09-02T15:36:25Z (ts 1788363385.362049) | https://petra-stella.slack.com/archives/C0B44GUKDK2/p1788363385362049?thread_ts=1788363376.233289&cid=C0B44GUKDK2 |
| Dev top-level | 2026-09-02T15:36:17Z (ts 1788363377.429409) | https://petra-stella.slack.com/archives/C0B44GUKDK2/p1788363377429409 |
| Dev reply (Was → Now) | 2026-09-02T15:36:34Z (ts 1788363394.493969) | https://petra-stella.slack.com/archives/C0B44GUKDK2/p1788363394493969?thread_ts=1788363377.429409&cid=C0B44GUKDK2 |

Published 2026-09-02T15:35:29Z (tag pushed 15:32:47Z; publish latency 162s, budget 600s; Release run 33649242062; PR #684 landed on main at bb14b7c6 15:16Z).
Linux SHA-256 58784cd9c29a907a57ec8ae65a4ec9eadf84b70adb142c37d082837f62ba2151;
macOS SHA-256 07b0352a0de54d7fc4070d82f4348eb11f2fad51fa150f95e9291efd59eaa1ab.
Host verified: `cas update` → cas 3.12.0 (bb14b7c); hub serving 3.12.0.
Posted via the supervisor session's claude.ai Slack connection (Daniel Villafranca <daniel@petrastella.io>).
