# Slack draft — Cassy v3.22.1 runtime release

Channel: #cas-internal (C0B44GUKDK2)

Status: POSTED (see the POSTED block at the end).

User top-level

```text
*Live on production — User — Cassy v3.22.1*
Was: the new release-report command needed a hand-built report to stand in for it, and an update could end with "outcome unknown". Now: the command produces the real report on its first run and an update tells you exactly which projects still need attention.
```

User reply

```text
*Release reports*

• *First run is the real report* — Was: the command's first run lost project settings from a secondary checkout, cut wrapped sentences, wrote placeholder "Was" lines, missed closed issues, called a live release unpublished, and opened with a generic line. Now: it reads settings from any checkout, keeps sentences whole, tells the Was → Now story from your release notes, counts closed issues, shows the publication time, opens with the release headline, and groups changes the way the notes do.

• *PDF without setup* — Was: the PDF step stopped when the browser module was missing. Now: it fetches a disposable copy and continues.

*Updates*

• *Refresh receipt kept* — Was: after an update, Cassy could say the refresh outcome was unknown even though it had just printed which projects failed. Now: it names the projects that did not refresh and the command to rerun.

*Quality of the checks themselves*

• *Guardrails travel with the skill* — Was: a change to a built-in skill could pass its own checks and still fail a release check minutes later. Now: the hand-off proof includes the guardrail tests that read that skill.

• *No more racing test* — Was: one pairing test could fail a release check by racing its own throwaway server. Now: it waits for readiness and names the address if anything is refused.

• *Guidance parked, not lost* — Was: three pieces of coordinator guidance survived only in a start-up prompt after a size trim. Now: they live in a reference page the playbook points to.

Report: the v3.22.1 release report (PDF and HTML), generated by the shipped command itself this time, is attached below in this thread.

Install: update with `cas update`, or download the Linux x86_64 archive (`560f25f14f6a7028250f99e5d44732adc705850406507d06ed97b334cd5af375`) or macOS ARM64 archive (`5de154ec47ee0069523c15bb79d0876fe11ac2597d4cfafef4d30629c5eda9ef`) from the Cassy v3.22.1 release.
```

Dev top-level

```text
*Live on production — Dev — Cassy v3.22.1*
Was: `cas release report` was unusable on its first real run and `cas update`'s plain post-swap path had no receipt transport. Now: store-detection config, joined changelog bullets, draft-sourced Was/Now with audience partitioning, issue/publication/verdict/theme evidence, a Playwright fallback, and a post-swap receipt file, plus proof-surface and test-fixture hardening.
```

Dev reply

```text
*Release report* (PRs #790, #794)

• *Sources* — Was: `acquire_sources` read `project_root/.cas`; `parse_changelog_section` split bullets per line; `was_now_sections` hard-coded a placeholder Was for both audiences. Now: `find_cas_root_from` + `[project].canonical_id`; continuation lines joined; draft Was/Now articles partitioned User/Dev with changelog fallback; inline-code headings preserved.

• *Evidence* — Was: `closingIssuesReferences` only, publication from release metadata only, top-levels rendered as articles, fixed `THEME_ORDER`. Now: PR body refs extracted and verified; `PUBLISHED_AT` from the receipt or release object; the User punch is the normalized verdict; themes derive from the draft's bold groups and persist in front matter.

• *PDF* — Was: exit on `require('playwright')` failure. Now: disposable `npm install --no-save playwright` and retry.

*Update* (PR #788)

• *Receipt transport* — Was: plain post-swap child with inherited stdio, every exit 1 → `refresh_failed_no_receipt`. Now: hidden `--refresh-receipt <file>` carries `project_refresh_receipt_json` (with `refresh_status`) back to the parent on both paths; version probe kept for older children.

*Proof surface and tests* (PRs #789, #792)

• *Scoped proof* — Was: builtin skill/reference/agent paths were invisible to `--proof`. Now: drift, agent-contract, and guardrail targets required, plus discovery by source and installed catalog spelling; rg errors fail closed; failure-log learn with self-test.

• *Pairing fixture* — Was: client raced the listener's first `accept()`. Now: readiness channel, bounded accept/read, address-bearing diagnostics; 50/50 under load.

*Supervisor skill* (PR #791)

• *Reference* — Was: reporting/release-train/routing text trimmed from the body. Now: `cas-supervisor/references/reporting-and-routing.md` in three flavors with catalog entries and a breadcrumb.

Validation: full release gate green on the first run for candidate 85a283b8; merge queue validated the synthetic tree (PR #797, run 34390385187). Tag-to-published 178 s; green-to-published 2,187 s. The attached report was produced by `scripts/release-train.sh --report` running the shipped `cas release report 3.22.1 --pdf` (one cosmetic warning about PR numbers read as issue refs, filed as cas-e65f).

Install: `cas-x86_64-unknown-linux-gnu.tar.gz` (`560f25f14f6a7028250f99e5d44732adc705850406507d06ed97b334cd5af375`) and `cas-aarch64-apple-darwin.tar.gz` (`5de154ec47ee0069523c15bb79d0876fe11ac2597d4cfafef4d30629c5eda9ef`), published by CI for Linux and macOS.
```

## POSTED

Posted 2026-09-09 19:19Z via the MechaCassy hub (mecha_post) to #cas-internal (C0B44GUKDK2), after `release-published-receipt.sh v3.22.1 --write-draft` replaced both digests (PUBLISHED_AT=2026-09-09T19:11:03Z) and the train's `--report` step generated the report with the shipped `cas release report 3.22.1 --pdf`:

- User top-level ts 1788981565.794209 — https://petra-stella.slack.com/archives/C0B44GUKDK2/p1788981565794209
- User reply ts 1788981586.620389 (thread of the user top-level)
- Dev top-level ts 1788981567.577199 — https://petra-stella.slack.com/archives/C0B44GUKDK2/p1788981567577199
- Dev reply ts 1788981598.204639 (thread of the dev top-level)
- Report files posted 19:20Z in the user thread via the claude.ai Slack MCP: PDF F0C08UJ0WEB https://petra-stella.slack.com/files/U076KKG4DGU/F0C08UJ0WEB/cassy-v3.22.1-release-report.pdf ; HTML F0C0Q8U894J https://petra-stella.slack.com/files/U076KKG4DGU/F0C0Q8U894J/cassy-v3.22.1-release-report.html
- Release-report receipt (train `--report` contract): ~/.cas/artifacts/release/v3.22.1-release-3230-prepare/release-report.receipt, verified by `--status` (PDF 4 pages).
- Host verification: `cas --version` = 3.22.1 (f26251c); hub 3.22.1; `cas update --yes --json` refresh_binary_version=3.22.1 (refresh_status=refresh_failed for the known contaminated projects; the parent-side receipt message applies from the next update). Receipts: release-workflow.json run 34393232957; install-path-proof.json run 34393534425 (Linux + macOS success); release-published.receipt; release-latency.receipt; host-update.json.
