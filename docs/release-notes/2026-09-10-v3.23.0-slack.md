# Slack draft — Cassy v3.23.0 runtime release

Channel: #cas-internal (C0B44GUKDK2)

**Status:** Draft. Do not post until the tagged workflow publishes both
archives, the published receipt replaces both checksum placeholders, and the
release report receipt is complete.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.23.0*
Was: long coordination traffic, repeated startup context, and free-form handoffs crowded the useful signal. Now: bounded messages, clear returns, and calmer updates keep it visible.
```

**Reply (Was → Now):**

```text
• *Message limits* — Was: oversized messages and notes could crowd out useful context. Now: clear limits reject overlong content before queueing and point to an artifact remedy.

• *Return format* — Was: handoffs varied and could repeat setup. Now: every handoff uses six predictable fields and stays quiet unless context is running low.

• *Focused updates* — Was: long status messages hid decisions and proof. Now: concise updates put the decision first while detailed evidence stays available.

• *Startup context* — Was: delivered startup context could appear again later. Now: it arrives once per session, with a short source marker and newest task notes first.

• *Reliable reports* — Was: release reports could count a pull request as an issue. Now: pull requests and issues are distinguished correctly.

Install: use `cas update` after publication, or download the v3.23.0 Linux x86_64 archive (SHA-256 `{{LINUX_SHA256}}`) or macOS ARM64 archive (SHA-256 `{{MACOS_SHA256}}`) from the GitHub release.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.23.0*
Was: traffic, return, delivery, and close-gate contracts were implicit. Now: caps, fixed returns, one-line provenance, and origin-aware recovery are enforced and tested.
```

**Reply (Was → Now):**

```text
• *Traffic caps* — Was: coordination bodies and notes had no shared size budget. Now: 1,200-character messages, 2,500-character escalations, and 1,500-character notes are enforced before queueing. Commit `6438dbd3`.

• *Return contract* — Was: launch responses varied and consumed context with narration. Now: every launch carries six required return fields and silent execution, with headroom only below 20 percent. Commits `789be31f` and `5c33d2d2`.

• *Pane budget* — Was: supervisor output mixed decisions with long evidence recaps. Now: concise pane responses foreground the decision and keep evidence in notes or artifacts. Commit `095c3b14`.

• *Delivery provenance* — Was: startup and fallback paths could replay delivered mail and expose a long provenance header. Now: startup claims and enqueue are idempotent, delivered mail is excluded from tool-result fallback, task details show the newest five notes, claim files are pruned, and provenance is one line. Commits `987ea4c8` and `aac77a6d`.

• *Close-gate safety* — Was: trunk reachability, merge recovery, and epic refresh could rely on local state. Now: origin reachability, worktree-scoped history changes, and published-parent checks fail closed. PR #806.

• *Release-report references* — Was: pull-request references could be counted as issues. Now: issue extraction distinguishes pull requests from issues. PR #801.

Validation: `cargo metadata --locked`, the full release gate, published asset receipt, and release report receipt are required before posting.

Install: `cas-x86_64-unknown-linux-gnu.tar.gz` (SHA-256 `{{LINUX_SHA256}}`) and `cas-aarch64-apple-darwin.tar.gz` (SHA-256 `{{MACOS_SHA256}}`) will be published by CI.
```

## POSTED

Posted 2026-09-10 (UTC) to #cas-internal (C0B44GUKDK2) via the direct
MechaCassy MCP:
- User top-level 1789048924.365619 — https://petra-stella.slack.com/archives/C0B44GUKDK2/p1789048924365619
- User reply 1789048932.103179 — https://petra-stella.slack.com/archives/C0B44GUKDK2/p1789048932103179?thread_ts=1789048924.365619&cid=C0B44GUKDK2
- Dev top-level 1789048933.259399 — https://petra-stella.slack.com/archives/C0B44GUKDK2/p1789048933259399
- Dev reply 1789048941.820479 — https://petra-stella.slack.com/archives/C0B44GUKDK2/p1789048941820479?thread_ts=1789048933.259399&cid=C0B44GUKDK2
