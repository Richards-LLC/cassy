# Slack draft — Cassy v3.25.6 runtime release

Channel: #cas-internal (C0B44GUKDK2). Transport: MechaCassy hub/bot only.

**Status: Draft.** Do not post until the tagged GitHub workflow publishes both
assets and `release-published-receipt.sh --write-draft` replaces both checksum
placeholders below. No tag, publication, report upload, or Slack post is part of
this preparation.

## Preparation record (not for Slack)

- Source: assembled v31 release base `362fc8c10825e7034b670693a455be97ece5346f`.
- The assembled source excludes cursor delivery `e33b699c`; this draft makes no
  cursor-flow claim. The release also makes no claim that the unresolved
  release-admission ENOENT issue is fixed.
- The previous release's four POSTED receipts remain in
  [`2026-09-11-v3.25.5-slack.md`](2026-09-11-v3.25.5-slack.md) and its canonical
  artifact bundle. They are not copied into this unposted release's receipt.
- Prior publication evidence:
  `/home/pippenz/.cas/artifacts/release/v3.25.5-release-3.25.5-prepare/`.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.25.6*
Was: active Codex work vanished from status. → Now: running work stays visible, and `cas doctor` distinguishes expected copies from safe cleanup.
```

**Only reply:**

```text
• *Skill maintenance* — Was: expected Codex copies hid stale skills. → Now: `cas doctor` explains both and offers cleanup only for Cassy-owned files.

• *Status you can trust* — Was: active Codex work could look dead. → Now: Cassy tracks the real process and preserves its available exit result.

• *Safer cleanup* — Was: user-created targets could resemble Cassy workspaces. → Now: uncertain or active data is preserved; only confirmed inactive targets are removed.

Install: use `cas update`, or download v3.25.6 from https://github.com/Richards-LLC/cassy/releases/tag/v3.25.6
Linux x86_64 SHA-256: `{{LINUX_SHA256}}`
macOS ARM64 SHA-256: `{{MACOS_SHA256}}`
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.25.6*
Was: status and release checks could accept incomplete signals. → Now: process identity, explicit proof, target content, and remote bytes must agree.
```

**Only reply:**

```text
• *Process status* — Was: active Codex work could look dead. → Now: verified identity and deferred waits preserve exit code or signal plus a bounded terminal tail.

• *Close verification* — Was: ambiguous pass notes or stale branches could satisfy checks. → Now: bounded typed proofs and target-branch content are required, including after request changes.

• *Credential isolation* — Was: launches could inherit machine-global operator secrets. → Now: protected values require scoped grant provenance; provider authentication remains.

• *Owned-target cleanup* — Was: branch names alone implied ownership. → Now: cleanup revalidates ownership and preserves uncertain, active, recent, or replaced targets.

• *PDF receipts* — Was: upload acknowledgement did not prove stored bytes. → Now: remote size, SHA-256, decode, and page count must match before receipt.

• *Concurrent writes* — Was: parallel notes or hook reports could lose updates or session context. → Now: appends are atomic and outcomes stay bound to their harness session.

Validation and artifact: {{VALIDATION_SUMMARY}}. Published Linux archive: `cas-x86_64-unknown-linux-gnu.tar.gz` (SHA-256 `{{LINUX_SHA256}}`). Published macOS archive: `cas-aarch64-apple-darwin.tar.gz` (SHA-256 `{{MACOS_SHA256}}`).
```

## Posting sequence

1. Replace the narrative placeholders after the release PR is merged.
2. Tag the fetched `origin/main` landing and run
   `./scripts/release.sh --publish-tag`. A bare `release.sh` is a local audit
   and does not touch the remote.
3. Run `release-published-receipt.sh --write-draft` against this draft. It
   downloads and hashes both published assets before replacing every digest
   placeholder; never type a digest from a local audit archive.
4. Post the User top-level and its only reply, then the Dev top-level and its
   only reply; append the receipt below.

## POSTED

Channel: `#cas-internal` (`C0B44GUKDK2`).

| Message | UTC timestamp | Permalink |
| --- | --- | --- |
| User top-level |  |  |
| User reply (Was → Now) |  |  |
| Dev top-level |  |  |
| Dev reply (Was → Now) |  |  |
