# Slack draft — Cassy v3.25.8 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

**Status: DRAFT. Not posted. Post only after the annotated tag, release workflow, install-path proof and both asset digests are verified; then replace this line with the POSTED receipt.**

Contents: the v32 issue burn-down (source merge PR #898, announced 2026-09-18 as "Source on main"), released as a runtime. The source-merge announcement receipts live in `docs/release-notes/2026-09-18-issue-burn-down-v32-slack.md`.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.25.8*
Was: closing a task after a reviewer merged it could fail with a confusing error. → Now: it closes, and fourteen more everyday snags are gone.
```

**Only reply:**

```text
• *Closing after review* — Was: after a reviewer merged your work, closing could reject the recorded change. → Now: the task closes from that merged work.

• *Retrying a rejected check* — Was: retrying could replay the same rejection. → Now: retry checks the repaired change while keeping prior feedback.

• *Starting independent work* — Was: an open prerequisite could block unrelated work from starting. → Now: unrelated work can start while the prerequisite remains open.

• *Fresh reminders* — Was: a recent action could still trigger an old reminder. → Now: recent activity resets the reminder quiet period.

• *Protected workspace* — Was: maintenance could pull the workspace out from under active work. → Now: active work keeps its workspace intact.

• *Clear build failures* — Was: a knowledge build could fail without naming the file. → Now: it identifies the file that failed and explains next steps.

• *Workspace-preserving restart* — Was: restarting a near-limit session could lose its workspace. → Now: an idle session restarts in place with its workspace and setup.

• *Shortcut names* — Was: shortcut names could behave differently from their documentation. → Now: documented names resolve consistently.

Update with `cas update`. Published downloads and installation checks passed on Apple Silicon macOS and Linux.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.25.8*
Was: close and lifecycle decisions trusted mutable refs and reusable state. → Now: delivery evidence and verification cycles bind to current ancestry.
```

**Only reply:**

```text
• *#873 / #887 / #892 — Anchors* — Was: merge tips, remote-only checks, and missing receipts could obscure delivery. → Now: recorded anchors, local targets, and branch tips preserve close proof.

• *#883 — Fresh verification* — Was: a rejected dispatch could authorize a retry. → Now: rejection stays in history and retry mints a current-head dispatch.

• *#886 — Dependency timing* — Was: dependencies stopped start and serialized work. → Now: start warns; close and merge enforce order; `requires_start` is the explicit hard gate.

• *#888 — Relay supersession* — Was: delayed relays could revive old state. → Now: transition and branch-tip identity revalidate each relay.

• *#884 / #885 — Checkout ownership* — Was: sync could rebase a live checkout and detach HEAD. → Now: both sync paths refuse live worker checkouts.

• *#890 — Shutdown reachability* — Was: shutdown checked only unpushed commits or the default branch. → Now: it checks task target, pinned epic and default branch, and fails safe.

• *#889 — In-place recycling* — Was: a near-limit idle Codex worker needed a full replacement. → Now: `recycle_worker` restarts it with the same name, worktree and recipe.

• *#893 — Idle reset* — Was: a tool call could leave the actionable-idle timer running. → Now: supervisor activity resets it.

• *#874 — Source diagnostics* — Was: builds hid which source failed. → Now: progress names each source; status exposes failure details.

• *#891 — MCP aliases* — Was: `get`, `blocked_by`, `summary`, `target`, `inbox` were rejected. → Now: they resolve to their canonical fields and are documented.

• *#897 — Seed freshness* — Was: worker target seeds could be missing, unrelated, or old. → Now: snapshot provenance is recorded, changed crates are skipped, stale seeds warn.

Release workflow and Apple Silicon macOS + clean Linux install proof passed on the published SHA. Linux `{{LINUX_SHA256}}` · macOS `{{MACOS_SHA256}}`.
```

## Prior release receipt carried forward (v3.25.7; not v3.25.8 POSTED evidence)

| Message | UTC timestamp | Message ID | Permalink |
| --- | --- | --- | --- |
| User top-level | 2026-09-15T18:11Z | 1789495897.898159 | https://petra-stella.slack.com/archives/C0B44GUKDK2/p1789495897898159 |
| User reply | 2026-09-15T18:11Z | 1789495906.420669 | https://petra-stella.slack.com/archives/C0B44GUKDK2/p1789495906420669?thread_ts=1789495897.898159&cid=C0B44GUKDK2 |
| Dev top-level | 2026-09-15T18:11Z | 1789495915.762469 | https://petra-stella.slack.com/archives/C0B44GUKDK2/p1789495915762469 |
| Dev reply | 2026-09-15T18:11Z | 1789495935.833019 | https://petra-stella.slack.com/archives/C0B44GUKDK2/p1789495935833019?thread_ts=1789495915.762469&cid=C0B44GUKDK2 |

Source: `docs/release-notes/2026-09-15-v3.25.7-slack.md`.
