# Slack draft — Cassy v3.27.1 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

Contents: the three fixes from the first Pebble test on the phone — replies that never arrived, the keyboard covering the thread, and the local hub not restarting on update. Asset digests are filled in by the train's receipts stage.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.27.1*
Was: you asked Cassy a question from your phone and nothing came back. → Now: the answer lands in the thread.
```

**Only reply:**

```text
• *Answers arrive* — Was: Cassy could address its reply to your name instead of the conversation, and it vanished while still reporting "sent". → Now: replies to your name reach you, and a reply to nowhere is refused on the spot.

• *Typing on the phone* — Was: opening the keyboard pushed the header and the latest messages off the top of the screen. → Now: header, latest message and the box you are typing in all stay in view.

• *Hub comes back after updates* — Was: updating Cassy could leave your hub running the old version, outside its service. → Now: the update restarts the hub through its service on the new version, at the same address.

Update with `cas update`. Published downloads and installation checks passed on Apple Silicon macOS and Linux.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.27.1*
Was: `commander:<label>` targets were queued to nobody and abandoned. → Now: they route to the operator lane; impossible targets are refused.
```

**Only reply:**

```text
• *Reply routing* — Was: message accepted any target string; a `commander:…` reply had no verified-device stamp, so `peek_operator_replies` never selected it and the daemon abandoned it 15 min later. → Now: `commander:<label>` resolves the newest verified Commander row (in_reply_to + device), row-source prefixes and empty targets are refused before enqueue, queue-before-register for plain names is unchanged, and Commander rows are framed with the exact reply command in inbox, hook and daemon renderers.

• *Keyboard viewport* — Was: viewport meta without `interactive-widget`, 100dvh shell scrolled under the soft keyboard. → Now: `interactive-widget=resizes-content`, `--keyboard-viewport-height` from visualViewport as fallback, `followTail()` on composer focus; pebble-qa keyboard receipts at 390x544 on both paths.

• *Hub service restart* — Was: update relaunched a detached `cas hub serve --launched-by update` beside an inactive unit and later left the old binary running. → Now: with cas-hub.service installed, restart goes through `systemctl --user restart`, the outcome records old→new version, the unit keeps `--tailscale-serve`, and status/doctor flag an inactive unit with a detached lock holder; systemctl is injected via `CAS_HUB_SYSTEMCTL` in tests.

• *Assets* — Was: n/a. → Now: `cas-x86_64-unknown-linux-gnu.tar.gz` `2c02c807a5ec392c8e933a65c62c9ee7a0a8c9778a0bca7b0751f9b5bef2a1cb`; `cas-aarch64-apple-darwin.tar.gz` `649cd316c23f4643e1c75f70348f16457b769917a64a537b10715ded66fa70f9`.

Install: `cas update`.
```

## POSTED

- **Posted at (UTC):** `2026-09-21T17:23:10Z`
- **Channel:** `#cas-internal (C0B44GUKDK2)`
- **User top-level:** `message_id=1790011390.875619` · <https://petra-stella.slack.com/archives/C0B44GUKDK2/p1790011390875619>
- **User reply:** `message_id=1790011392.147339` · <https://petra-stella.slack.com/archives/C0B44GUKDK2/p1790011392147339?thread_ts=1790011390.875619&cid=C0B44GUKDK2>
- **Dev top-level:** `message_id=1790011393.388849` · <https://petra-stella.slack.com/archives/C0B44GUKDK2/p1790011393388849>
- **Dev reply:** `message_id=1790011394.659129` · <https://petra-stella.slack.com/archives/C0B44GUKDK2/p1790011394659129?thread_ts=1790011393.388849&cid=C0B44GUKDK2>
