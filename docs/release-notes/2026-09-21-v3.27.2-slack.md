# Slack draft — Cassy v3.27.2 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

Contents: conversation history on the hub. Asset digests are filled in by the train's receipts stage.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.27.2*
Was: reopening a conversation on the hub showed an empty thread. → Now: what you asked and what Cassy answered are there.
```

**Only reply:**

```text
• *History on open* — Was: the hub only remembered messages while the page stayed open; a fresh open or a hub restart showed nothing. → Now: opening a conversation brings back your messages and Cassy's replies for that machine, with a control to load earlier ones.

• *Only your conversation* — Was: n/a. → Now: history is limited to messages sent from your paired device and replies addressed to it; terminal output is never shown.

• *Needs both sides updated* — Was: n/a. → Now: a machine still on an older version keeps the previous behaviour until it updates.

Update with `cas update`. Published downloads and installation checks passed on Apple Silicon macOS and Linux.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.27.2*
Was: hub-web ConversationHistory was in-memory only. → Now: a device-scoped history page is served from prompt_queue through daemon and hub.
```

**Only reply:**

```text
• *Store* — Was: no history read. → Now: `PromptQueueStore::conversation_history(session, device, before, limit)` returns verified Commander sends for that device plus operator-lane replies addressed to it or to the wildcard recipient, ordered with an explicit cursor.

• *Protocol* — Was: n/a. → Now: a history request/response pair on the client/daemon protocol, request-correlated; the hub enforces the authenticated device id before dispatch and gates the page by request id.

• *hub-web* — Was: hydration impossible. → Now: `ConversationHistory` hydrates from the page before live frames, dedupes by notification id and client ref, renders through the Pebble thread model, and pages with "load earlier"; dist rebuilt once.

• *Assets* — Was: n/a. → Now: `cas-x86_64-unknown-linux-gnu.tar.gz` `{{LINUX_SHA256}}`; `cas-aarch64-apple-darwin.tar.gz` `{{MACOS_SHA256}}`.

Install: `cas update`.
```
