# Slack draft — Cassy v3.27.4 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

Contents: factory and release-train reliability fixes from the 3.27.3 cut. Asset digests are filled in by the train's receipts stage.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.27.4*
Was: a finished change could sit unnoticed for an hour. → Now: Cassy notices the moment it lands and moves on.
```

**Only reply:**

```text
• *Landings are noticed* — Was: after a change landed, nothing happened until someone looked. → Now: Cassy picks it up at once and tells you if a step is still open five minutes later.

• *Second tasks start clean* — Was: a second task could drag the first task's changes along with it. → Now: the slate is reset, or you get the exact fix before anything starts.

• *Conflicts get resolved* — Was: resolving a merge conflict could stall at the last step. → Now: it completes on its own.

• *Releases cut cleaner* — Was: the 3.27.3 release needed seven hand steps. → Now: each of them is automated and self-tested.

Update with `cas update`. Published downloads and installation checks passed on Apple Silicon macOS and Linux.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.27.4*
Was: idle assignees, stale worker branches, blocked rebases, hand-driven cuts. → Now: PR-merge wakes, branch reconciliation, rebase-aware guard, automated train gaps.
```

**Only reply:**

```text
• *Daemon* — Was: no view of delivery PRs. → Now: PR numbers and merge commits recorded on the task; one typed assignee wake with the close command; supervisor relay after five minutes (PR #941).

• *Task start* — Was: factory/<worker> kept prior commits. → Now: reset when the prior delivery is merged, refuse with `git rebase --onto` otherwise; envelope carries commits_not_on_target_base (PR #938).

• *Guards* — Was: commit guard refused detached rebase HEAD; checkout-read guard scanned only cas-cli/tests. → Now: branch from rebase-merge/head-name (PR #939); every test-bearing source including lib cfg(test) modules (PR #942).

• *Release train* — Was: receipts via docs PR; stale lockfile, wall-clock draft date, post-publish lint, enqueue race. → Now: receipts commit on the release branch (PR #933); lockfile refresh, docs-only rebase, pinned run date, preflight lint, checks-aware enqueue (PR #940).

• *Integration* — Was: union sweep conflicted on ancestor epics; close gate suggested fewer targets than the checker demanded. → Now: ancestors skipped and base named (PR #936); one shared derivation, cached per close (PR #937).

• *Assets* — Was: n/a. → Now: `cas-x86_64-unknown-linux-gnu.tar.gz` `{{LINUX_SHA256}}`; `cas-aarch64-apple-darwin.tar.gz` `{{MACOS_SHA256}}`.

Install: `cas update`.
```
