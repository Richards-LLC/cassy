# Slack draft — Cassy v3.27.5 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

Contents: project-wide conversation history on the hub, readable older replies, compact list rows. Asset digests are filled in by the train's receipts stage.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.27.5*
Was: a hub conversation showed only today's session. → Now: it shows the whole history of that project with you.
```

**Only reply:**

```text
• *Whole history* — Was: opening a conversation showed only the current session's messages, often nothing. → Now: everything you asked and everything Cassy answered for that project, across sessions, with a light divider between sessions and a clear "No earlier history" end.

• *Older replies read well* — Was: a status answer from an older session arrived as one paragraph. → Now: it shows a bold lead line and a numbered list even without formatting in the text.

• *Compact list* — Was: one long reply could fill the Conversations screen. → Now: every row previews two lines.

Update with `cas update`, then restart the project's running session to get the wider history. Published downloads and installation checks passed on Apple Silicon macOS and Linux.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.27.5*
Was: conversation_history filtered on factory_session; plain replies rendered as paragraphs. → Now: project-scoped, device-filtered history with session metadata; legacy-prose readability pass.
```

**Only reply:**

```text
• *Store/daemon/hub* — Was: `PromptQueueStore::conversation_history` keyed on factory_session. → Now: project-wide with verified-device and recipient filters intact, chronological cursor paging, session metadata on history rows; hub-web renders dividers and the end marker (PR #947).

• *Renderer* — Was: bodies without Markdown rendered as split paragraphs. → Now: lead-gated "(1) … (2) …" enumerations become an ordered list with a bold lead; Markdown bodies untouched (PR #946). Conversation-list previews clamp to two lines / 160 chars with an allowlisted intentional clip (PR #945).

• *Release train* — Was: assemble self-heal lacked CAS_FACTORY_SESSION under the stripped launch env; prep bump read CHANGELOG relative to cwd; announce lint hid the offending line. → Now: factory_session recorded in run.env and passed only to the recover subprocess; prep runs in the worktree; lint prints the line (PR #944).

• *Assets* — Was: n/a. → Now: `cas-x86_64-unknown-linux-gnu.tar.gz` `{{LINUX_SHA256}}`; `cas-aarch64-apple-darwin.tar.gz` `{{MACOS_SHA256}}`.

Install: `cas update`.
```
