# Slack draft — Cassy v3.27.6 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

Contents: running conversations always appear on the hub, older status replies read well, release train recovery fix. Asset digests are filled in by the train's receipts stage.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.27.6*
Was: a running conversation could vanish from the hub. → Now: every live conversation is listed.
```

**Only reply:**

```text
• *Every live conversation shows* — Was: a project that was running but had no helpers at work was hidden from the hub, so the machine looked empty. → Now: it is listed as soon as it is running; only stalled, empty ones stay hidden.

• *Older status replies read well* — Was: a status reply like "Status 13:2xZ." still arrived as one paragraph. → Now: it shows a bold lead line and a list.

• *Empty conversations* — Was: a new conversation said "No earlier history". → Now: it shows the empty state.

Update with `cas update`. Published downloads and installation checks passed on Apple Silicon macOS and Linux.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.27.6*
Was: the hub catalog required a non-empty worker roster. → Now: any live, non-dormant supervisor session is listed.
```

**Only reply:**

```text
• *Hub catalog* — Was: `supervisor_sessions` default filter required `!workers.is_empty()`, hiding live supervisors with zero workers. → Now: default lists every Live non-dormant supervisor session; `workers=1` and `dormant=1` reveals unchanged; tests pin staffed, empty-roster, worker-only, hung-empty and dormant rows (PR #952).

• *Renderer* — Was: the legacy lead regex required two-digit minutes, so "Status 13:2xZ." never restructured; empty threads showed the end marker. → Now: lead matches the real text; empty threads render the empty state (PR #951).

• *Release train* — Was: assemble self-heal's integration-recover refused without the recorded supervisor identity; post-publication lacked the date helper. → Now: recorded identity passed to recovery; helper sourced (PR #950).

• *Assets* — Was: n/a. → Now: `cas-x86_64-unknown-linux-gnu.tar.gz` `{{LINUX_SHA256}}`; `cas-aarch64-apple-darwin.tar.gz` `{{MACOS_SHA256}}`.

Install: `cas update`.
```
