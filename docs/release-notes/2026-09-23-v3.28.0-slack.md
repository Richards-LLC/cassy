# Slack draft — Cassy v3.28.0 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

Contents: newer default AI models, Claude Haiku retired, session wrap-up moved to the background, and a Claude Code version check. Asset digests are filled in by the train's receipts stage.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.28.0*
Was: Cassy ran on older AI models and used Claude Haiku for chores. → Now: newer, cheaper models by default, and Haiku is gone.
```

**Only reply:**

```text
• *Newer models* — Was: everyday coding ran on GPT-5.6 Luna and planning on Fable 5.1. → Now: coding runs on GPT-6 Sol; planning, design and hard problems run on Claude Opus 5.5.

• *Haiku retired* — Was: small chores ran on Claude Haiku. → Now: they run on GPT-6 Luna, and Haiku is not used anywhere.

• *Faster session end* — Was: ending a session could pause while summaries and reviews ran. → Now: they run in the background after the session ends.

• *Claude Code check* — Was: nothing told you when Claude Code was out of date. → Now: Cassy warns below 2.1.280 and suggests `claude update`.

Update with `cas update`. Published downloads and installation checks passed on Apple Silicon macOS and Linux.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.28.0*
Was: Luna/xhigh standard, Fable/medium supervisor, Haiku light. → Now: GPT-6 Sol/medium, Opus 5.5/high, GPT-6 Luna/xhigh; Claude 2.1.280 pinned.
```

**Only reply:**

```text
• *Lane registry* — Was: standard gpt-5.6-luna/xhigh, supervisor and taste claude-fable-5-1/medium, heavy gpt-6-astra/high, light claude-haiku. → Now: standard gpt-6-sol/medium (fb gpt-6-luna/xhigh), supervisor/taste/heavy claude-opus-5-5/high (fb fable-5-1/high, opus-5/high, gpt-6-astra/high), light gpt-6-luna/xhigh (fb opus-5-5/low) (PR #964).

• *Haiku removed* — Was: claude_haiku recipe plus Haiku defaults in daemon, hooks, init and extraction. → Now: recipe deleted; registry, slug and spawn validation reject Haiku with a light-lane pointer (PR #964).

• *Maintenance jobs* — Was: Stop blocked and asked the parent session to spawn Haiku subagents. → Now: Stop queues light-lane one-shot Codex jobs (flock-serialized, 300 s cap); daemon extraction uses the same runner (PR #964).

• *Claude conformance* — Was: no Claude Code live matrix or receipt. → Now: claude-code-2.1.280 receipt (11 pass, 2 not_covered); preflight/doctor warn below the pin (PR #964).

• *Codex matrix* — Was: pinned to suspended gpt-5.6-terra, two trivial cs calls. → Now: follows the standard-lane recipe, complex-schema check, budget_aborted liveness, request_user_input disabled for workers (PR #964).

• *Assets* — Was: n/a. → Now: `cas-x86_64-unknown-linux-gnu.tar.gz` `{{LINUX_SHA256}}`; `cas-aarch64-apple-darwin.tar.gz` `{{MACOS_SHA256}}`.

Install: `cas update`.
```
