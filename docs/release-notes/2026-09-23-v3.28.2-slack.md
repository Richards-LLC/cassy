# Slack draft — Cassy v3.28.2 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

Contents: an independent review before visible changes merge, proof of quality at close, hub flow tests before release, the hub palette fix, and a fix for tasks stuck after merge. Asset digests are filled in by the train's receipts stage.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.28.2*
Was: visible changes shipped after only their author checked them. → Now: a second reviewer checks the real build and its polish first.
```

**Only reply:**

```text
• *Second reviewer* — Was: the person who built a change was the only one to try it. → Now: someone else walks it on the real build, including phone, dark mode and keyboard, before it can merge.

• *Proof at close* — Was: a visible change could be marked done without a recording or screenshots. → Now: it needs a recorded walkthrough, light and dark screenshots at desktop and phone sizes, and a polish score.

• *Hub flow checks* — Was: hub releases were checked piece by piece. → Now: the eleven main hub flows are tested end to end, and a release waits for a scored walkthrough.

• *Command palette* — Was: typing filtered nothing, Enter did nothing, and reopening kept old text. → Now: it narrows to matches, Enter opens the conversation ready to reply, and it reopens clean.

• *High contrast* — Was: in Windows high-contrast mode the palette had no edges or visible focus. → Now: edges, a full focus ring and a clearly marked open session.

• *Stuck tasks* — Was: some finished tasks could not be marked done. → Now: they close normally, with the correction recorded.

Update with `cas update`. Published downloads and installation checks passed on Apple Silicon macOS and Linux.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.28.2*
Was: user-facing deliveries merged on self-reported QA. → Now: evidence gate at close plus an independent QA round bound to the exact tip.
```

**Only reply:**

```text
• *Independent QA pass* — Was: no second look before merge. → Now: parking a user-facing delivery opens a qa_pass round and QA task for a non-implementer reviewer; worktree_merge, the raw git merge guard and the close backstop require a passed or waived round for the tip (migration 257, `qa.independent_pass`).

• *Evidence close gate* — Was: demo_statement closes relied on prose. → Now: the close validates the cas-c3b8 bundle.json for the delivered head (trace Expect steps, receipt, aria, polish renders, visual-qa, critique floor); non-web demos need a real-build ledger row and terminal-qa when render paths change (`qa.evidence_gate`).

• *Skip markers* — Was: an added fixme/skip/only could slip through. → Now: every delivery is scanned, with a logged cas-allow-skip escape.

• *Hub journeys* — Was: no end-to-end hub flow suite. → Now: HUB-J1..J11 in hub-web/e2e/journeys; release prep blocks until a scored evaluation covers the hub-web/dist tree.

• *Palette filter* — Was: .palette-command display:grid overrode [hidden]. → Now: hidden rows hide, session matches sort first, HUB-J3 asserts it.

• *Palette keyboard and focus* — Was: Enter in the filter left the dialog open; jumps left focus on body; a lease render could rebuild the open dialog and wipe the filter; reopen kept stale filter state. → Now: Enter closes and jumps, keyboard/mouse jumps focus the composer (touch does not), render() treats focus in the open palette as composing, and open resets the filter.

• *Forced colors* — Was: palette rows and dialog had no edges and the focus ring was clipped by the list's overflow. → Now: ButtonBorder rows, CanvasText dialog edge, an inset ring, and an open-session Highlight fill that survives hover (8.73:1 minimum).

• *Scoped proof surface* — Was: nested test modules could be missed by the scoped-proof surface check. → Now: nested modules are qualified, with tests (PR #972).

• *Ledger grammar* — Was: the close gate required a leading pipe the docs didn't show. → Now: rows with or without outer pipes are accepted.

• *Close-gate deadlock* — Was: proof_scope_fix needed a delivery transaction a supervisor merge never created. → Now: worktree_merge records an observed merge; scope and risk corrections work in progress or parked; override waives measured gaps but keeps the scoped proof (PR #969).

• *Assets* — Was: n/a. → Now: `cas-x86_64-unknown-linux-gnu.tar.gz` `794d4607b09a419021e8f416ffc037291b7e2f95bd1b7613e84a171a34fbcd7e`; `cas-aarch64-apple-darwin.tar.gz` `043cfb46422510185865a1377c391971da5ef4b4d04df7953c50c9ba08c987a4`.

Install: `cas update`.
```

## POSTED

- **Posted at (UTC):** `2026-09-23T21:21:32Z`
- **Channel:** `#cas-internal (C0B44GUKDK2)`
- **User top-level:** `message_id=1790198492.819419` · <https://petra-stella.slack.com/archives/C0B44GUKDK2/p1790198492819419>
- **User reply:** `message_id=1790198494.082189` · <https://petra-stella.slack.com/archives/C0B44GUKDK2/p1790198494082189?thread_ts=1790198492.819419&cid=C0B44GUKDK2>
- **Dev top-level:** `message_id=1790198495.306159` · <https://petra-stella.slack.com/archives/C0B44GUKDK2/p1790198495306159>
- **Dev reply:** `message_id=1790198496.568449` · <https://petra-stella.slack.com/archives/C0B44GUKDK2/p1790198496568449?thread_ts=1790198495.306159&cid=C0B44GUKDK2>
