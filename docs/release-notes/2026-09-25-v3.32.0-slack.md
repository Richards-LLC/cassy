# Slack draft — Cassy v3.32.0 runtime release

Channel: #cas-internal (`C0B44GUKDK2`). Transport: MechaCassy hub/bot only.

**Status:** Draft. Do not post until the tagged GitHub workflow publishes both
assets, the release report is verified, and `release-published-receipt.sh
--write-draft` replaces both checksum placeholders below.

## User thread

**Top-level:**

```text
*Live on production — User — Cassy v3.32.0*
Was: suggested commands could fail and startup guidance got cut off. → Now: suggestions work as written and startup guidance arrives whole.
```

**Only reply:**

```text
*Everyday use*

• *Suggested commands* — Was: help text could offer a command Cassy rejects. → Now: every suggested command is checked against what Cassy accepts.

• *Startup guidance* — Was: session startup text could exceed its size limit and arrive as a preview. → Now: it fits in full for every role.

• *Closing tasks* — Was: close checks could judge an older version of your change. → Now: they check the version you just delivered.

• *Command names* — Was: messaging and team control shared one tool. → Now: team control has its own tool; the old names still work this release.

*Your installation*

• *Updates* — Was: `cas update` left some skill files at their first version and kept retired ones. → Now: it refreshes every file, keeps your edits, removes the rest.

• *Grok, OpenCode and Codex* — Was: Grok and OpenCode could load skills with commands they cannot run. → Now: each gets a copy that works for it.

• *Instruction files* — Was: `CLAUDE.md` repeated the shared project instructions and grew past its limit. → Now: the shared file leads and `CLAUDE.md` imports it.

• *Project boundaries* — Was: rules and notes from one project could land in another. → Now: Cassy refuses them and keeps them out of sync.

• *Release notes skill* — Was: it was called `release-notes`. → Now: it is `cas-release-notes`, so it no longer clashes with a Grok command.

Update with `cas update`. Published Linux x86_64 archive SHA-256: `{{LINUX_SHA256}}`. Published Apple Silicon macOS archive SHA-256: `{{MACOS_SHA256}}`.
```

## Dev thread

**Top-level:**

```text
*Live on production — Dev — Cassy v3.32.0*
Was: skills and runtime prompts drifted from the MCP surface. → Now: a call-shape lint, one skill tree and budget tests pin them to it.
```

**Only reply:**

```text
*Prompts and tools*

• *Call-shape lint* — Was: suggested calls could omit `summary`, `remind_message` or `dispatch_id`. → Now: a test checks every call in skills and runtime templates.

• *Tool split* — Was: `coordination` shipped 61 params to every session. → Now: `factory` holds fleet, worktree, server, db, loop and queue; old names alias for one release.

• *SessionStart* — Was: worker and supervisor payloads hit 11.8–13.2 KB, past the hook cap. → Now: at most 9,216 B for every role, pinned by a test.

• *MCP schema* — Was: one description passed the 2 KB cut and 16% of tools/list was boilerplate. → Now: descriptions ≤2,048 chars, action enums, no nullable noise.

• *Worker prompts* — Was: Claude workers in a Codex session got `mcp__cs__` names. → Now: prefixes resolve per recipient and one renderer builds every contract.

• *Close gates* — Was: gates keyed off stale heads, so one sibling merge voided other dispatches. → Now: they use the current tip; platform proof defers to assembly.

*Catalog, install and store*

• *One skill tree* — Was: Claude, Codex and Grok catalogs were hand-kept twins. → Now: bare tool names; Grok and OpenCode get project copies, Codex no `.md` agents.

• *Install sync* — Was: non-SKILL.md files never refreshed after first install. → Now: every file refreshes, removed files prune, and `cas doctor` reports parity.

• *Store guard* — Was: another project's rules and pages could be written, pulled or pushed here. → Now: writes are refused and sync keeps them out.

• *Verifier and Stop jobs* — Was: task-verifier was 27.7 KB and session-learn parsed skill text. → Now: 9.3 KB, a dedicated classifier, and `rule action=promote`.

• *Operator-data lint* — Was: shipped skills carried real names and ids. → Now: a lint keeps them synthetic.

• *Instruction files* — Was: repo `CLAUDE.md` duplicated `AGENTS.md`. → Now: `AGENTS.md` is canonical and `CLAUDE.md` imports it with Claude-only lines.

Validation: nextest 10,160/10,160 plus doctests at the assembled head. Published `cas-x86_64-unknown-linux-gnu.tar.gz` SHA-256: `{{LINUX_SHA256}}`. Published `cas-aarch64-apple-darwin.tar.gz` SHA-256: `{{MACOS_SHA256}}`.
```

## Posting sequence

1. Land the release commit through the protected-main PR, then tag the fetched
   landing and publish using the release train.
2. Verify both published archives, installation, the release report and the
   latency receipt. Use `release-published-receipt.sh --write-draft` for both
   digests.
3. Post the User parent and reply, then the Dev parent and reply through
   MechaCassy. Append returned timestamps and permalinks below.

## POSTED

Channel: `#cas-internal` (`C0B44GUKDK2`).

| Message | UTC timestamp | Permalink |
| --- | --- | --- |
| User top-level |  |  |
| User reply (Was → Now) |  |  |
| Dev top-level |  |  |
| Dev reply (Was → Now) |  |  |
