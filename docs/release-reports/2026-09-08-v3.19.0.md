# Cassy v3.19.0

Published 8 September 2026 · 20:46 UTC

Five themes, twenty-one issues closed: clearer states and safer next steps across Cassy.

## Change map

Delivery has the largest cluster; the closures reach five everyday surfaces.

One filled dot represents one closed issue. Dots have equal weight: this is a count of closures, not a measured impact score. Installation also improved, with no separate issue in this ledger.

| Surface | Issues closed | Issue numbers |
| --- | ---: | --- |
| Factory | 4 | #705, #746, #748, #749 |
| Delivery | 6 | #719, #734, #742, #744, #750, #751 |
| Cloud | 3 | #717, #730, #732 |
| Diagnostics | 5 | #718, #737, #738, #739, #745 |
| MechaCassy | 3 | #724, #731, #752 |
| Install | 0 | Release hardening; no listed issue |
| Total | 21 | Five issue-bearing themes |

Source: [PR #761](https://github.com/Richards-LLC/cassy/pull/761) and the 21 closed GitHub issues in the fixes ledger; retrieved 2026-09-08 20:58 UTC. Grouping is editorial, following the PR. “Every state explains itself” is the release direction, not a claim that every possible state was exhaustively tested.

## Release at a glance

| Measure | Value | Definition |
| --- | ---: | --- |
| Issues closed | 21 | The named, verified closed issues in this report |
| Features added | 4 | Top-level Added entries in CHANGELOG 3.19.0 |
| Fix entries | 2 | Top-level Fixed entries; additional repairs appear under Changed |
| Green to published | 49m 30s | gate.green.epoch → release-published.receipt; 2970 seconds |

Counts describe this release only; they are not a comparison with the previous release. Published at 2026-09-08T20:46:23Z. Issue closures were recorded between 20:54:44Z and 20:55:16Z, after publication.

## What you can do now

### Daily confidence
#### Clearer health checks

Was: one warning could cover several different problems.

Now: checks separate missing projects, incomplete setup, and unreachable services with the right remedy.

#### History from the right place

Was: code history could look in the wrong repository.

Now: it uses the configured history source or the project's GitHub home.

#### Honest hub status

Was: a healthy public route could look loopback-only.

Now: status shows when the live public route is working.

### Safer recovery
#### Cloud conflicts

Was: a project-name mismatch could look like a retryable sync failure.

Now: Cassy explains the conflict and what to change.

#### Safe cleanup

Was: cleanup could drift after its preview.

Now: Cassy checks the exact rows and stops if state changes.

#### Accepted changes

Was: accepted shared changes could disappear from the local task list.

Now: they appear as open tasks in the intended project.

#### Known projects

Was: a missing folder and an uninitialized project could look the same.

Now: each gets a precise repair path.

### Reliable work
#### Messages you can find

Was: a delivered message could be invisible or repeat.

Now: it stays visible until observed and repeated delivery is bounded.

#### Safe urgent requests

Was: an urgent request could affect the wrong assignment.

Now: ownership is checked before the request takes effect.

#### Clean restarts

Was: clearing a conversation could resume under the wrong identity.

Now: the recipient's new session is confirmed before work continues.

#### Predictable parallel builds

Was: simultaneous builds could compete for the machine.

Now: configured limits keep them within a safe budget.

### Trustworthy updates
#### File uploads you can trust

Was: a successful byte count did not prove a usable image arrived.

Now: uploads are downloaded, hash-checked, decoded, and previewed.

#### Safer installs

Was: a temporary GitHub hiccup could make the installer give up with no explanation.

Now: it retries with credentials and tells you exactly what the release service returned.

## Under the hood

### Branch and build controls
#### Close validation

Was: close and commit guards could inspect the wrong target worktree or stale epic base.

Now: both resolve the declared target, and spawns retain the focused epic base.

#### Push safety

Was: a rejected push existed only as hook stderr.

Now: non-factory destinations are refused and recorded in worker activity.

#### Build throttling

Was: concurrent builders could ignore configured limits.

Now: `factory.max_concurrent_builders` and `factory.worker_build_jobs` cap process and Cargo jobs, with the active throttle in spawn output.

#### Release drift

Was: committed hub-web `dist` output could drift from its source unnoticed.

Now: the release gate rejects stale generated assets.

### Delivery and messaging
#### Parked retries

Was: a parked delivery anchor could point at a stale session.

Now: retries refresh the anchor and preserve actionable upstream Slack errors.

#### Urgent ownership

Was: an urgent wake could consume work owned by another session.

Now: assignment ownership is validated before dispatch.

#### Context reset

Was: clearing context could leave the recipient under an old identity.

Now: the reset runs in the recipient harness and confirms the new transcript.

#### Bounded redelivery

Was: an unobserved Claude delivery could repeat without a clear cap.

Now: attempts and retries stay within a bounded delivery budget.

#### Session routing

Was: a worker relay could land in a generic supervisor session.

Now: relays route to the owning supervisor session.

#### Inbox visibility

Was: a transport-delivered receipt could hide an unread row.

Now: it remains pollable until an observed wake consumes it, and consumption retires exactly one row.

### Cloud synchronization
#### Conflict diagnostics

Was: a cloud HTTP 409 could become a generic queue retry.

Now: `cas cloud sync` and `cas doctor` name the project conflict, park affected rows, and give the exact remedy.

#### Verified purge

Was: foreign-row cleanup could delete a different count or re-admit rows after its preview.

Now: `cas cloud purge-foreign` applies the verified set exactly and aborts on drift.

#### Proposal materialization

Was: an accepted cross-project proposal could remain absent locally.

Now: `cas cloud pull` creates the open target task from the server-attested destination and prints concrete pull errors.

### Diagnostics and configuration
#### Epic status

Was: `mcp__cas__coordination action=epic_status` could spend unbounded work on a large epic.

Now: `offset`, `limit`, and `summary` provide bounded pages and a fast ancestry-only view.

#### Known-repository classifier

Was: `cas doctor` treated missing roots and live roots without a Cassy store alike.

Now: `cas doctor --fix` prunes only missing roots and gives safe manual guidance for live roots.

#### History repository

Was: history indexing reused the bug-intake repository setting.

Now: `history.github_repo` is independently configurable through `cas config`, with GitHub-origin fallback.

#### Upstream reachability

Was: proxy startup exposed only a coarse connection failure.

Now: `cas doctor` probes each configured upstream once, shows redacted detail, and boot output records reachable routes.

#### Healthy hub route

Was: `cas hub status` printed loopback-only for a live CAS-created Tailscale Serve route.

Now: it reports the live hub target while retaining distinct unavailable and unrecorded-route states.

#### Portable Codex workers

Was: Playwright MCP inherited a fixed host home path.

Now: Codex derives `HOME` and its profile from the host home.

### MechaCassy and installation
#### Read preflight

Was: the skill treated an available transport as proof that posting was safe.

Now: it requires the read preflight and documents the approved fallback.

#### Channel operations

Was: channel resolution, edits, and deletes had incomplete guidance.

Now: the skill documents the complete channel contract and safe error handling.

#### Upload integrity

Was: file size or a success response could stand in for proof.

Now: downloaded SHA-256 equality, image decoding, and visible preview are required.

#### Install receipts

Was: a temporary GitHub hiccup could make the installer give up with no explanation.

Now: it retries with credentials and tells you exactly what the release service returned.

Source: [CHANGELOG 3.19.0](https://github.com/Richards-LLC/cassy/blob/v3.19.0/CHANGELOG.md), [PR #761](https://github.com/Richards-LLC/cassy/pull/761), and the [user/dev release draft](../release-notes/2026-09-08-v3.19.0-slack.md). These are shipped changes; the draft supplies the Was → Now wording, not publication proof.

## Fixes ledger

All 21 listed issues are closed. Titles below are shortened to describe the resulting behavior; linked issues preserve the original reports. Sorted by issue number. The theme column is the data behind the change map.

| Issue | What changed | Theme |
| ---: | --- | --- |
| [#705](https://github.com/Richards-LLC/cassy/issues/705) | Refused pushes leave a visible record | Factory |
| [#717](https://github.com/Richards-LLC/cassy/issues/717) | Explain project-registration conflicts | Cloud |
| [#718](https://github.com/Richards-LLC/cassy/issues/718) | Give code history its own repository setting | Diagnostics |
| [#719](https://github.com/Richards-LLC/cassy/issues/719) | Keep delivered messages visible until observed | Delivery |
| [#724](https://github.com/Richards-LLC/cassy/issues/724) | Document channel, edit and delete operations | MechaCassy |
| [#730](https://github.com/Richards-LLC/cassy/issues/730) | Apply the exact verified cleanup plan | Cloud |
| [#731](https://github.com/Richards-LLC/cassy/issues/731) | Verify downloaded image integrity | MechaCassy |
| [#732](https://github.com/Richards-LLC/cassy/issues/732) | Materialize accepted proposals locally | Cloud |
| [#734](https://github.com/Richards-LLC/cassy/issues/734) | Route messages to the owning supervisor | Delivery |
| [#737](https://github.com/Richards-LLC/cassy/issues/737) | Derive Codex worker paths from the host home | Diagnostics |
| [#738](https://github.com/Richards-LLC/cassy/issues/738) | Bound and paginate epic status | Diagnostics |
| [#739](https://github.com/Richards-LLC/cassy/issues/739) | Distinguish missing roots from uninitialized projects | Diagnostics |
| [#742](https://github.com/Richards-LLC/cassy/issues/742) | Confirm worker identity after context reset | Delivery |
| [#744](https://github.com/Richards-LLC/cassy/issues/744) | Refresh parked delivery anchors | Delivery |
| [#745](https://github.com/Richards-LLC/cassy/issues/745) | Expose upstream reachability diagnostics | Diagnostics |
| [#746](https://github.com/Richards-LLC/cassy/issues/746) | Preserve the focused epic as the spawn base | Factory |
| [#748](https://github.com/Richards-LLC/cassy/issues/748) | Resolve the declared close target | Factory |
| [#749](https://github.com/Richards-LLC/cassy/issues/749) | Resolve commit guards inside linked worktrees | Factory |
| [#750](https://github.com/Richards-LLC/cassy/issues/750) | Validate urgent assignment ownership | Delivery |
| [#751](https://github.com/Richards-LLC/cassy/issues/751) | Bound repeated delivery attempts | Delivery |
| [#752](https://github.com/Richards-LLC/cassy/issues/752) | Document read preflight and its approved fallback | MechaCassy |

## Install

Update an existing installation:

```bash
cas update
```

Or choose the archive for your platform. SHA-256 values below were computed from fresh downloads and matched the GitHub asset digests byte for byte.

### Linux x86_64

[cas-x86_64-unknown-linux-gnu.tar.gz](https://github.com/Richards-LLC/cassy/releases/download/v3.19.0/cas-x86_64-unknown-linux-gnu.tar.gz)

SHA-256: `4bb221b30252ffbf45cb164fbe9ac4c64b8e9f0709b53ce617c68072274c05d5`

### macOS ARM64

[cas-aarch64-apple-darwin.tar.gz](https://github.com/Richards-LLC/cassy/releases/download/v3.19.0/cas-aarch64-apple-darwin.tar.gz)

SHA-256: `0980b70998be147a7f8a7e109b94eba120e72f36ecc2506ff08ae62cc846c4eb`

[Open the v3.19.0 release](https://github.com/Richards-LLC/cassy/releases/tag/v3.19.0)

## Evidence and scope

This brief describes the published release relative to v3.18.1. It is an operator review artifact, not a new runtime release or proof of a Slack publication. No production performance benchmark or exhaustive state-coverage claim is made. The full gate turned green at 2026-09-08T19:56:53+00:00; publication followed 2970 seconds later. This includes the intervening merge and tag work, not only artifact upload time. The separate release-latency.receipt records 172 seconds from tag to publication.

Source priority: CHANGELOG 3.19.0, PR #761, the user/dev release draft, the 21 GitHub issue titles and closed timestamps, then published release assets. The release metadata and downloaded bytes are authoritative for publication and checksums. The consulted Slack source still contains draft placeholders; none were used for the install hashes.

Markdown source: [2026-09-08-v3.19.0.md](2026-09-08-v3.19.0.md). Source checkout: `258e46141365ea3a4337edba39ffcdb82663fb1d`. Figure and issue snapshot: 2026-09-08 20:58 UTC. Release window: 8 September 2026. Build and visual proof are retained with task cas-aaae. Timing receipts: `/home/pippenz/.cas/artifacts/release/v3.19.0-epic-92c0-merge/gate.green.epoch` and `release-published.receipt`; copies are retained with the report evidence.

Queries used:

```text
gh pr view 761 --repo Richards-LLC/cassy --json body,mergeCommit,mergedAt
gh issue list --repo Richards-LLC/cassy --state closed --limit 100 --json number,title,url,closedAt
gh release view v3.19.0 --repo Richards-LLC/cassy --json assets,publishedAt,url
gh release download v3.19.0 --repo Richards-LLC/cassy --dir .report-build --pattern '*.tar.gz'
sha256sum .report-build/*.tar.gz
```

The issue query was filtered to #705, #717, #718, #719, #724, #730, #731, #732, #734, #737, #738, #739, #742, #744, #745, #746, #748, #749, #750, #751 and #752. All 21 were present and closed.

The HTML is generated from this markdown with `python3 docs/release-reports/render-v3.19.0.py` from the repository root. The checked-in HTML opens directly; generation is only needed when updating the source. The PDF is rendered from that same HTML with Playwright Chromium `page.pdf`, A4 format, print backgrounds and 15 mm CSS page margins; Letter is separately checked.
