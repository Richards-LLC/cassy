# Brief: Cassy v3.19.0 release report

## Single idea
Cassy 3.19.0 makes the next step clearer across five issue-bearing surfaces, with the largest concentration in delivery; 21 closures substantiate that breadth.

## Hero form
Verdict hero plus a grouped unit-dot plot: six directly labelled surface rows, five filled with 21 equal-weight dots and an explicitly uncounted install improvement. Delivery's six dots make the largest cluster legible before the prose. The view preserves product-surface order across future editions; the sentence names the largest cluster. Equal areas deliberately avoid inventing impact measurements.

## Emotional register
Assured, humane, collected: sandstone paper, a large regular serif verdict, system sans reading text, mono folio numbers, quiet ruled evidence. This is an operator release summary with a product-capability lead; the task's explicit reusable section order takes precedence over the generic report matrix.

## Distinctive move
The “Cassy closure stitch”: issue dots form short indigo stitches across named product surfaces; the six-dot delivery stitch is the visual emphasis. A small six-point stitch returns beside the install command as the sole decorative echo, labelled decorative to assistive technology. Future releases keep the stitch, surface order, palette and typographic folio, while the closure counts change. No motion is needed.

## Deliberately omitted
No KPI cards before the argument, no logo band, gradients, fabricated before/after metrics or subjective severity bubbles. Four reader themes differ from the five issue themes: the former organize actions; the latter account for closures. Install is shown with zero listed issues rather than inventing a twenty-second dot. No network fonts or assets. Green-to-published timing uses the full-gate epoch and published receipt; it is not confused with the shorter tag-to-publication latency.

## Design inheritance and section forms
The requested `.claude/skills/design-spec/references/` path is absent in this checkout. Use tracked `docs/design/petrastella-design-language.md` and `docs/design/design-tokens.json`: the same Petrastella roles, system font stacks, spacing and warm-stone/light plus rose-brown/dark palette. Tokens retain their names. Marks use evidence/verdict semantic colors rather than categorical hues, so no categorical palette is introduced.

1. Hero: verdict plus unit-dot figure, with text alternative and an adjacent open data-table disclosure below the first screen.
2. Headline: ruled stat strip; explicit changelog counting method, no false trend.
3. What you can do now: alternating callout rail, before/after pairs, marginal-note row and one plain card grid, in the four Slack reader themes.
4. Under the hood: compact grouped before/after ledger prose.
5. Fixes: numbered evidence ledger, linked to every issue.
6. Install: command pull-quote with the stitch echo, then two ruled archive rows and full digests.
7. Provenance: quiet reading column with exact queries and limitations.

## Print and review plan
Generate A4 PDF with Chromium `page.pdf` from the HTML; also generate Letter proof. Print sections begin on fresh pages, tables repeat headings, rows avoid breaks, links print their destinations, and full hashes wrap. Render every PDF page back to PNG and inspect the page images. Strict visual QA covers 1280×800 and 390×844 in light and dark, with JS disabled parity and keyboard checks. All source words and data must survive rendering.

## Critique
Scored 2026-09-08 by wise-puma-38 after the final renders, with a blind second-reader review by release_critique before the final print refinements.

| Dimension | Author | Second reader | Evidence |
| --- | ---: | ---: | --- |
| Distinctiveness | 5 | 5 | Sandstone/serif verdict and closure stitches lead the screen; the indigo stitch returns once at install. |
| Fit to argument | 4 | 4 | Five populated rows and the six-dot delivery cluster show breadth and concentration. Clarity and safety are explained by the sourced Was → Now text, not measured by dot area. |
| Hierarchy | 5 | 5 | At 1280×800 the figure ends at y=536; at 390×844 it ends at y=650. Verdict and figure precede the stat strip in both schemes. |
| Craft | 4 | 4 | Strict visual QA: PASS, zero findings, zero allowlists. Final A4 and Letter are 10 pages each, with no content outside 40pt bounds. A compact print ledger accommodates full URLs; some section-ending white space is intentional. The initial wrapped PDF folio was fixed. |
| Accessibility | 5 | 4 | Light/dark and JS-off pass; 34 controls have visible keyboard focus, Enter toggles the disclosure, print reopens it. All 21 issue links and both hashes survive PDF output. Second reader sampled print; author inspected all-page contact sheets and checked page bounds. |

All required floors hold; no dimension is zero. The second-reader score remains the independent score, not an average or a retroactive rescore.

## Verification receipt

- `node scripts/visual-qa.mjs docs/release-reports/v3.19.0.html --strict --artifact-dir /home/pippenz/.cas/artifacts/cas-aaae/visual-qa` → exit 0, PASS; 1280×800 and 390×800, light and dark.
- Same command with `--viewport 390x844 --artifact-dir /home/pippenz/.cas/artifacts/cas-aaae/visual-qa-phone` → exit 0, PASS; exact requested phone size in both schemes.
- `node .report-build/proof.mjs` → exit 0, first-screen/offline/JS-off/21-row/focus checks PASS; Chromium `page.pdf` creates A4 and Letter from the same HTML after intentionally closing the map disclosure.
- `python3 .report-build/pdf-check.py` → exit 0; 10 pages per format, zero content-boundary findings, 57 links per format, 21 issue links preserved, both 64-character hashes intact, collapsed disclosure expanded for print. All pages rendered back to PNG and reviewed as contact sheets; full-size page samples inspected.
- `python3 .report-build/fidelity.py` → exit 0, PASS on 256 markdown fragments and every markdown link destination. Stat-strip column headings are presentation structure; every measure, value and definition is checked.
- `node .report-build/keyboard.mjs` → exit 0, PASS on 34 controls and the disclosure's Enter toggle.
- `python3 docs/release-reports/render-v3.19.0.py` regenerates the HTML using only the Python standard library. No runtime assets or build step are needed to open the committed HTML. Scratch verification commands above ran in the worktree; their durable result files are below.

Durable evidence root: `/home/pippenz/.cas/artifacts/cas-aaae/`.
Desktop: `desktop-light.png`, `desktop-dark.png`; phone: `phone-light.png`, `phone-dark.png`; PDF: `pdf-a4-01.png` through `pdf-a4-10.png`, `pdf-letter-01.png` through `pdf-letter-10.png`; overview: `pdf-a4-contact.png`, `pdf-letter-contact.png`.
Machine receipts: `browser-checks.json`, `pdf-checks.json`, `fidelity.json`, `keyboard-checks.json`, both visual-QA directories and `sources/sha256.txt`. The small visual receipt is committed as `v3.19.0.visual-qa.md`.

The cas-html-reports review checklist is satisfied: markdown fidelity; source priority and uncertainty; semantic landmarks/headings/tables; offline rendering; copyable commands/digests; direct SVG labels and text/data alternatives; token roles; reduced-motion safety (no motion); keyboard and JS-off; light/dark contrast; A4/Letter print; exact source URLs and timing provenance. No runtime behavior, built-in skill, MCP, hook, migration or CLI surface is changed. This is a report for review; Slack publication belongs to the supervisor's later workflow.
