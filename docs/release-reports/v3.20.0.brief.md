# Brief: Cassy v3.20.0 release report

## Single idea
Cassy 3.20.0 adds explicit evidence to work and release reporting, while two named issue closures repair idle daemon work and memory relevance.

## Hero form
Verdict plus a directly labelled unit-dot plot: QA 0, Release 0, Daemon 1, Memory 1. The equal-sized Daemon and Memory stitches tie for the largest closure group; no issue is invented to make the two capability themes look larger. The figure makes the distinction between new workflow and counted repair visible. This release introduces these four scope names in the assigned QA, Release, Daemon, Memory order; v3.19.0's different surfaces are not reclassified into a false trend.

## Emotional register
Assured, humane, collected: sandstone paper, regular serif verdict, system sans reading text, mono folio and quiet rules. There is no root DESIGN.md. Inherit the tracked docs/design/petrastella-design-language.md and docs/design/design-tokens.json, as v3.19.0 did. The token schema already matches the renderer, including color.light/dark roles and typography.family; pass the token file explicitly with no palette remapping or network fonts.

## Distinctive move
The closure stitch remains the identity: one equal-area dot in each of two named repair surfaces, with both tied largest groups in verdict indigo. Empty QA and Release rows are explicitly labelled zero, not represented by hollow impact bubbles. The sparse distribution argues for two precise repairs alongside broader capabilities. One decorative six-point stitch returns beside install.

## Deliberately omitted
No KPI cards before the argument, logo band, network assets, invented improvement percentages or severity bubbles. No fresh CPU/recall benchmarks or installed-binary proof are available from this report pass; runtime effects are attributed to the released changelog. GitHub asset metadata and the publication receipt establish matching digests, without claiming a fresh archive download. The shorter tag latency is not substituted for green-to-published timing.

## Section forms and source record

| Order | Section | Form / source |
| --- | --- | --- |
| Hero | Verdict + change map | Equal-dot figure; the two PR #770 issues directly verified CLOSED at 23:24 UTC, closure window 22:01:47–22:24:07 UTC |
| 1 | Release at a glance | Ruled stat strip; CHANGELOG 3.20.0 entry counts and green/publication endpoints |
| 2 | What you can do now | Grouped Was → Now themes from the publication draft; callout rail, pairs and marginal rows |
| 3 | Under the hood | Compact grouped Was → Now developer changes from the draft, changelog and skill contract |
| 4 | Fixes ledger | Linked two-row issue table with exact closure times |
| 5 | Install | Command pull-quote and asset rows; published asset URLs and full matching digests |
| 6 | Evidence and scope | Reading column; pinned source URLs, queries, retrieval time, receipt paths and limits |

## PDF plan
Commit A4; also prove Letter with Chromium. Use the skill's render.py and references/template.html. Supervisor authorized a single print rule, `.changes{orphans:1;widows:1}`, in all three builtin template mirrors: inline-block articles otherwise count as line boxes and default widow/orphan rules keep several articles together. Individual articles retain break-inside:avoid; no other skill file changed. Flow sections continuously, keep cards and table rows together and repeat table headers; no section-by-section forced pages. Expand disclosures for print, retain URL destinations and full digests, render every page to PNG and inspect for clipping, orphan headings and unexplained half-empty pages.

## Critique

Scored after final browser renders and all-page PDF review on 2026-09-08.

| Dimension | Score | Evidence |
| --- | ---: | --- |
| Distinctiveness | 5 | Sandstone/serif verdict, equal issue stitches and one decorative install echo preserve the v3.19.0 identity. |
| Fit to argument | 4 | Two populated repair rows and two explicitly zero capability rows account for the sourced release without inventing impact metrics. |
| Hierarchy | 5 | Entire figure ends at y442 on 1280×800 and y570 on 390×844, with the verdict before the stat strip in both schemes. |
| Craft | 4 | Strict QA has zero findings; A4 and Letter are five continuous pages, with all 14 articles intact and no orphan headings, clipped rows or half-empty pages. Final-page whitespace remains ordinary document flow. |
| Accessibility | 5 | Both schemes and JS-off retain all 123 source fragments; 18 controls have visible keyboard focus, Enter toggles map data and print reopens it. |

All required floors hold; no dimension is zero. Scores are the author's review, not an independent reader's rating.

## Verification receipt

- Renderer: `python3 cas-cli/src/builtins/skills/cas-release-report/scripts/render.py docs/release-reports/2026-09-08-v3.20.0.md --output docs/release-reports/v3.20.0.html --project-root . --tokens docs/design/design-tokens.json --emphasis 'Clearer proof' --footer 'Clearer proof. Quieter waiting. Relevant recall.'` → exit 0. The HTML is 30 KB, with inline CSS/SVG and no network dependency.
- Strict gate: `node scripts/visual-qa.mjs docs/release-reports/v3.20.0.html --strict --artifact-dir /home/pippenz/.cas/artifacts/cas-16a3/visual-qa` → exit 0, PASS at 1280×800 and 390×800, light/dark, zero findings and allowlists. Repeat with `--viewport 390x844 --artifact-dir /home/pippenz/.cas/artifacts/cas-16a3/visual-qa-phone` → exit 0, PASS in both schemes.
- Browser proof: `node .report-build/proof.mjs` → exit 0; all 123 Markdown fragments, all source links, 14 change articles and both ledger rows retained with JS disabled; zero network requests; 18 visibly focused keyboard controls; disclosure Enter and print reopening pass.
- PDF: skill recipe in `.report-build/render-pdf.mjs`, then `.report-build/check-pdf.py` for each format → exit 0. A4 and Letter both five pages, zero content outside 40pt printable bounds. Every final page rendered to PNG and inspected in contact sheets; install/provenance pages also read at full size. Table headers repeat; each archive and its full digest stay together.
- Fidelity: `python3 .report-build/pdf-fidelity.py` → exit 0; both formats preserve 123 fragments, every source URL and both full hashes. Additional PDF integrity check confirms each of 14 Was/Now articles stays on one page.
- Builtin mirror check: `scripts/run-scoped-tests.sh -p cas --test builtin_flavor_drift_test` → exit 0; 17 tests passed, including fresh bundle installation and execution for each harness. The only builtin change is the approved one-line print flow fix in each template mirror.

Tool versions: Node v24.19.0; Playwright 1.62.0-alpha-1783623505000; screen Chromium 152.0.7977.82; PDF Chromium 153.0.8010.12; PyMuPDF 1.27.2.3.

Durable proof root: `/home/pippenz/.cas/artifacts/cas-16a3/`. Receipts: `browser-proof.json`, `keyboard-controls.json`, `pdf-fidelity.json`, `pdf-integrity.json`, `pdf/{a4,letter}/receipt.json`, strict QA directories, `drift.log`/`drift.exit`, and `LEDGER.md`. Screenshots: `1280-{light,dark}.png`, `390-{light,dark}.png`, `keyboard.png`, `print-expanded.png`, `pdf/{a4,letter}/01.png` through `05.png`, and both contact sheets. Proof helpers are copied to this directory for repeatability.

The original six-page render and whitespace failure remain in `pdf/initial/`; they are not final proof. A temporary asset-table experiment was discarded because the generic renderer applies a change-map caption to generic tables; final asset paragraphs pair each URL with its digest. No content or required check was cut from the 30-minute task box. Archive download and runtime benchmarking were outside this report's scope, as stated in the source.

The cas-qa-craft ledger records six real-build cells, all PASS after revision. Announcement draft links are owned by the supervisor on the publication branch (notification 28056), so the report commit does not overwrite filled digests or posting receipts. The post-publication template refinement is carried forward; the published v3.20.0 tag is unchanged.
