# Cassy v3.20.0 report — visual QA PASS

Strict QA: PASS at 1280×800 and 390×844 in light and dark, plus 390×800; zero findings, zero allowlists. Author rubric: distinctiveness 5, fit 4, hierarchy 5, craft 4, accessibility 5. Verdict and full figure are above the fold (figure bottom 442px desktop / 570px phone).

HTML: 123 source fragments, all source links, 14 Was/Now articles and two issue rows retained, including JS-off. No network requests. Keyboard proof: 18 controls with visible focus, disclosure Enter toggle and print reopening.

PDF: A4 committed; Letter proofed. Five pages each; every page rendered to PNG and inspected, no clipped content or orphan headings, no half-empty pages. Bounds findings: 0. All 123 source fragments, issue URLs, install commands and full SHA-256 hashes survive; all 14 articles stay together. Continuous flow uses the supervisor-approved `.changes{orphans:1;widows:1}` print fix in all three template mirrors. Flavor-drift test: 17/17 PASS, exit 0.

Reproduction commands and versions: [concept brief](v3.20.0.brief.md). Source: [release Markdown](2026-09-08-v3.20.0.md). Outputs: [HTML](v3.20.0.html), [A4 PDF](v3.20.0.pdf).

Durable receipts: `/home/pippenz/.cas/artifacts/cas-16a3/` — `LEDGER.md`, `browser-proof.json`, `keyboard-controls.json`, `pdf-fidelity.json`, `pdf-integrity.json`, `drift.log`, `drift.exit`, `visual-qa/`, `visual-qa-phone/`, `pdf/a4/`, `pdf/letter/`. Original pagination failure retained under `pdf/initial/`; the final five-page files supersede it. Six demo cells: six real-build PASS, zero FAIL, zero NOT EXERCISED.

This review exercises the report, not a runtime benchmark or fresh archive installation. Published digests match the release receipt and GitHub asset metadata. The supervisor updates publication-draft links without rewriting the published tag.
