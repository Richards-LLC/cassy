# Brief: Cassy v3.21.0 release report

## Single idea

Cassy 3.21.0 makes an assembled epic testable as one product while repairing the two ways long sessions lose relevant context: delivery silence and noisy ranking.

## Hero form

Verdict plus a directly labelled unit-dot plot: QA 1, Memory 2, Release 0, Doctor 0. Memory is the largest closure group, so its two equal-area dots receive the verdict color. The single QA dot records the epic-flow reference closure; Release and Doctor remain labelled zero because their new capabilities have no separate counted closure. The figure is an accounting of named closures, not a proxy for effort or impact.

## Emotional register

Assured, humane, collected: sandstone paper, regular serif verdict, system sans reading text, mono folio, and quiet rules. There is no root `DESIGN.md`. The report inherits the tracked [Petrastella design language](../design/petrastella-design-language.md) and [design tokens](../design/design-tokens.json), which already match the renderer schema; `docs/design/design-tokens.json` is passed explicitly and no network fonts load.

## Distinctive move

The closure stitch stays the identity: two equal-area Memory dots form the decisive group, one QA dot records the release-linked closure, and empty Release and Doctor rows state their capability distinction rather than inventing impact. The four-row order mirrors this edition's product story: QA, Memory, Release, Doctor. One decorative six-point stitch returns beside Install.

## Deliberately omitted

No KPI cards before the argument, logo band, network assets, invented comparison metrics, severity bubbles, or claim of a fresh archive download. The publication receipt and GitHub asset metadata establish the two full digests, while the saved gate receipt establishes green-to-published timing. This report cannot support an installed-binary benchmark or production telemetry conclusion.

## Section forms and source record

| Order | Section | Form / source |
| --- | --- | --- |
| Hero | Verdict + change map | Equal-dot figure; closed issues #759, #763, and #764 queried at 2026-09-09 14:04 UTC |
| 1 | Release at a glance | Ruled stat strip; CHANGELOG 3.21.0 counts and gate/publication endpoints |
| 2 | What you can do now | Grouped Was → Now themes from the published release draft |
| 3 | Under the hood | Compact Was → Now ledger from the draft, changelog, and PRs #771–#773 |
| 4 | Fixes ledger | Linked three-row issue table with exact closure times |
| 5 | Install | Command pull-quote and asset rows; publication receipt and GitHub asset URLs/digests |
| 6 | Evidence and scope | Reading column; pinned commit, queries, retrieval time, receipts, and limits |

## PDF plan

Commit A4 and proof Letter with Chromium. Use the builtin `cas-release-report` `scripts/render.py` and `references/template.html`, continuous section flow, keep-together cards and rows, repeated table headings, expanded disclosures, printed link destinations, and complete digests. Render every page to PNG and inspect for clipping, orphan headings, split rows, missing disclosures, and unexplained half-empty pages. Record Node, Playwright, Chromium, and PyMuPDF versions with both page counts.

## Critique

Scores and evidence are recorded after the final browser and PDF passes below.

| Dimension | Score | Evidence |
| --- | ---: | --- |
| Distinctiveness | 5 | Sandstone/serif verdict, equal closure stitches, labelled zero rows, and the install stitch preserve Cassy's identity. |
| Fit to argument | 5 | The two-dot Memory group and single QA dot show the sourced closure distribution without turning capability rows into fake closures. |
| Hierarchy | 5 | The final strict visual pass proves the verdict and entire figure above fold at both requested viewports and both schemes. |
| Craft | 4 | Continuous A4 and Letter pagination, bounds checks, and all-page PNG inspection pass; ordinary final-page whitespace remains. |
| Accessibility | 5 | Light/dark, JavaScript-disabled, keyboard disclosure, SVG description, adjacent data table, and offline-link checks pass. |

All required floors hold; no dimension is zero. Scores are the worker's review against the brief rubric, not an independent reader's rating.

## Verification receipt

- Renderer: `python3 cas-cli/src/builtins/skills/cas-release-report/scripts/render.py docs/release-reports/2026-09-09-v3.21.0.md --output docs/release-reports/v3.21.0.html --project-root . --tokens docs/design/design-tokens.json --emphasis 'One epic walk' --footer 'One epic walk. Two memory repairs. Three closures.'` → exit 0. The HTML is standalone with inline CSS/SVG and no network dependency.
- Strict gate: `node scripts/visual-qa.mjs docs/release-reports/v3.21.0.html --strict ...` → exit 0, PASS at 1280×800 and 390×844 in light and dark, zero findings and allowlists.
- Browser proof: the final proof script → exit 0; all Markdown fragments, source URLs, issue rows, JS-off content, keyboard disclosure, and zero network requests are retained.
- PDF: the `render-pdf.mjs` and `check-pdf.py` helpers from the skill recipe → exit 0 for A4 and Letter; every page is rendered to PNG and inspected, with zero bounds findings and printed link destinations.
- Durable proof root: `/home/pippenz/.cas/artifacts/cas-a3cf/`; `LEDGER.md` records the real-build demo matrix, captures, labels, and honest limitations.

The announcement draft remains supervisor-owned for report/PDF links and authorized Slack posting. The saved publication receipt supplies both final asset digests.
