# Visual QA receipt: Cassy v3.22.0

Build: standalone `docs/release-reports/v3.22.0.html` and adjacent Markdown at commit `c2db094c1993ba032b7727c16c4ae4da5ddd1d88`.

## Strict visual QA

Both commands exited 0 and printed `PASS` with zero findings, zero informational findings, and zero allowlisted findings:

```text
npm exec --yes --package=playwright -- node scripts/visual-qa.mjs --strict --viewport 1280x800 --artifact-dir /home/pippenz/.cas/artifacts/cas-1bda/visual-qa-desktop file:///home/pippenz/Petrastella/cas-src/.cas/worktrees/noble-hound-88/docs/release-reports/v3.22.0.html
PASS

npm exec --yes --package=playwright -- node scripts/visual-qa.mjs --strict --viewport 390x844 --artifact-dir /home/pippenz/.cas/artifacts/cas-1bda/visual-qa-phone file:///home/pippenz/Petrastella/cas-src/.cas/worktrees/noble-hound-88/docs/release-reports/v3.22.0.html
PASS
```

The strict runner covered light and dark schemes at both 1280×800 and 390×844. It checked effective text contrast, clipping, overlap, visibility, viewport escape, fixed-size truncation, print text retention, and JavaScript-disabled content. Receipts: `/home/pippenz/.cas/artifacts/cas-1bda/visual-qa-desktop/visual-qa.md` and `/home/pippenz/.cas/artifacts/cas-1bda/visual-qa-phone/visual-qa.md`.

## Demo matrix

The seven-cell `cas-qa-craft` pass used the real local HTML/Markdown build, with one screenshot per cell. All seven cells passed:

| ID | Condition | Result | Evidence |
| --- | --- | --- | --- |
| M01 | 1280×800 light happy path | Verdict, one-dot map, and Release at a glance are visible above the fold. | `/home/pippenz/.cas/artifacts/cas-1bda/qa/M01-desktop-light.png` |
| M02 | 390×844 light phone | Verdict and map fit without horizontal overflow; lower content remains scrollable. | `/home/pippenz/.cas/artifacts/cas-1bda/qa/M02-phone-light.png` |
| M03 | 1280×800 dark revisit | Hierarchy, labels, and closure dot remain legible. | `/home/pippenz/.cas/artifacts/cas-1bda/qa/M03-desktop-dark.png` |
| M04 | 390×844 dark phone | Phone layout remains readable with no clipped hero content. | `/home/pippenz/.cas/artifacts/cas-1bda/qa/M04-phone-dark.png` |
| M05 | Keyboard tabs with JavaScript disabled | Content remains present and keyboard focus is visible on the source link. | `/home/pippenz/.cas/artifacts/cas-1bda/qa/M05-keyboard-js-off.png` |
| M06 | Second visit, phone, Evidence and scope | Source-fidelity section and published digests are reachable and readable. | `/home/pippenz/.cas/artifacts/cas-1bda/qa/M06-revisit-evidence.png` |
| M07 | Adjacent Markdown source offline | Source link resolves to the same report content with no network dependency. | `/home/pippenz/.cas/artifacts/cas-1bda/qa/M07-adjacent-markdown.png` |

No telemetry sweep is configured. No feature thresholds or retry constants were found in the touched report/renderer surface. No contradictory terminal claims were observed.

## PDF evidence

The chosen A4 PDF and review Letter PDF each have 6 pages. The bounds checker exited 0 for both with `bounds_findings: []`; the URI extraction preserved report, issue, release, asset, workflow, and source links. Page PNGs `01.png`–`06.png` under each format directory were inspected. The committed PDF SHA-256 is `9ac95e8816c30ef2b08fc22a391aa0423c6214629fa3ab7e818babcc3adcf9e9`, equal to the checked A4 artifact.

Intentional limitations: archives were not redownloaded, installed binaries were not rerun, and no production telemetry claim is made.
