---
version: 3.24.0
date: 2026-09-10
themes: ["Safer coordination", "Clearer recovery", "Factory", "Protected execution", "Lifecycle contracts", "Verification", "Runtime recovery", "Execution and proof"]
---

# cas-src v3.24.0

Published 10 September 2026 · 17:15 UTC

Work completes more safely, predictably, and with clearer recovery.

## Change map

The closure map is empty until release evidence names verified issues.

One filled dot represents one closed issue. Dots have equal weight: this is a count of closures, not a measured impact score.

| Surface | Issues closed | Issue numbers |
| --- | ---: | --- |
| Safer coordination | 0 | No listed issues |
| Clearer recovery | 0 | No listed issues |
| Factory | 0 | No listed issues |
| Protected execution | 0 | No listed issues |
| Lifecycle contracts | 0 | No listed issues |
| Verification | 0 | No listed issues |
| Runtime recovery | 0 | No listed issues |
| Execution and proof | 0 | No listed issues |
| Total | 0 | 0 issue-bearing themes |

Source: CHANGELOG `/home/pippenz/Petrastella/cas-src/.cas/epic-ac2c-merge/CHANGELOG.md`, release-notes `/home/pippenz/Petrastella/cas-src/.cas/epic-ac2c-merge/docs/release-notes/2026-09-10-v3.24.0-slack.md`, GitHub `Richards-LLC/cassy` release v3.24.0; retrieved 2026-09-10T17:18:10Z.

## Release at a glance

| Measure | Value | Definition |
| --- | ---: | --- |
| Issues closed | 0 | Named, verified closed issues in this report |
| Features added | 3 | Top-level Added entries in the CHANGELOG section |
| Changed entries | 8 | Top-level Changed entries in the CHANGELOG section |
| Fix entries | 2 | Top-level Fixed entries in the CHANGELOG section |
| Green to published | 36m 54s | gate.green.epoch to the published receipt, when available |

## What you can do now

### Safer coordination

#### Clear identities

Was: one exact identifier was required.

Now: start, claim, and close accept a name or ID.

#### Supported messages

Was: coordination could use the wrong transport.

Now: messages stay on the supported path.

#### Gate ownership

Was: only one owner could finish a gate.

Now: authorized reviewers can finish it and get a clear ownership warning.

#### Bounded completion

Was: large completion checks could wait too long.

Now: checks finish within a limit and safely show unfinished work.

#### Retryable proof

Was: a close retry could lose approved evidence.

Now: the approved evidence is reused when the proof is still valid.

#### Merged results

Was: proof could point at an old result after a merge.

Now: it follows the result that was actually published.

### Clearer recovery

#### Credential settings

Was: variable placeholders could fail silently.

Now: values and defaults expand, and missing required settings explain the fix.

#### Hub recovery

Was: a stuck hub could block a restart.

Now: startup is transactional, forced recovery restores service, and health checks explain the remedy.

#### Fresh notices

Was: old assignment notices could reappear.

Now: they disappear once the work has started or finished.

### Protected execution

#### Profile write-through

Was: credentials in linked profiles did not reliably follow the profile.

Now: configured credentials and linked profile updates stay in sync.

#### Whole-process stopping

Was: stopping a service could leave its children running.

Now: the complete process group is stopped and checked.

#### Accurate proof targets

Was: test data could point proof at the wrong target.

Now: declarations identify the target precisely.

#### Oversized entries

Was: oversized history entries could remain quarantined.

Now: they are shortened, retried, and recorded as recovered.

#### Protected image credentials

Was: image-service keys could appear in command details.

Now: the key is sent through a protected temporary header file.


## Under the hood

### Lifecycle contracts

#### Assignee identities (#799)

Was: lifecycle calls required one identity form.

Now: UUID and name resolve to the same assignee. Commit `42ae8311`.

#### Message routing (#793)

Was: supervisors could use the host SendMessage transport.

Now: supervisor coordination uses the CAS message path. Commit `86db4c81`.

#### Gate ownership (#800)

Was: worker-assigned gates could not be closed by the supervisor.

Now: supervisor closure is supported and cross-owner assignment warns. Commit `c34f25e3`.

#### Epic close (#802)

Was: close evaluated children with unbounded repeated probes.

Now: batched probes are bounded and partial evaluation fails closed. Commit `d53e32d0`.

#### Close retry (#755)

Was: retrying could mint a new dispatch and orphan an approved verdict.

Now: superseded dispatches preserve the approved result. Commit `9d7e1b49`.

#### Post-merge proof (#753)

Was: proof could bind to a stale worker checkout.

Now: post-merge verification binds to the published target head. Commit `9d7e1b49`.

### Runtime recovery

#### Proxy variables (#803)

Was: proxy configuration treated `${VAR}` as literal text.

Now: `${VAR}` and `${VAR:-default}` expand, with `missing_credential_env` for required gaps. Commit `0a16d12e`.

#### Hub startup (#804)

Was: a lock holder could strand startup between auth, bind, and recording.

Now: startup is transactional and `--force` recovers wedged holders with a doctor remedy. Commit `219a033b`.

#### Assignment delivery (#756)

Was: delivered assignment boilerplate could replay after state changed.

Now: direct, queued, MCP, and hook paths revalidate task state. Commit `c43b112e`.

#### Credential profiles (#758)

Was: isolated processes missed credentials from configured files and linked profiles.

Now: credential discovery and symlink write-through are preserved. Commit `87614f4a`.

### Execution and proof

#### Process groups (#796)

Was: stopping a registered PID could leave nested watchers alive.

Now: each service has a dedicated group and stop verifies no survivors. Commit `fc40166a`.

#### Scoped proof (#778)

Was: source-path strings could claim integration-test ownership.

Now: exact targets and module declarations determine ownership. Commit `67733a3b`.

#### Embedding recovery (#805)

Was: provider-size failures quarantined units permanently.

Now: oversized units are truncated, retried, and marked with a recovery receipt. Commit `d386dc56`.

#### Image credentials (#757)

Was: the API key appeared in curl arguments.

Now: a mode-600 header file carries the key without exposing it in argv. Commit `5dabbdcc`.


## Fixes ledger

| Issue | State | Closed | Theme |
| --- | --- | --- | --- |

## Install

```bash
cas update  # install v3.24.0 through the project's normal release channel
```

| Asset | Digest | Size |
| --- | --- | ---: |
| cas-aarch64-apple-darwin.tar.gz | sha256:a74305f1d2cf4ace39d769eddab861d7932cd410b7e3f6a2945cbd82bad68c0f | 26458193 |
| cas-x86_64-unknown-linux-gnu.tar.gz | sha256:117d688a6d0274d7132a20546c1ee8f848f2c9369337ea62b30f1074bfcb8f14 | 29442306 |

## Evidence and scope

Retrieved: 2026-09-10T17:18:10Z.

Source: CHANGELOG `/home/pippenz/Petrastella/cas-src/.cas/epic-ac2c-merge/CHANGELOG.md`, release-notes `/home/pippenz/Petrastella/cas-src/.cas/epic-ac2c-merge/docs/release-notes/2026-09-10-v3.24.0-slack.md`, GitHub `Richards-LLC/cassy` release v3.24.0; retrieved 2026-09-10T17:18:10Z.

GitHub issue theme assignment uses labels first, then the keyword table in the CLI. Any issue classified as `Unclassified` is shown in the map so a human can correct the Markdown before rendering.

Release-notes draft: `docs/release-notes/2026-09-10-v3.24.0-slack.md`.

CHANGELOG source: `CHANGELOG.md`.

GitHub release: https://github.com/Richards-LLC/cassy/releases/tag/v3.24.0 (published or unknown).

Release receipt: `/home/pippenz/.cas/artifacts/release/v3.24.0-epic-ac2c-merge/release-published.receipt`.

Source warnings:

- GitHub issues: referenced closures unavailable or not closed: #753, #755, #756, #757, #758, #778, #793, #796, #799, #800, #802, #803, #804, #805
