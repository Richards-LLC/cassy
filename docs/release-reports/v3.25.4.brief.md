# Brief: Cassy v3.25.4 release report

## Single idea

Seven verified issue closures concentrate in delivery verification, while clearer conversations and recovery instructions extend beyond the closure count.

## Hero form

Verdict plus a directly labelled equal-area unit-dot plot: Checks 2, Release 1, Factory 4, Hub 0. Checks abbreviates the previous edition’s Checks and notices label to prevent SVG label/dot overlap. Preserve the previous edition's first three surface positions and append Hub as an explicitly uncounted improvement. Factory's four dots lead the distribution; dots measure verified completed closures, never impact, effort or severity. The adjacent open table and linked ledger reconcile to the same seven unique issues.

## Emotional register

Assured, humane, collected. Use the project `docs/design/design-tokens.json` directly: warm stone background, sandstone hero, indigo verdict, serif display, system sans reading text and mono folio. No root DESIGN.md exists; the Hub design describes a separate application surface. Light and dark roles map directly to the generic renderer schema without overrides or network fonts.

## Distinctive move

The closure stitch groups seven equal issue dots into four named surfaces. Four verification closures form the largest stitch; Hub's zero stays labelled so an improvement cannot be mistaken for a missing source. One decorative six-point stitch returns at Install. The verdict names the concentration without claiming the most important work is the most numerous.

## Deliberately omitted

No KPI cards before the argument, logo band, network assets, invented trend or severity bubbles. Open issues 832, 834, 838, 842, 847, 850, 854 and 856 earn no dots, even where source commits name them. Issue 843 was closed as not planned and 829 has no delivered fix in this tag diff, so neither counts as a release fix. Published assets and hosted install proof do not establish a refreshed local host or new Slack/report upload integrity.

## Section forms and source record

| Order | Section | Form / source |
| --- | --- | --- |
| Hero | Verdict + change map | Equal-area closure stitches; GitHub closed-completed issues intersected with delivered commit references in v3.25.3..v3.25.4 |
| 1 | Release at a glance | Ruled stat strip; seven closures and CHANGELOG Added 1 / Changed 4 / Fixed 8; timing endpoints explicitly separate |
| 2 | What you can do now | Grouped Was → Now; all 13 User draft changes |
| 3 | Under the hood | Compact Was → Now ledger; all 16 Dev draft changes |
| 4 | Fixes ledger | Seven linked rows: 821, 825, 840, 844, 845, 848, 855; closed timestamps from authenticated GitHub API |
| 5 | Install | Command pull-quote and exact published asset URLs/hashes; no local-host execution claim |
| 6 | Evidence and scope | Reading column; source tag/commit, full gate, publication/download/latency receipts, query and unresolved limitations |

Closure window: strictly after v3.25.3 publication at 2026-09-11T04:25:37Z through v3.25.4 publication at 2026-09-11T17:07:51Z, inclusive of the end. Query `gh issue list --repo Richards-LLC/cassy --state closed --search 'closed:2026-09-11' --limit 100 --json number,title,closedAt,url,state,stateReason`, followed by individual `gh api repos/Richards-LLC/cassy/issues/<number>` and source-reference reconciliation. Snapshot and retrieval time: `/home/pippenz/.cas/artifacts/cas-a412/issues.json`. No tracking-state writes.

## PDF plan

Commit A4; verify Letter separately. Use the unchanged generic renderer and continuous print flow, keeping changes and rows together rather than forcing section pages. Render every page to PNG, inspect flow and bounds, and compare complete words, issue URLs, commands and both 64-character digests against Markdown. Large evidence stays under `/home/pippenz/.cas/artifacts/cas-a412/`; disposable build helpers stay in `.report-build/`.

## Critique

| Dimension | Score | Evidence |
| --- | --- | --- |
| Distinctiveness | 5 | The sandstone serif verdict and directly labelled closure stitch occupy the first screen; the six-point stitch recurs once at Install. |
| Fit to argument | 4 | Factory's four of seven equal dots visibly dominate; the zero-labelled Hub row distinguishes shipped improvements from verified closures without assigning importance. |
| Hierarchy | 5 | One verdict leads the map above the first rule; the full verdict and map fit at both requested sizes before the smaller evidence sections. |
| Craft | 4 | Strict visual QA: PASS · 0 findings · 0 informational · 0 allowlisted; continuous print flow has seven pages per format, repeated table headers and no split change rows. The entire A4 map table fits page 1; Letter keeps caption/header with four surface rows on page 1, then repeats the header with Total on page 2. |
| Accessibility | 5 | Both schemes and widths pass contrast/geometry checks; all 33 keyboard controls show focus, disclosure toggles with Enter, JavaScript-off content is identical, and both print formats retain all source fragments and URLs. |

Scored by clever-dragon-66 on 2026-09-11 after opening all four first-screen views and all fourteen full-page PDF PNGs. Distinctiveness, fit and hierarchy meet the public-surface floor; no mechanical defect remains. Supervisor separately inspected the mobile-light/desktop-dark hero and final-page contact sheets; these scores are the worker's assessment, not an invented second-review score.

Initial manual inspection caught an SVG label/dot collision missed by the generic strict checker and an almost empty eighth Letter page. Shortening the source verdict, using the documented Checks alias and condensing duplicate provenance resolved them without renderer or font changes. The final browser proof adds a label/dot bounding-box assertion. Supervisor subsequently found an orphan map-table header on Letter page 1; the earlier craft4/no-defect assessment was wrong and is superseded. Moving the two intact gate/timing paragraphs after the table in its reading guide kept headers with data without losing a source fragment. A table-flow regression rejects the preserved old PDF and passes both corrected formats; all fourteen pages were reopened before this rescore. Historical observations and a preserved subsequent eight-page attempt live under `artifacts/cas-a412/attempts/`; the original first-attempt bytes were overwritten during iteration and are not claimed retained.

## Verification receipt

Final proof is explicitly isolated at `/home/pippenz/.cas/artifacts/cas-a412/final/`: browser and fidelity exits are both 0. Node v24.19.0, Playwright 1.63.0, Chromium 153.0.8010.12 and PyMuPDF 1.27.2.3 produced strict light/dark captures at 1280×800 and 390×844, JS-off comparisons, keyboard checks, A4/Letter PDFs, every-page PNGs and fidelity receipts. No external requests were made. All fourteen final full-page PNGs were reopened after the source-order correction; their hashes are bound in `final/table-flow-review.json`. Pages 3–7 in both formats are also byte-identical to the previous reviewed version.

All 189 semantic Markdown fragments survive in HTML and both PDFs; the renderer transforms the glance table's three structural headings into a stat strip. All 29 Was → Now articles, seven issue dots, 28 distinct source URLs, the install command and both complete asset digests survive; each PDF has 60 link annotations and zero bounds findings. Each PDF has seven pages; final-page fills are A4 0.56 and Letter 0.98. Desktop verdict/map bottoms are 426.03/393.44px of 800px; phone bottoms are 313.95/545.16px of 844px.

The adjacent `v3.25.4.visual-qa.md` binds exact Markdown, HTML and both PDF hashes to these receipts. A4 is committed; Letter and large evidence remain private artifacts. Local report QA does not establish uploaded-byte integrity, refreshed local hosts or Slack publication. No new upload or POSTED receipt exists.
