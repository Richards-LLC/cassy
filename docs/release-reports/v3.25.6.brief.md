# Brief: Cassy v3.25.6 release report

## Single idea

Eight top-level changelog entries shipped, while GitHub recorded zero completed issue closures backed by this source in the publication window.

## Hero form

Verdict plus the directly labelled closure map, preserving the latest published surface order: Checks, Release, Factory, Hub. All four counts are zero, so the figure draws no issue dots; its text and adjacent table state the result. The measure is completed closures with delivered source, not shipped work, importance, severity, or impact.

## Emotional register

Assured, humane, collected. The renderer reads `docs/design/design-tokens.json` directly. There is no root `DESIGN.md`; `docs/design/petrastella-design-language.md` informs the project palette and typography. The mapped token source supplies warm stone and sandstone surfaces, an indigo verdict, local serif display, system sans reading text, and mono folio without remote fonts.

## Distinctive move

Keep the established closure stitch but show four explicitly labelled zero rows without decorative issue dots. The argument remains legible: improvements shipped, but no completed issue closure with delivered source is supported by the current window. The small six-point stitch returns at Install as decoration only; it is not a data mark.

## Deliberately omitted

No positive closure count inferred from changelog or task references, no guessed issue-to-surface assignments, no severity bubbles, and no invented comparison trend. #854 remains open and cursor issue #869 plus commit `e9ae7c89` are excluded. No claim of a local host refresh, manual consumer-GUI/Gatekeeper validation, uploaded PDF, or Slack publication.

## Section forms and source record

| Order | Section | Form / source |
| --- | --- | --- |
| Hero | Verdict + change map | Zero-count closure map; exact `closed_at` window joined to delivered source in `v3.25.5...a87153be` |
| 1 | Release at a glance | Ruled stat strip; GitHub closure count plus CHANGELOG Added 3 / Changed 2 / Fixed 3; distinct gate-green and tag-publication endpoints |
| 2 | What you can do now | Grouped Was → Now; all three User-thread changes in the release draft |
| 3 | Under the hood | Compact Was → Now ledger; all six Dev-thread changes in the release draft |
| 4 | Fixes ledger | Header-only table because no issue qualifies as a completed delivered closure |
| 5 | Install | Update command and exact published asset URLs/full SHA-256 digests |
| 6 | Evidence and scope | Source commit, release and install runs, receipts, exact issue window and limitations |

Issue search: [GitHub closed-issue search](https://github.com/Richards-LLC/cassy/issues?q=is%3Aissue+is%3Aclosed+closed%3A2026-09-11..2026-09-15), retrieved 2026-09-15 around 13:36 UTC. Exact window: strictly after `2026-09-11T22:36:34Z` (v3.25.5 publication), through `2026-09-15T13:21:02Z` inclusive. The only non-PR issue closed inside it was #870 (`not_planned`), with no delivered source reference; completed closure count is zero. The 14 release-referenced issue candidates queried were open. Full gate receipt has status 0 and exact source SHA `f0a4292`; its tree matches published commit `a87153be`. The `gate.green.epoch` receipt is `12:46:50Z`, yielding 34m12s gate-green→published. Tag-push→published is separately 144s.

## PDF plan

Commit the A4 PDF and verify Letter separately. Render every page of each format to PNG under `/home/pippenz/.cas/artifacts/cas-726c/`; inspect each page for clipping, orphan headings, split rows, missing text/links and half-empty pagination. Verify every Markdown fragment, install command, public URL, and both full digests. Keep Browser/Playwright scratch and `TMPDIR` on disk in that task artifact tree; do not use `/tmp`.

## Critique

| Dimension | Score | Evidence |
| --- | ---: | --- |
| Distinctiveness | 4 | The sandstone serif verdict and four-row zero closure stitch preserve this report series’ identity; the empty map is the memorable, evidence-led move. |
| Fit to argument | 4 | Every surface is explicitly zero and the adjacent explanation distinguishes shipped changes from completed closures, with no invented dots. |
| Hierarchy | 4 | At 1280×800 the verdict ends at y=535 and figure at y=488; at 390×844 they end at y=399 and y=673, so both lead within the first screen. |
| Craft | 4 | Strict visual QA PASS (0 findings); every A4 and Letter page was reviewed, with 5 pages each, repeated table headers, no bounds findings and all required links/digests retained. |
| Accessibility | 4 | Both schemes pass contrast/clipping checks, the first two keyboard stops have visible 3px focus, JS-off text is identical, and print preserves report content; PDF is untagged and screen-reader PDF behavior remains untested. |

## Verification receipt

The strict `scripts/visual-qa.mjs` runner completed against the standalone HTML in light and dark at 1280×800 and 390×844: PASS, zero findings, zero informational findings, zero allowlisted findings, four screenshots. It checked contrast, overlap, clipping, viewport escape, JavaScript-disabled content, and print text. Separate first-screen captures record the headline and complete map above the fold in both sizes and schemes. Keyboard Tab reaches the visible “Skip to release details” link and then the map disclosure, each with a visible 3px outline. JavaScript-off text matches exactly (7,594 characters). Print text is 7,549 characters because the screen-only skip link is hidden; report content remains present.

PDF rendering used Node v24.19.0, Playwright 1.63.0, Chromium 153.0.8010.36, and PyMuPDF 1.27.2.3. The committed A4 and review-only Letter PDFs each have five pages. All ten page PNGs were opened and inspected; repeated headers are intact, no change rows split, no orphaned headings or clipping appeared, and no page was half-empty. Bounds checks found zero issues. Both PDFs have 33 URI annotations and retain every required URL, install command, evidence fragment, and both full SHA-256 values (line-wrap-normalized text extraction). A4 is `docs/release-reports/v3.25.6.pdf`; Letter, page PNGs, per-format receipts, browser captures, and JSON QA receipts are under `/home/pippenz/.cas/artifacts/cas-726c/`. The generated PDF is not tagged and was not tested with a PDF screen reader.
