# Brief: Cassy v3.25.7 release report

## Single idea

The focused terminal cursor now follows the hosted pane, and rolling integration keeps observing matching merges across UTC midnight; both fixes shipped in v3.25.7.

## Hero form

Verdict plus the established closure map, retaining the four top-level surfaces while splitting Factory into its two shipped areas. The two completed factory issue closures sit on separate directly labelled rows; Checks, Release, and Hub remain explicit zero rows. This counts GitHub closures whose delivered fix is in the published source, not changelog bullets or the total amount of release work.

## Emotional register

Assured, humane, collected. Use the report series' Petrastella warm-stone surfaces, sandstone hero, indigo verdict, local serif display, system sans reading text, and mono evidence. The renderer reads `docs/design/design-tokens.json`; do not load remote fonts or assets.

## Distinctive move

Keep the familiar closure stitch and put the cursor and rolling-integration closures on separate labelled Factory subrows, then explain their distinct operator outcomes. The count is small enough to audit directly in the adjacent ledger.

## Deliberately omitted

No claim of authenticated Codex or Claude interaction: the cursor report is bounded to a rebuilt Cassy interface and deterministic local child. No claim that a host-wide multi-project refresh completed; its receipt records unrelated cloud registration failures after the binary update. No PDF upload or Slack publication. Do not carry the v3.25.6 POSTED receipts forward as v3.25.7 proof.

## Section forms and source record

| Order | Section | Form / source |
| --- | --- | --- |
| Hero | Verdict + change map | Five-row map retaining four top-level surfaces with the Factory row split by verified product area; exact `closed_at` window joined to fixes present in published tag |
| 1 | Release at a glance | Ruled stat strip; issue records, two top-level Fixed entries, tag/publication receipt, exact-SHA install workflow |
| 2 | What you can do now | Two compact Was → Now narratives: focused cursor; UTC rollover-safe checks |
| 3 | Under the hood | Three Was → Now changes: hosted cursor state/geometry; session-scoped dated-log reading; authenticated, bounded supervisor recovery through the real sweep |
| 4 | Fixes ledger | Exact #869/#879 close times, source commits, and public issue links |
| 5 | Install | Update command and published platform archives with full verified SHA-256 values |
| 6 | Evidence and scope | Published tag/source, gate and install receipts, exact issue window, proof limitations |

The closure interval is strictly after v3.25.6 publication at `2026-09-15T13:21:02Z` and inclusive of v3.25.7 publication at `2026-09-15T17:13:20Z`. GitHub's closed-issue query for 2026-09-15 returned #869 at `16:52:27Z` and #879 at `16:52:28Z`, and no other issue in that interval. Both source fixes are ancestors of the published tag. The query was refreshed at approximately `17:22Z` on publication day. The changelog has two Fixed entries; that count is reported independently, not used as a proxy for closures.

Cursor source: [e9ae7c89](https://github.com/Richards-LLC/cassy/commit/e9ae7c89) plus final regression [4bf2cd5b](https://github.com/Richards-LLC/cassy/commit/4bf2cd5b); hosted coordinate validation used a rebuilt Cassy binary and deterministic local child, not authenticated Codex/Claude. Rollover source: [6d45c315](https://github.com/Richards-LLC/cassy/commit/6d45c315). Its hidden one-shot `cas factory integration-recover` path authorizes only a live registered supervisor bound to the active factory session, project, and focused epic; it runs the real sweep in a fresh process, bounds it by four times configured sweep timeout and four hours, and reports success only for a passed receipt. Published tree: [`d14d575e1e4b9071136280d3b37fa40f34e9a001`](https://github.com/Richards-LLC/cassy/commit/d14d575e1e4b9071136280d3b37fa40f34e9a001).

## PDF plan

Commit A4 and render Letter as review evidence. Render every page of each format to PNG beneath `/home/pippenz/.cas/artifacts/cas-69384/`; inspect each page for clipping, orphan headings, split rows, missing links/digests, and unexplained half-empty pages. Check extracted text and URI annotations against Markdown, both full archive digests, and the copyable install command. The standalone HTML preserves the scope note as a separate paragraph and has a narrow report-local print override for provenance/footer flow; it preserves the source wording and footer links. Final page images and receipts: `render/final4-A4/` and `render/final4-Letter/`. Keep browser scratch and receipts in that artifact tree.

## Critique

| Dimension | Score | Evidence |
| --- | ---: | --- |
| Distinctiveness | 4 | Two directly labelled Factory subrows put one auditable closure mark on each shipped area within the established closure stitch. |
| Fit to argument | 4 | The two public issue records map to separate fixes in the published tree; the separate two-entry changelog count is not substituted for closure evidence. |
| Hierarchy | 4 | Outcome-first, 13-word verdict leads; the map immediately quantifies the two verified closures without suggesting it measures all shipped work. |
| Craft | 4 | Strict responsive checks and all eight A4/Letter pages pass; final evidence tables and footer links fit without an orphan footer or half-empty page. |
| Accessibility | 4 | Strict contrast/overflow checks pass in both schemes; keyboard skip/map/disclosure focus and activation pass; JavaScript-off text is identical. The PDF remains untagged and screen-reader behavior is untested. |

## Verification receipt

PASS. `scripts/visual-qa.mjs --strict` passed at 1280×800 and 390×844 in light and dark, 0 findings/informational/allowlisted. Keyboard Tab reached the skip link, map link, and disclosure with visible 3px outlines; Enter closed and reopened the disclosure. JavaScript-on/off body text matched exactly; print text retained `cas update`, both complete SHA-256 values, and scope limitations; no HTTP requests occurred. Both PDFs were regenerated with Node 24.19.0, Playwright 1.63.0, Chromium 153.0.8010.12 and PyMuPDF 1.27.2.3; A4 and Letter each have 4 pages, 0 bounds findings, and every page was inspected. PDF receipts: `/home/pippenz/.cas/artifacts/cas-69384/render/final4-A4/receipt.json` and `final4-Letter/receipt.json`; page captures are in the corresponding directories. A4 URI annotations: 37; Letter: 38. The count difference is format-dependent wrapping; each format contains all 13 unique source URLs from Markdown, with no missing URL. No orphan footer, clipped content, split evidence row, or half-empty page remains. A4 SHA-256 `ca417e6ad30f13b558f1d5e57ed9b3a0dcc774fd1a1ce8768f0681453f47a0a9`; review-only Letter SHA-256 `c5c5308dcf3ef22b56b5b9a03312f418c54a75feda53ada4a25cc7e172a4c68f`. The final responsive run receipts are under `/home/pippenz/.cas/artifacts/cas-69384/visual-final2-desktop/` and `visual-final2-phone/`. The PDF is untagged; screen-reader behavior remains untested.
