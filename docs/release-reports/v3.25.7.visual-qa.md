# Cassy v3.25.7 report — visual QA

**Result: PASS** — standalone HTML and A4/Letter PDFs checked after the minimal pagination correction.

- Strict visual QA: `scripts/visual-qa.mjs --strict` passed for 1280×800 and 390×844, light and dark; 0 findings, 0 informational, 0 allowlisted. Receipts and screenshots: `/home/pippenz/.cas/artifacts/cas-69384/visual-final2-desktop/` and `visual-final2-phone/`.
- Keyboard and content: Tab reached “Skip to release details”, “Read the map data”, and the map disclosure with visible 3px outlines. Enter closed and reopened the disclosure. JavaScript-on/off body text matched exactly. Print retained `cas update`, both full archive digests, the issue closure records, and scope limitations. No HTTP requests occurred.
- PDF generation: Node 24.19.0, Playwright 1.63.0, Chromium 153.0.8010.12, PyMuPDF 1.27.2.3. Both A4 and Letter are 4 pages; all eight pages were rendered to PNG and inspected. Both formats have 0 out-of-bounds findings, all required text and full digests, and all 13 unique source URLs. URI annotation totals differ because link labels wrap into format-dependent annotations: A4 37, Letter 38.
- Pagination correction: all report claims and evidence are retained. A distinct scope note follows the user-facing changes; a narrow report-local print rule compacts only install/provenance spacing and keeps the final evidence row and footer together. No orphan footer, split evidence row, clipping, or half-empty final page remains. The report footer links remain present.
- A4 report PDF SHA-256: `ca417e6ad30f13b558f1d5e57ed9b3a0dcc774fd1a1ce8768f0681453f47a0a9`.
- Letter review PDF SHA-256: `c5c5308dcf3ef22b56b5b9a03312f418c54a75feda53ada4a25cc7e172a4c68f`.
- Page PNGs and extraction/bounds receipts: `/home/pippenz/.cas/artifacts/cas-69384/render/final4-A4/` and `final4-Letter/`.

The PDF is untagged; PDF screen-reader behavior is untested. Cursor validation is from a rebuilt Cassy binary with a deterministic local child, not an authenticated Codex or Claude session. No report upload or v3.25.7 Slack publication is claimed.
