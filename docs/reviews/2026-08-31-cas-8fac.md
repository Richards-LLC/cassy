# Phase 4 review — EPIC cas-8fac (memory-system effectiveness program)

Date: 2026-08-31 · Reviewed tree: `epic/epic-memory-system-effectiveness-program-close-the-cas-8fac` @ a8f88c0d vs `main` @ 383b6a99 · Mode: report-only, sharded (5 subsystem shards, one persona set each) · Reviewers: read-only agents; findings verified against the checkout.

## Totals

| Severity | Count |
|---|---|
| P0 | 1 |
| P1 | 4 |
| P2 | 10 |
| P3 | 14 |
| advisory | 2 |

## Dispositions

- **P0 (fixture third-party name/quote)** → fix-round task **cas-2488** (also carries the harness P1 hermeticity finding and its P2s).
- **P1 index-repair lock hang + repair-error disables indexing** (+3 P2) → fix-round task **cas-25a9**.
- **P1 outcome writes silently dropped on pre-m244 schema** (+P2 attribution lost in m244 rebuild, +P3 judge cool-down) → fix-round task **cas-8feb**.
- **P2/P3 polish** (context_for_task limit, ai_context telemetry, "memory" tag literal, cas_stats error rendering, search description, memory-tool scoping/cap logging) → task **cas-6fb5**.
- **Accepted (no code change):** `rule_review_enabled` default flip applies to existing configs that omit `[hooks]` — intentional operator-half decision (P3 in the program), bounded by the factory-worker exemption and the one-blocked-turn reentrancy guard; called out in release notes. Non-atomic ledger-save/DB-write window in ambient recall — pre-existing pattern, noted for a later hardening pass. `daemon.relevance_sampling_enabled=true` with an unconfigured judge — documented residual on the epic (needs a judge before it produces labels).
- **Verified clean by reviewers (no finding):** deletion confinement of legacy-root retirement (no `..`/absolute/symlink escape; `<cas_root>/index` never a canonical reader path); archived entries cannot leak into the canonical index; `mark_index_pending_batch` bound params chunked at 500; no hard-coded index paths outside the resolver; `SearchOptions` has no serde surface for removed boost flags; all three aggregate SELECTs match `parse_aggregate` indices; LEFT JOIN zero-outcome groups and divide-by-zero handled; `hook_command` removal safe for old markdown frontmatter; m245 double-add guarded; retrieval_metrics allow-list safe (all request fields `Option` + `serde(default)`); session_id hashed before SQL; daemon timer guards zero interval and survives sampling errors; default `MemoryTier` is Working so the tier filter does not drop unset entries; gate math sound (div-by-zero guard, improvement never fires, missing selector fails); fixture regex sweeps for emails/IPs/tokens/phones/SSNs/home paths/Slack ids/currency/client codenames clean.

## Findings

| Sev | Shard | File:line | Title | Why it matters | Suggested fix | Conf | Pre-existing |
|---|---|---|---|---|---|---|---|
| P0 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/tests/data/retrieval-eval/fixture.json:2606` | Fixture names a real third party ('Ben') with a personal quote — redaction pass missed a client/personal name | The fixture's own provenance and the committed test `the_fixture_carries_no_secret_or_client_confidential_shapes` (cas-cli/tests/retrieval_eval_test.rs:148-189) both frame this file as shipping to a PUBLIC repository, and the test's own comment says a hit there should be 'treated as a disclosure incident, not a formatting nit.' The redaction pass only regex-scrubbed structural secrets (IPs, emails, tokens, currency) and a curated list of accounting-client codenames (Roark, Cirrus, Rearden, SugarTree, Moultrie, Renovo, QBO, Form 1065, Form 8825). It has no defense against a plain first name. Entry `2026-08-15-4` names a real individual, 'Ben', by first name, states a private fact about his personal device usage ('Ben: MOBILE PRIMARY'), and quotes his own words verbatim ('it will be bens primary surface so it must be equally badass.'). This is exactly the class of 'client/personal name beyond the operator's public identity' the review was asked to find, and it is live in the file today, not a hypothetical. | Redact 'Ben' (and the direct quote attributing personal usage habits to him) from entry 2026-08-15-4's title and body before this fixture is committed/published, the same way the two infra-identity entries were removed outright per the provenance note. Consider extending the redaction test with a name-based check (or a manual name-audit checklist) rather than relying solely on structural/regex patterns and a curated client-domain word list. | 0.85 | False |
| P1 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/src/retrieval_eval.rs:749` | Tests that drive the real crate::hooks::build_context path perform live, uncontrolled network and filesystem I/O against the operator's actual cloud login and home directory | `helpful_memories_production_ranking` calls `crate::hooks::build_context` (cas-cli/src/hooks/context.rs:95), which — unlike `ProductionRunner::rank`, which calls the core `build_context_with_stores` directly — also runs `build_host_constraints_section(cas_root)` (context.rs:228, reads `dirs::home_dir()/.cas`, NOT the fixture's isolated `cas_dir`) and `fetch_personal_patterns_for_context()` / `fetch_team_suggestions_for_context()` (context.rs:252,258; defined at 852 and 945), each of which issues a live `ureq::get` with a 5s timeout to the cloud API whenever `CloudConfig::load()` reports a logged-in state. `CloudConfig::config_path()` resolves via `find_cas_root()` from the process's CWD, and `load()` inherits credentials from `~/.cas/cloud.json` — the real, machine-wide login — when the project config lacks its own token. Since cas-src is itself a CAS-managed project, any developer who has ever run `cas login` on the machine running this test suite will have this test suite make two real outbound HTTPS calls with a real bearer token per invocation, and also read their real home-directory SQLite store, none of it mocked or gated by a `TestEnvGuard`. This is exercised by `the_fast_production_runner_matches_the_real_build_context_path` (12 calls: 6 cases x 2 query modes) and `the_production_selector_does_not_inherit_the_worker_environment` (2 calls). Result: non-hermetic tests whose timing and outcome depend on the operator's login/network state, and — combined with finding on the unasserted 60s budget — a real risk of slow/flaky CI or local runs that nothing in the suite would flag as a budget violation. | Route the 'real path' fidelity tests through a build_context variant (or an injectable CloudConfig/host-dir override) that is hermetically scoped to the test's own tmp cas_dir, or set an env var / config flag the harness can use to force `is_logged_in() == false` and skip the host-constraints home-dir read during these specific tests, matching the isolation already given to `CAS_AGENT_ROLE` via `NeutralHookEnv`. | 0.8 | False |
| P1 | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/hybrid_search/legacy_index.rs:127` | META_LOCK acquisition in retire is a blocking flock with no timeout — it wedges the whole daemon maintenance loop instead of warning | The code comment asserts that a held writer/meta lock 'turns that race into a retryable doctor warning'. That is true for INDEX_WRITER_LOCK (tantivy declares it `is_blocking: false`, so MmapDirectory uses `try_lock_exclusive` and returns LockError::LockBusy), but it is FALSE for META_LOCK: tantivy declares META_LOCK with `is_blocking: true`, and `MmapDirectory::acquire_lock` then calls `file.lock_exclusive()` — an unbounded blocking flock with no retry policy and no timeout. Concrete failure: an old `cas serve` (or any second CAS process) holds `.cas/index/.tantivy-meta.lock` — tantivy takes it in `IndexReader::reload` (src/reader/mod.rs:194) and in `ManagedDirectory::garbage_collect` (src/directory/managed_directory.rs:138) — and is then stopped, wedged, or performing a long GC over the stray index. `repair_legacy_index` is called from `generate_bm25_index`, which runs synchronously inside `daemon::run_maintenance`, which the MCP daemon awaits inside its `select!` arm (cas-cli/src/mcp/daemon.rs:765). The blocked flock therefore stalls not just indexing but the entire daemon event loop (heartbeats, cloud sync, agent reaping) forever, with no log line and no way to observe it except a stuck process. `cas doctor --fix` hangs identically with no output. | Do not use `Directory::acquire_lock(&META_LOCK)` here. Either drop the meta lock entirely (the writer lock already excludes every tantivy writer, and readers of a root you are about to delete cannot be made safe by a lock anyway), or bound it: acquire it on a helper thread with a channel timeout, or open the lock file yourself and use `try_lock_exclusive` in a bounded retry loop, mapping exhaustion to the same `CasError::Other("legacy index metadata is active")` the comment promises. | 0.85 | False |
| P1 | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/daemon/indexing.rs:20` | A failed legacy repair aborts the entire BM25 indexing cycle, silently disabling all background memory indexing with no self-heal path | `generate_bm25_index` returns early on ANY repair error and never reaches `indexer.process_pending(...)`. The stray root is a persistent on-disk condition, so a persistent repair error is permanent: `.managed.json` unreadable or malformed (`std::fs::read(&managed_path)?` / `serde_json::from_slice` at legacy_index.rs:132), a managed name that is a directory (`remove_file` → EISDIR), an EPERM/read-only-FS deletion failure, or a long-lived concurrent writer holding `.tantivy-writer.lock`. From that moment every new memory entry stays `indexed_at IS NULL` forever and is invisible to hybrid search, ambient recall, and the Helpful-Memories scorer — exactly the class of bug cas-bc42 was created to fix. `cas doctor --fix` is not a workaround: it calls the same `repair_legacy_index` and hits the same error. Worse, `repair_legacy_index` clears `indexed_at` for the legacy ids (line 85) *before* the failure, so each 30s cycle re-dirties those rows and then refuses to index them. Only a manual `rm .cas/index/meta.json` recovers. | Make repair advisory, not gating: record the error into `result.errors` and fall through to `BackgroundIndexer::open` + `process_pending` regardless. The canonical index must keep draining the pending queue even when the stray root cannot be retired. | 0.9 | False |
| P1 | store/migrations (correctness+data-integrity) | `crates/cas-store/src/retrieval_store.rs:666` | record_outcome_with_attribution silently drops rows via INSERT OR IGNORE and still reports success | SqliteRetrievalStore::open()/.init() (crates/cas-store/src/retrieval_store.rs:518-526) never runs the numbered migration runner; it only calls execute_batch(RETRIEVAL_SCHEMA) (a CREATE TABLE IF NOT EXISTS no-op on an existing table) and ensure_attribution_column. For an existing project, the ordered migrations (m244's table rebuild that adds 'unresolved' to the outcome CHECK constraint) only run via an explicit `cas doctor`/`cas update`, or the MCP server's ensure_mcp_schema at startup (cas-cli/src/mcp/server/runtime.rs:37-74, called before spawn_daemon at line 263). Hook subprocesses do not call check_migrations/run_migrations anywhere (verified: no 'migration' references in cas-cli/src/ambient_recall.rs, and hooks/context.rs never calls it either); they open SqliteRetrievalStore directly. cas-cli/src/ambient_recall.rs's finalize_ambient_recall_feedback (the normal Stop-hook path, called on effectively every session) writes RetrievalOutcome::Unresolved via record_outcome_with_attribution, and any MCP caller can now pass outcome='unresolved' too per the updated tool docs in crates/cas-mcp/src/types/ops_secondary.rs. On a DB that has not yet run m244, the outcome CHECK constraint still only allows ('used','helpful','ignored','corrected','harmful'). Verified empirically with sqlite3 that `INSERT OR IGNORE` against a CHECK-constraint violation raises no error and reports `changes() = 0` -- SQLite's IGNORE conflict-resolution algorithm applies to CHECK constraints, not just PK/UNIQUE. record_outcome_with_attribution never inspects the affected-row count from conn.execute(...)? and unconditionally returns Ok(event) (line 682). The caller (ambient_recall.rs record_pending_outcomes) treats `.is_ok()` as proof of a durable write and marks the ledger entry outcome_recorded = true, permanently losing that outcome event with no error surfaced anywhere. | Check the affected-row count returned by conn.execute(...) and return an error (e.g. StoreError) when it is 0 and the id was not already present (distinguish 'ignored because idempotent retry of the same id' from 'ignored because a constraint rejected the row'), so callers cannot mistake a silently-dropped write for success. Alternatively/also, gate any write path that can emit the new 'unresolved' outcome (or any new enum variant) behind a schema-readiness check, or have SqliteRetrievalStore::init() invoke the m244 table-rebuild logic itself (mirroring ensure_attribution_column's out-of-band self-heal for m245) so direct-open callers are never stuck on the stale CHECK constraint. | 0.85 | False |
| P2 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/tests/retrieval_eval_test.rs:415` | The load-bearing fast/real equivalence pin only checks 6 of 56 fixture cases | The module doc and the test's own comment call `the_fast_production_runner_matches_the_real_build_context_path` one of only two 'load-bearing' tests: the entire committed baseline is produced by `ProductionRunner`, and this test is the sole license for treating that hoisted runner as equivalent to the real `crate::hooks::build_context` path a session actually receives. It only iterates `fixture.cases.iter().take(6)` (of 56), in both query modes — a ~10.7% sample. A case-specific divergence (e.g. one entry type or tag shape that trips a code path only some cases exercise) could land in `context.rs` and never be caught by this pin, while `the_scorer_replica_matches_production_under_the_production_config` (line 452), the sibling equivalence test for the A/B rig, does cover all 56 cases. The likely reason (avoiding ~29s of real per-case `build_context` calls, per the harness's own ~260ms/case estimate, compounded by the live network calls in the finding above) is not stated in the test. | Either run all 56 cases (accepting the runtime cost, now that the network-I/O finding above should also be fixed to make each call cheap and deterministic), or explicitly document why 6 is a sufficient sample and widen it if the sample ever needs to grow with the fixture. | 0.65 | False |
| P2 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/src/retrieval_eval.rs:839` | ProductionRunner omits rule/skill/knowledge/agent stores and the host-constraints/cloud/mcp-tools sections that real build_context assembles, so the equivalence pin cannot catch drift in those areas | Real production (`build_context_with_token_budget`, cas-cli/src/hooks/context.rs:118-165) populates `ContextStores.project_rule_store`, `.agent_store`, `.project_skill_store`, `.knowledge_store`, and `.rule_match_cache`, all of which can consume the shared token/memory budget alongside Helpful Memories, and then appends Host Constraints / MCP tools / cloud-patterns sections afterward. `ProductionRunner::rank` (retrieval_eval.rs:819-856) starts from `ContextStores::empty()` and only ever sets `project_store`, `task_store`, `entry_scorer`, and `recent_files` — every other store stays `None`. Because the fixture's materialized corpus never seeds rules, skills, knowledge, or an agent store, today's measured ranking happens to be identical between the two paths (which is exactly why the equivalence test at line 401 currently passes), but that equivalence is not proof against a future change where rule/skill/knowledge content starts competing for the same context budget — the fixture and the runner both stay silent on that dimension. The module's own docs describe `helpful_memories_production` as 'the row that describes what a session actually receives,' which overstates what is actually pinned. | Either seed the fixture corpus with a representative rule/skill so the equivalence test can actually exercise budget contention, or explicitly document in the module header that the harness measures Helpful-Memories ranking in isolation from rules/skills/knowledge/host-constraints, and that a change to how those compete for budget is a known blind spot. | 0.7 | False |
| P2 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/src/hooks/handlers/handlers_session.rs:162` | Harness never measures or discloses the build_context_ai branch that real SessionStart can take | `handle_session_start` chooses between `build_context_ai` and `build_context` based on `config.hooks.ai_context` (default `false`). The retrieval_eval module doc (retrieval_eval.rs:12-18) claims `helpful_memories_production` is 'the REAL SessionStart path,' but that is only true for a project with `ai_context` left at its default. No selector, row, or doc caveat covers the AI-context branch, so a project that has enabled it would receive rankings this harness cannot describe or gate at all. | Add a one-line caveat to the retrieval_eval module doc noting the measured path assumes `hooks.ai_context = false` (the default), so a reader knows the AI-context branch is out of scope rather than assuming full SessionStart coverage. | 0.55 | True |
| P2 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/src/retrieval_eval.rs:764` | The documented 60-second suite runtime budget is never asserted anywhere | `ProductionRunner`'s doc comment justifies hoisting store/scorer opens by citing a runtime budget ('~58 s for the full matrix and would put the suite over its runtime budget'), and the review brief calls out this exact 60s budget. Neither `retrieval_eval.rs` nor `retrieval_eval_test.rs` contains any `Instant`/`Duration`/elapsed-time assertion. If a future change reintroduces the per-case ~260ms path (or, per the network-I/O finding above, live cloud calls start timing out or retrying), the suite will simply get slower — nothing fails, nothing warns, and CI absorbs it silently until someone notices. | Add a soft assertion (e.g. wrap `run_all` or the whole test body in an `Instant::now()` check that fails or at least prints a loud warning past some generous multiple of the expected budget) so a regression in per-case cost is caught by the suite itself rather than by someone eyeballing CI minutes. | 0.75 | False |
| P2 | hooks/context/ambient (correctness+perf+contracts) | `cas-cli/src/hooks/handlers/handlers_middle/session_stop/mod.rs:251` | rule_review_enabled default flip changes Stop-blocking behavior for every project without an explicit [hooks] section | config.hooks is None whenever a project's config.toml has no [hooks] section at all (Config::default() leaves it None). Previously that path resolved rule_review_enabled to false; now it resolves to true, so any project/CI harness that never explicitly configured [hooks] silently gains a new Stop-blocking behavior once >= rule_review_threshold (default 5) draft rules accumulate in the shared store. The blast radius is bounded (factory workers are exempted upstream via is_factory_worker at stop_flow.rs:619/630, and the stop_hook_active reentrancy guard limits it to one blocked turn), but a genuinely single-shot/non-interactive invocation (e.g. `claude -p` in a script or eval harness with no loop to act on a 'block' decision) has no such second turn to clear the block, and its behavior is outside this diff's control. This is a real default-on behavior change for existing configs, not just new ones. | Consider only defaulting to the new-project default when there is no persisted config.toml at all (fresh install), and preserving unwrap_or(false) for any config.toml that predates this change but omits [hooks] explicitly, or document/announce the flip prominently since it changes existing deployments' Stop semantics without a config migration step. | 0.6 | False |
| P2 | mcp/config/daemon (correctness+API/config) | `cas-cli/src/mcp/tools/service/agent_search_system/search_context.rs:172` | context_for_task path still hardcodes limit=5, bypassing the new hooks.context_limit threading | The epic threads hooks.context_limit into `cas_context` (the no-task_id branch of the `context` action) but the sibling task-focused branch of the exact same MCP action still falls back to a literal 5. An operator who sets hooks.context_limit=10 gets 10 entries when calling action=context without task_id, but silently gets 5 when task_id is set — an inconsistent, undocumented behavior split within one action. SessionStartRequest.limit's schema doc explicitly promises 'falls back to hooks.context_limit, default 5' and nothing in the request distinguishes the task_id case, so callers have no way to know this path is exempt. | Replace `req.limit.unwrap_or(5)` with the same `req.limit.unwrap_or_else(\|\| Config::load(&self.inner.cas_root).unwrap_or_default().context_limit())` pattern used by the non-task_id branch (or thread cas_context_for_task through the same context_limit() helper used elsewhere in this diff), so both branches of the `context` action honor the configured limit identically. | 0.85 | False |
| P2 | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/hybrid_search/legacy_index.rs:84` | Entries are re-queued from a snapshot taken before the writer lock is held; documents a concurrent old daemon writes into the legacy root in that window are deleted without ever being re-queued | `repair_legacy_index` reads the entry-id set in `inspect_legacy_index` (which takes no lock at all), calls `mark_index_pending_batch` on that snapshot, and only then enters `retire_legacy_index_files`, which is where `INDEX_WRITER_LOCK` is first acquired. A pre-fix `cas serve` is by construction still writing to that same legacy root — that is the entire premise of the repair — so between the snapshot and the lock it can commit new entry documents and call `store.mark_indexed_batch` on them. Those entries are then (a) not in the re-queued id list, (b) marked `indexed_at = NOW` by the old daemon, and (c) deleted along with the rest of the legacy segments. They are permanently absent from the canonical index and permanently excluded from `list_pending_index` (which selects only `indexed_at IS NULL OR updated_at > indexed_at`) until someone edits the memory again. Silent, unbounded-in-principle memory loss from search, with no doctor check that would ever surface it (the stray root is gone, so `legacy_search_index_check` reports Ok). | Invert the order so the exclusive writer lock covers the read: acquire `INDEX_WRITER_LOCK` on the legacy root first, then inspect (re-read the id set under the lock), then `mark_index_pending_batch`, then delete, then release. Restructure `retire_legacy_index_files` to take the already-open locked `Index` (or return the id set) instead of re-opening it. | 0.7 | False |
| P2 | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/hybrid_search/legacy_index.rs:143` | A mid-loop deletion failure leaves a half-deleted legacy root that can never be inspected again, permanently poisoning every later repair attempt | `retire_legacy_index_files` deletes the managed segment files first and `meta.json` last, and any single `remove_file` error aborts with `?`. If the loop fails partway (EPERM/EROFS, a `.managed.json` entry that names a directory, or Windows' refusal to delete a mapped file), the root is left with `meta.json` present but segment files missing. On the next cycle `inspect_legacy_index` passes its `meta.json.is_file()` gate, `Index::open_in_dir` succeeds, and `searcher.search(&AllQuery, &DocSetCollector)` fails on the missing segment files — so the entry-id set can no longer be recovered from the legacy root at all, and (combined with the early-return in daemon/indexing.rs:20) BM25 indexing is dead permanently. The failure is also not atomic in the other direction: nothing records that a repair was attempted, so there is no resume point. | Delete `meta.json` FIRST (after the id set is safely re-queued), then `.managed.json`, then the segment files, and make the segment-file loop best-effort: accumulate errors and log them rather than aborting. Removing `meta.json` first makes the root inert and the operation idempotent — a crash at any later point leaves a state that `inspect_legacy_index` correctly reports as `None` and that leaks at worst some orphan `.fast`/`.idx` bytes. | 0.65 | False |
| P2 | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/hybrid_search/search_index_impl.rs:340` | Synchronous wait_merging_threads on every batch, combined with an unbounded max_per_run in the repair path, can hold the daemon's maintenance arm for the duration of a full re-index | `index_entries_batch_and_merge` is now the only path `BackgroundIndexer` uses (both `process_batch` and the `process_single` fallback), and `wait_merging_threads` joins the writer's worker threads and the merging thread with no timeout — and the merging thread itself takes the blocking `META_LOCK` during `garbage_collect_files`. `repair_legacy_index` then drives `process_pending` with `batch_size: 256, max_per_run: usize::MAX`, so a store with e.g. 50k re-queued entries performs ~200 sequential commit+full-merge rounds in one call. That call runs inside `daemon::run_maintenance` (cas-cli/src/daemon/maintenance.rs:65), which the MCP daemon awaits inside its `select!` arm (cas-cli/src/mcp/daemon.rs:765), so agent reaping, lease reclamation, worktree cleanup, event pruning and cloud sync are all stalled for the whole duration. `list_pending_index(usize::MAX)` additionally materialises every pending entry (content included) in memory in one Vec. | Bound the repair drain the same way the daemon bounds itself: use the caller's `IndexingConfig` (or a fixed cap such as `max_per_run: 5_000`) and let subsequent maintenance cycles finish the queue — the rows are already marked pending, so progress is durable. Separately, call `wait_merging_threads` once at the end of `process_pending` rather than once per batch. | 0.7 | False |
| P2 | store/migrations (correctness+data-integrity) | `cas-cli/src/migration/migrations/m244_retrieval_outcomes_add_unresolved.rs:31` | m244's table rebuild silently discards attribution provenance data added out-of-band before the migration runs | ensure_attribution_column (crates/cas-store/src/retrieval_store.rs:353-369) runs unconditionally on every SqliteRetrievalStore::init() call, independent of the migration ledger, and adds the `attribution` column via ALTER TABLE the first time any direct-store caller opens the store -- this can happen well before the ordered m244/m245 migrations execute (see the previous finding for why hook-driven opens bypass the migration runner). cas-cli/src/ambient_recall.rs's record_pending_outcomes calls record_outcome_with_attribution with RETRIEVAL_ATTRIBUTION_AUTOMATIC for outcomes such as Used/Helpful/Ignored/Corrected -- these values are already valid under the OLD CHECK constraint, so (unlike the Unresolved case) they insert successfully with `attribution='automatic'` written into the out-of-band column. When the ordered migrations later run, m244's `up` rebuilds retrieval_outcomes via `CREATE TABLE retrieval_outcomes_new (... no attribution column ...)` and `INSERT OR IGNORE INTO retrieval_outcomes_new (id, query_id, result_id, outcome, actor_hash, session_hash, correction_ref, created_at) SELECT <same 8 columns, no attribution> FROM retrieval_outcomes`, then `DROP TABLE retrieval_outcomes`. Any `attribution` value already present on the source table (added by ensure_attribution_column) is not selected and is destroyed when the old table is dropped. m245's subsequent `ALTER TABLE retrieval_outcomes ADD COLUMN attribution TEXT NOT NULL DEFAULT 'explicit'` then re-adds the column with the default for every row, silently relabeling rows that were genuinely 'automatic' (or, via the daemon's judge sampler, 'judge') as 'explicit'. This corrupts exactly the attribution signal this epic is designed to produce for measuring rule-lifecycle effectiveness, and it happens with no error, warning, or test coverage -- the migration's own test (migration_preserves_legacy_rows_and_accepts_unresolved) only exercises a source table without an attribution column at all. | Make m244's rebuild attribution-aware: detect whether the source retrieval_outcomes table already has an `attribution` column (e.g. via pragma_table_info) and, if so, carry it forward in both the CREATE TABLE and INSERT OR IGNORE SELECT list (defaulting to 'explicit' only for rows that truly lack it), instead of unconditionally omitting it. This also removes the current ordering dependency where m244 must always run before m245 gains meaning. | 0.65 | False |
| P3 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/src/retrieval_eval.rs:919` | ConfigurableHybridScorer duplicates HybridContextScorer's blending arithmetic beyond the acknowledged contextual_overlap_bonus reuse | The module doc states only `contextual_overlap_bonus` is reused from production and the rest is 'a deliberate replica,' but the replica reproduces `HybridContextScorer::score_with_hybrid`'s options (cas-cli/src/hooks/scorer.rs:40-66) and `score_entries`'s blend arithmetic (`hs * 0.7 + basic_normalized * 0.3`, `basic_score / 3.0`) verbatim as separate magic-number literals. This is mitigated by `the_scorer_replica_matches_production_under_the_production_config`, which pins the two implementations to identical output under `ScorerConfig::PRODUCTION` across every fixture case, so a divergence WILL be caught by the full test suite — but only if that test runs. A narrower `cargo check`/scoped `--lib` iteration (the documented default worker loop in this repo's CLAUDE.md) would not catch a tuning change to the 0.7/0.3/3.0 constants in scorer.rs that isn't mirrored here until the integration suite runs. | Consider having `HybridContextScorer` itself take a `ScorerConfig`-shaped options struct (with `temporal` defaulting to production's `true`) instead of hardcoding `enable_temporal: true`, so `ConfigurableHybridScorer` could wrap/delegate to the real implementation instead of re-typing its body; this removes the duplication risk entirely rather than relying on a pin test to catch it after the fact. | 0.6 | False |
| P3 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/src/lib.rs:63` | pub retrieval_eval module (1594 lines, zero production callers) is unconditionally compiled into the shipped cas binary | `pub mod retrieval_eval;` has no `#[cfg(test)]` or feature gate, and nothing in `cas-cli/src` outside the module itself references `retrieval_eval::` — it exists solely to be driven from `cas-cli/tests/retrieval_eval_test.rs`. The lib.rs comment above the module list explains this is necessary (`cfg(test)` is false when the lib is compiled as a dependency for `tests/*.rs` integration binaries, so a `cfg(test)`-gated module would be invisible to the very test that needs it), and the pattern matches the pre-existing sibling `retrieval_parity` module, so this is a known, deliberate, and already-established trade-off rather than a new defect. It is still ~1600 more lines of eval-only orchestration code, SQL, and Tantivy-writing logic permanently linked into the release binary with no functional purpose there. | If binary size/surface area becomes a concern, consider a Cargo feature (e.g. `eval-harness`, enabled by the test profile via `[dev-dependencies]`-style `required-features` on the test target) rather than an unconditional `pub mod`, for both this module and its `retrieval_parity` sibling. | 0.5 | True |
| P3 | eval harness + tests (testing+maintainability+privacy) | `cas-cli/tests/hooks_test/learning_rule_duplicate.rs:253` | Edited test remains permanently #[ignore]d — the fix it carries never runs | The diff renames `test_stop_not_blocked_without_rule_review_config` to `test_stop_not_blocked_with_rule_review_disabled` and adds real setup (writing `[hooks.stop]\nrule_review_enabled = false` into the temp config) to preserve the explicit opt-out contract now that the feature defaults to enabled. The test still carries `#[ignore = "CLI commands removed - tests need MCP fixtures"]`, unchanged by this diff, so none of this new assertion logic ever executes in CI. The `#[ignore]` predates this PR, but the PR invests real effort in a test body that stays silently retired. | Either restore this test with the MCP-based fixtures the ignore reason calls for, or drop the edit and leave the pre-existing ignored test alone rather than investing new logic in code that cannot run. | 0.5 | True |
| P3 | hooks/context/ambient (correctness+perf+contracts) | `crates/cas-core/src/hooks/context/build_start.rs:806` | 'memory' surfaced-item detection depends on Debug-format string of ContextItemType with no compile-time link to the literal it is matched against | The Helpful Memories loop emits the surfaced-item type as `format!("{:?}", item.item_type)`, which for `ContextItemType::Memory` (a plain enum, #[derive(Debug, Clone, Copy)], no custom Display/serde rename) currently renders exactly "Memory". The consumer in cas-cli/src/hooks/context.rs:181 does `item_type.eq_ignore_ascii_case("memory")`, which happens to tolerate the capitalization difference today. This works, but the contract between producer and consumer is an accidental byproduct of Rust's default Debug formatting for a unit variant, not an explicit shared constant. Any future change to ContextItemType (adding a data-carrying variant, a custom Debug impl, or renaming Memory to something else) would silently break memory_ids collection with no compiler error, and the callback's other producers (build_start.rs:53, 673, 881) already use explicit literal lowercase strings ("rule", "skill", "related") rather than Debug formatting, showing this call site is the outlier. | Emit a literal lowercase "memory" string at build_start.rs:806-809 (matching the other three callback call sites) instead of Debug-formatting the enum, and drop the case-insensitive comparison in favor of an exact match once both sides agree on the literal. | 0.7 | False |
| P3 | hooks/context/ambient (correctness+perf+contracts) | `cas-cli/src/ambient_recall.rs:2447` | exact-id memory-tool detection matches by bare last path segment, not scoped to the cas MCP server | exact_memory_retrieval_ids() identifies the memory tool solely by `tool_name.rsplit("__").next()` case-insensitively equaling "memory", with no check that the preceding segment is the cas server (e.g. `cas`/`cs`). Any other MCP server that exposes a tool whose final `__`-delimited segment is literally "memory" (or a bare tool named "memory" with no delimiter at all) would have its list/show/recent/get responses scanned for "id" fields and those ids attributed as exact evidence-id matches against the ambient recall ledger, marking unrelated injected cards as `used`. No such conflicting tool exists among the MCP servers currently wired into this environment, so this is a latent robustness gap rather than an active bug. | Match on the full tool name (e.g. ends_with("__cas__memory") / ends_with("__cs__memory") or an explicit allow-list of known server prefixes) instead of only the trailing segment, so a differently-namespaced third-party "memory" tool cannot be attributed to CAS's ledger. | 0.35 | False |
| P3 | hooks/context/ambient (correctness+perf+contracts) | `cas-cli/src/ambient_recall.rs:2410` | LEDGER_ENTRY_CAP (128) can silently truncate legitimate ids out of a large memory list/recent response | collect_exact_id_fields stops recursing as soon as 128 distinct ids have been collected from a tool_response, with no signal to the caller that truncation occurred. If a `memory list`/`recent` call returns more than 128 entries (or the tool response nests other unrelated 128+ id-bearing objects ahead of the ones that matter), ids beyond the cap are silently dropped from exact-match attribution and their corresponding ledger entries fall back to the weaker 4-character substring heuristic (or no match at all). Given typical injection/ledger sizes this is unlikely to bite in practice, but it is a real, un-signaled truncation path. | If this matters in practice, raise the cap for this specific extraction path (it bounds an in-memory Vec from a single tool response, not the ledger itself) or log when truncation occurs so it's observable. | 0.3 | False |
| P3 | hooks/context/ambient (correctness+perf+contracts) | `cas-cli/src/ambient_recall.rs:1851` | Ambient-recall DB write (record_ambient_artifact_surfaces / record_ambient_query) and RecallLedger.save() are two separate non-atomic persistence steps; a crash between them lets the same rule/skill card be re-selected and double-recorded later in the same session | render_packet() only re-injects a candidate whose evidence_id has not already been marked seen in the on-disk RecallLedger (ledger.has_seen at ambient_recall.rs:2751), so under normal operation the same rule/skill cannot be recorded twice via record_ambient_artifact_surfaces()/rules.surface_count within one session. However, the DB write at line 1851 (and record_ambient_query just above it) commits before ledger.record()+ledger.save() persists the 'seen' state to disk. If the hook process is killed/times out or the ledger file write fails in that narrow window, the next ambient-recall invocation in the same session reloads the stale ledger, treats the same candidate as new again, and re-runs record_ambient_artifact_surfaces, incrementing rules.surface_count and inserting a duplicate surfaced_artifacts row for a card that was already counted. This ordering pattern (DB commit before ledger persist) pre-dates this diff for record_ambient_query; this change extends the same non-atomic pattern to a second, counter-mutating side effect (rules.surface_count), which is more consequential to double-count than a pure telemetry row. | Persist ledger.save() (or at least the newly-seen evidence ids) before or atomically with the DB writes, or make record_batch/rules.surface_count idempotent per (session_id, artifact_id) so a replay from a stale ledger cannot double-increment the counter. | 0.35 | True |
| P3 | hooks/context/ambient (correctness+perf+contracts) | `cas-cli/src/hooks/context.rs:426` | SessionStart retrieval telemetry (retrieval_queries/retrieval_query_results) is only recorded on the non-AI context path; build_context_ai never calls record_context_session_start_query | record_context_session_start_query is only invoked from build_context_with_token_budget (called by build_context), gated on hook_event_name == "SessionStart". build_context_ai (used when [hooks].ai_context is enabled and the AI call succeeds) builds its own context string independently and never calls build_context_with_token_budget, so no retrieval_queries/retrieval_query_results rows are recorded for SessionStart injections made through the AI-context path. This is a coverage gap rather than a correctness bug: any project using ai_context=true gets no Helpful-Memories retrieval telemetry from SessionStart at all, silently, which will under-report memory usefulness_rate/coverage_rate for those sessions. | If SessionStart retrieval telemetry is meant to cover the ai_context path too, call record_context_session_start_query (or an equivalent) with the AI-selected memory ids from build_context_ai as well. | 0.65 | False |
| P3 | mcp/config/daemon (correctness+API/config) | `cas-cli/src/mcp/tools/core/system.rs:838` | System-stats retrieval funnel/precision text conflates 'no data' with 'store failed to open' | `SqliteRetrievalStore::open(&self.cas_root).ok()` silently swallows any open/aggregate error, and the fallback strings ('unavailable (0 judge labels)', 'No retrieval results recorded') then assert a concrete zero. This directly contradicts the sibling doc comment in cas-store ('A missing denominator is None, not a fabricated zero') — a genuine store-open failure (e.g. a locked/corrupted cas.db) is reported identically to a healthy store with zero retrieval history, which could mask an operational problem from `cas_stats` readers. | Distinguish the open/query-error case from the genuinely-empty case, e.g. propagate the error into the output text ('retrieval store unavailable: <error>') instead of reusing the same zero-labels wording, or surface it via the existing doctor/preflight error-reporting convention. | 0.55 | False |
| P3 | mcp/config/daemon (correctness+API/config) | `cas-cli/src/mcp/tools/service/mod.rs:799` | Tool description drops 'time' from the history action summary even though since/until filtering is still fully supported | The `search` tool's top-level description previously advertised history search as text/path/time; this diff (in the course of rewording the retrieval_metrics blurb) silently dropped '/time', even though `since`/`until` (relative or RFC3339 time bounds) remain fully implemented and passed through in history.rs. The per-field schemars docs for since/until still describe them correctly, but an agent skimming only the top-level action summary now under-discovers a still-supported capability — a schema-description/behavior drift introduced incidentally by this diff. | Restore 'text/path/time' (or otherwise re-mention time-bounded filtering) in the top-level `search` tool description so it stays consistent with the still-supported since/until fields. | 0.6 | False |
| P3 | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/hybrid_search/legacy_index.rs:145` | Retire creates .tantivy-writer.lock / .tantivy-meta.lock in the legacy root and never removes them | `MmapDirectory::acquire_lock` opens the lock path with `.create(true)`, so acquiring the two locks materialises `.cas/index/.tantivy-writer.lock` and `.cas/index/.tantivy-meta.lock` if they were absent. The retire list only removes `meta.json` and `.managed.json`, so after a successful repair the legacy directory is left holding two stray tantivy lock files forever. This contradicts the change description ("deleting only Tantivy-managed files ... plus lock/meta files") and leaves a directory that looks half-alive to an operator or to any future tool that probes for tantivy artifacts. It is not a correctness bug today because `inspect_legacy_index` gates on `meta.json`, but the residue defeats the point of retiring the root. | Drop both `DirectoryLock` guards before the final cleanup and extend the list to `["meta.json", ".managed.json", ".tantivy-writer.lock", ".tantivy-meta.lock"]` (still best-effort via `remove_file_if_present`). | 0.9 | False |
| P3 | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/hybrid_search/legacy_index.rs:47` | inspect_legacy_index materialises every document in the stray index on every doctor run and every daemon cycle | The stray root is exactly the place where a daemon has been quietly writing entries for however long the bug existed, so it can hold tens of thousands of documents. `DocSetCollector` builds a `HashSet<DocAddress>` of the entire index and the loop then calls `searcher.doc(address)` (a stored-field fetch, decompressing the block) for every document, purely to collect ids. This runs on every plain `cas doctor` invocation (the read-only `legacy_search_index_check` at doctor.rs:838) and on every daemon maintenance cycle while the root exists — including the recreated-empty-root loop, where an old binary keeps recreating `.cas/index/meta.json` after each retire. Concrete symptom: `cas doctor` takes seconds and allocates hundreds of MB on a machine with a large stray index. | Give the read-only doctor check a cheap variant that reports `searcher.num_docs()` without fetching stored fields, and only do the full id extraction inside `repair_legacy_index`. Streaming the ids per-segment via the `id` fast/stored field, or capping the scan, would also avoid the full-index HashSet. | 0.7 | False |
| P3 | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/hybrid_search/legacy_index.rs:55` | Non-entry documents in the legacy root are deleted with no re-queue path, and requeued_entries counts ids rather than rows updated | `inspect_legacy_index` reports `documents` (all doc types) but only harvests ids where `doc_type == "entry"`; `repair_legacy_index` then deletes every managed file. Any task/rule/skill/artifact/code_symbol document that ever reached the stray root is destroyed with no queue to rebuild it — the store's pending-index mechanism (`indexed_at`) exists only for entries. Realism is low today (pre-fix, only `BackgroundIndexer` wrote to `<cas_root>/index` and it indexes entries only), but the repair reports `legacy_documents` and `requeued_entries` side by side and treats a divergence as unremarkable. Relatedly, `requeued_entries` is `state.entry_ids.len()`, not the number of rows the UPDATE actually touched, so ids that were deleted or purged from the store since they were indexed inflate the doctor's success message (`UPDATE entries SET indexed_at = NULL WHERE id IN (...)` matching zero rows is not an error). | Fail loudly (or at least warn in the doctor row) when `documents > entry_ids.len()`, so a stray root containing non-entry doc types is not silently discarded. Have `store_mark_index_pending_batch` sum `conn.execute` row counts and return them so `requeued_entries` reports rows actually re-queued. | 0.6 | False |
| P3 | store/migrations (correctness+data-integrity) | `crates/cas-store/src/retrieval_store.rs:713` | sample_injected_relevance can re-offer the same unjudged result on every sampling pass with no backoff | The eligibility query excludes a (query_id, result_id) pair only when a row with attribution='judge' already exists for it. When the judge callback returns None (unlabeled) for a sampled result, no row is written for it at all (report.unlabeled += 1; continue at line 751-753), so the NOT EXISTS predicate still matches it next time. Since results are ordered by q.created_at DESC, r.rank ASC with a LIMIT, a persistently-unjudgeable recent item can keep consuming slots in the fixed-size sample (relevance_sampling_sample_size, default 20) on every scheduled pass (default weekly) until enough newer queries push it past the LIMIT window, wasting judge budget that could go to genuinely new candidates. | Record a sentinel row (or a separate 'attempted' marker/timestamp) even when the judge returns None, so a subsequent pass can apply a cool-down before re-offering the same item, or simply cap how many times a given (query_id, result_id) can be resampled. | 0.6 | False |
| advisory | mcp/config/daemon (correctness+API/config) | `cas-cli/src/mcp/daemon.rs:618` | daemon.relevance_sampling_enabled defaults to true with no wired-in judge, so the weekly pass is a permanent no-op today | The only call site for the sampling job uses `run_unconfigured_injected_relevance_sampling`, whose judge closure always returns `Ok(None)`. Because the SQL selection in `sample_injected_relevance` only excludes rows already judge-labelled (attribution='judge'), and this default judge never writes a judge label, every idle weekly tick re-selects the same unjudged rows and does nothing (labels_recorded stays 0 forever) for every project running `cas serve` with the (enabled-by-default) config. This matches the epic's stated intent ('fail-open... leaves an honest null precision until a judge is attached'), but the config metadata description ('Enable the weekly bounded judge pass...') does not disclose that no judge currently exists to attach, so an operator reading `cas config get daemon.relevance_sampling_enabled` would reasonably believe real evaluation is happening. | Either default `daemon.relevance_sampling_enabled` to false until a real judge integration lands, or add a line to the config metadata description clarifying that the scheduled pass is inert without an attached judge. | 0.5 | False |
| advisory | search/index repair (security+adversarial+concurrency, Opus) | `cas-cli/src/hybrid_search/search_index_impl.rs:87` | SearchIndex::open still rebuilds by deleting the whole index directory on any schema field-count mismatch — a mixed-version fleet wipes the canonical index | Unchanged by this patch but directly adjacent to it, and the patch's concurrency story assumes an OLD binary may still be running. `SearchIndex::open` compares the on-disk field count against the compile-time `EXPECTED_FIELD_COUNT` (currently 15) and, on mismatch, does `std::fs::remove_dir_all(index_dir)`. Concrete scenario: after the fix, an older CAS binary whose `EXPECTED_FIELD_COUNT` predates the cas-7b1e frontmatter fields opens `<cas_root>/index/tantivy` (readers used that path since the initial release, hybrid.rs:252) and silently deletes the canonical index the new daemon just rebuilt — then the new daemon rebuilds it, and they thrash. Note the patch materially *reduces* the blast radius of this code: pre-fix, `BackgroundIndexer::open(cas_dir.join("index"))` on a mismatch would have run `remove_dir_all(<cas_root>/index)`, taking `index/tantivy`, `index/code`, `index/code-vectors` and `index/knowledge-vectors` with it. Worth an explicit note in the release/rollout so operators stop old daemons before running `cas doctor --fix`. | Version the index directory (e.g. `index/tantivy-v15`) so a version mismatch is a different path rather than a destructive rebuild, and/or take `INDEX_WRITER_LOCK` before the `remove_dir_all`. At minimum, document that all CAS processes must be stopped before running `cas doctor --fix`. | 0.7 | True |
